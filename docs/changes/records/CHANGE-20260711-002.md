# CHANGE-20260711-002: 统一行情工作区第一版（阶段三）

## 变更编号
CHANGE-20260711-002

## 任务名称
统一行情工作区改造 · 阶段三：三栏工作区第一版

## 需求出处
advice.md + 用户阶段三指令（堆叠 PR，base=feat/user-admin-shell-v1）

## 修改前行为
- `/market` 直接复用 `WatchlistPage` 作为阶段性真实内容（阶段二）。
- `StockDetailPage` 内部直接调用所有数据 hooks，未抽取复用。
- 无三栏工作区；无 URL 状态管理（scope/symbol/timeframe）。
- `detailNavigation.resolveBackPath` watchlist fallback 为 `/watchlist`。

## 修改后行为
- `/market` 渲染 `MarketWorkspacePage`（三栏布局）：
  - 左栏 `MarketInstrumentPane`：`scope=watchlist` 用 `useWatchlistMonitorStatus`；`scope=market` 用 `useInstruments` 搜索（≥2字符，限50条，不发 N+1）。
  - 中栏 `StockResearchWorkspace`：唯一 K 线研究区，复用 `useStockResearchData` hook。
  - 右栏 `StockStructuralStatePanel`：可收起；收起时不挂载、不请求 structural/temporal 数据。
- URL 状态：`/market?scope=watchlist|market&symbol=xxx&timeframe=1d`；scope/symbol/timeframe 进 URL；右栏折叠和 viewport 留本地。
- 切换股票不整页刷新（改 URL symbol 参数，React Query 缓存复用）。
- `useStockResearchData` 集中 bars/indicators/quote/events/watchlist/memo 请求与组装。
- `detailNavigation.resolveBackPath` watchlist fallback 改为 `/market?scope=watchlist`。
- 图表 timeframe 仅展示，不改变 1d+15m 监控配置或 1m 事件触发口径。
- 不新增后端 API；CaptureStockPage 独立不变；不删除 IndexPage/ScreenerPage/CaptureStockPage。

## 影响模块
- 前端：features/market-workspace/、features/stock-research/、App.tsx、pages/detailNavigation.ts
- 文档：AGENTS.md、docs/current/00/04/05、docs/maps/frontend-route-map、test-coverage-map

## 修改文件
### 新增
- `frontend/src/features/market-workspace/MarketWorkspacePage.tsx` — 三栏工作区页面
- `frontend/src/features/market-workspace/MarketWorkspace.module.scss` — 工作区样式
- `frontend/src/features/market-workspace/MarketInstrumentPane.tsx` — 左栏组件
- `frontend/src/features/market-workspace/MarketInstrumentPane.module.scss` — 左栏样式
- `frontend/src/features/market-workspace/marketWorkspaceUrlState.ts` — URL 状态编解码纯函数
- `frontend/src/features/market-workspace/__tests__/marketWorkspaceUrlState.test.ts` — 7 用例
- `frontend/src/features/stock-research/StockResearchWorkspace.tsx` — 中栏研究区组件
- `frontend/src/features/stock-research/useStockResearchData.ts` — 数据组装 hook
- `docs/changes/records/CHANGE-20260711-002.md` — 本记录

### 修改
- `frontend/src/App.tsx` — /market 路由从 WatchlistPage 改为 MarketWorkspacePage；移除 WatchlistPage import
- `frontend/src/pages/detailNavigation.ts` — resolveBackPath watchlist fallback `/watchlist`→`/market?scope=watchlist`
- `frontend/src/pages/__tests__/detailNavigation.test.ts` — 更新 fallback 断言
- `AGENTS.md` — 第14条更新阶段三工作区描述
- `docs/current/00-product-business.md` — /market 描述更新为行情工作区
- `docs/current/04-frontend-ux.md` — /market 路由表更新 + §2.4 工作区描述
- `docs/current/05-testing-acceptance.md` — 追加阶段三阻断验收
- `docs/maps/frontend-route-map.md` — /market 行更新为 MarketWorkspacePage
- `docs/maps/test-coverage-map.md` — 追加工作区 URL 状态测试条目
- `docs/changes/CHANGELOG.md` — 索引登记

## 文档更新
- AGENTS.md §12.14.8-9
- docs/current/00-product-business.md §2 导航结构
- docs/current/04-frontend-ux.md §2 路由表 + §2.3 尚未完成 + §2.4 工作区
- docs/current/05-testing-acceptance.md §3 工作区测试
- docs/maps/frontend-route-map.md §1 /market 行
- docs/maps/test-coverage-map.md §1 追加
- 不修改 02-data-api-contracts.md / api-route-map.md（无 API 变化）
- 不修改 03-jobs-integrations-operations.md / worker-job-map.md（无任务变化）

## 测试证据
- `node --test` 全部前端测试：32 passed, 0 failed（含 appNavigation 6 + routeStructure 10 + detailNavigation 3 + marketWorkspaceUrlState 7 + screenerUrlState 6）
- `tsc --noEmit`：0 errors
- `eslint` 改动文件：0 errors
- `vite build`：成功（221 modules, 3s），构建后删除临时产物
- 4 个文档检查：全部 PASS

## Git 分支
feat/market-workspace-v1（base=feat/user-admin-shell-v1）

## Git Commit
（见 git log）

## 数据库迁移
无

## 配置变化
无

## 风险
1. `StockDetailPage` 自身仍直接调用 hooks，未切换到复用 `StockResearchWorkspace`——存在数据组装逻辑重复，下一阶段统一。
2. `event_id` URL 参数已保留在 `/market` 路由但 MarketWorkspacePage 未消费——下一阶段实现。
3. `WatchlistPage` import 已从 App.tsx 移除，但页面文件保留（禁止删除），后续阶段需决定是否删除。
4. `MarketWorkspacePage` 的 `StockResearchWorkspace` 传入 `source="watchlist"` 固定值，从 ScreenerPage 进入工作区时 source 可能需要动态化——下一阶段。

## 遗留问题
- StockDetailPage 切换到复用 StockResearchWorkspace → 下一独立 PR
- event_id 自然语言事件解释 → 下一阶段
- 复盘模式 → 本轮不开发

---

## 收口修复（同分支追加 commit）

### 根因
advice.md review 发现 PR #70 第一版存在以下缺陷：
1. **P0 timeframe 双重状态**：`StockResearchWorkspace` 内部 `useState('1d')` 与 `MarketWorkspacePage` 从 URL 传入 `useStockResearchData` 的 timeframe 脱节。URL `?timeframe=15m` 时数据为 15m 但图表以 1d 渲染。
2. **P0 请求未门控**：`useWatchlistMonitorStatus` 和 `useInstruments` 均无 `enabled` 参数，导致 watchlist scope 也请求 `/instruments`，market scope 也请求 `/watchlist/monitor-status`，搜索词不足 2 字符时仍请求前 50 只股票。
3. **P1 无用请求**：`useStockResearchData` 请求了 `MarketWorkspace` 未使用的 watchlist/batchInstruments/stockMemo。
4. **P1 URL 字段丢失**：URL 状态只有 scope/symbol/timeframe，每次 `setSearchParams` 会清除未来可能新增的 source/strategy/event_id；非法 timeframe 原样传后端。
5. **P1 策略硬编码**：`StockResearchWorkspace` 即使收到 `source='selection'` 仍固定 `WATCHLIST_MONITOR`。
6. **P1 周期文案错误**：所有 timeframe 下状态条都显示"完整日线"。
7. **P2 注释失真**：`StockResearchWorkspace` 注释声称被 `StockDetailPage` 复用，但实际未复用。
8. **P2 AccountMenu 未复用导航真源**：组件内 inline 构建菜单项，未调用 `appNavigation` helper。

### 修复内容
1. `marketWorkspaceUrlState.ts`：
   - 新增 `DisplayTimeframe` 类型 + `ALLOWED_TIMEFRAMES` 常量，非法 URL 值回退 `1d`。
   - 新增 `ResearchSource` 类型 + `source`/`strategy`/`eventId` 字段到 `MarketWorkspaceUrlState`。
   - 新增 `defaultStrategyForSource(source)`：watchlist→watchlist_monitor, selection→dsa_selector。
   - encode 时保留 source/strategy/event_id；strategy 等于 source 默认值时省略。
2. `useApi.ts`：
   - `useInstruments(params, options?: { enabled? })` 增加可选 enabled 参数。
   - `useWatchlistMonitorStatus(options?: { enabled? })` 增加可选 enabled 参数。
3. `MarketInstrumentPane.tsx`：
   - `useWatchlistMonitorStatus({ enabled: scope === 'watchlist' })`。
   - `useInstruments(params, { enabled: scope === 'market' && canSearch })`。
4. `useStockResearchData.ts`：
   - 移除 `useWatchlist`/`useBatchInstruments`/`useStockMemo` 查询。
   - 只保留 instrument/bars/indicators/quote/events 核心查询。
   - `timeframe` 参数类型从 `string` 收紧为 `DisplayTimeframe`。
5. `StockResearchWorkspace.tsx`：
   - 删除内部 `useState('1d')`，改为接收受控 `timeframe: DisplayTimeframe` + `onTimeframeChange`。
   - 接收严格类型 `source: ResearchSource` + `strategyKey: string`，禁止硬编码。
   - 新增 `barsStatusLabel(tf, isPartial)` 根据 timeframe 显示真实周期文案。
   - 新增 instrument/bars/indicators 错误状态显示（含重试按钮）。
   - 修正注释：当前仅 `MarketWorkspacePage` 使用。
6. `MarketWorkspacePage.tsx`：
   - 从 URL 解析 source/strategy/eventId。
   - `handleTimeframeChange` 写回 URL（保留 source/strategy/event_id）。
   - `handleSelectSymbol` 选择新股票时 `eventId: null`（清除旧 event_id）。
   - 传 timeframe/onTimeframeChange/source/strategyKey 给 `StockResearchWorkspace`。
7. `appNavigation.ts`：
   - 新增 `AccountMenuVariant` 类型 + `getAccountMenuItemsForVariant(isAdmin, variant)` 函数。
   - variant='admin' 时移除"管理后台"项，追加"返回行情"。
8. `AccountMenu.tsx`：
   - 移除 inline 菜单构建，改为调用 `getAccountMenuItemsForVariant(isAdmin, variant)`。
9. `global.scss`：
   - 新增 `.tv-chart-error` 和 `.tv-chart-retry` 样式。
10. 测试：
    - `marketWorkspaceUrlState.test.ts` 从 7 用例扩展到 16 用例（含非法 timeframe 回退、source 校验、strategy 默认推导、event_id 清除等）。
    - `appNavigation.test.ts` 从 6 用例扩展到 10 用例（含 4 项 getAccountMenuItemsForVariant 测试）。

### 测试结果（收口后）
- `node --test` 4 个测试文件：**39 passed, 0 failed**
- `tsc --noEmit`：**0 errors**
- `eslint` 10 个改动文件：**0 errors**
- `vite build` 到 `/tmp/market-workspace-build`：成功（227 modules, 3.46s），构建后立即删除
- 4 个文档检查：全部 PASS

### 风险（收口后更新）
1. `StockDetailPage` 仍保留独立实现（直接调用 hooks 自行组装数据），存在数据组装逻辑重复 → 下一独立 PR 迁移。
2. `event_id` URL 参数已解析、保留并传递到 `MarketWorkspacePage`，但工作区尚未消费 → 下一阶段。
3. `WatchlistPage` 文件保留但 App.tsx 路由表不再引用 → 后续阶段决定是否删除。
4. `source` 已从 URL 解析并传入 `StockResearchWorkspace`，但 `ScreenerPage` 目前跳转目标仍为 `/stock/:symbol`（非 `/market`），从 Screener 进入工作区需后续调整导航目标。

### Git Commit（收口）
（见 git log）

---

## 最终验收（同分支追加 commit）

### 根因
advice.md 第二轮 review 发现 PR #70 仍存在三个阻断/收口问题，且未完成真实浏览器 E2E 验证：

1. **P0 左栏选择上下文不重置**：从 `MarketInstrumentPane` 选股时仍保留原 `source`/`strategy`，导致从趋势选股（selection+dsa_selector+event_id）进入工作区后点左栏股票仍保持 selection 上下文，与 `source` 定义冲突。切换 scope 时也未退出 selection 上下文。
2. **P1 搜索缓存结果残留**：`MarketInstrumentPane` 即使 `canSearch===false` 仍无条件 `{searchResults.map(...)}`，禁用 Query 不等于缓存数据为空，可能残留旧搜索结果。
3. **P1 非日线周期显示"日线回退"**：`StockResearchWorkspace` 状态条在 15m/1h/1w/1mo 下仍显示"日线回退"；partial 文案未包含当前周期。
4. **E2E 缺失**：上一轮只有纯函数测试 + tsc + eslint + build + docs 检查，没有真实 DOM、URL、网络请求联动验证。

### 修复内容
1. `marketWorkspaceUrlState.ts`：
   - 新增纯函数 `selectInstrumentFromMarketPane(state, newSymbol)`：选股时强制 `source='watchlist'`、`strategy=defaultStrategyForSource('watchlist')='watchlist_monitor'`、`eventId=null`，保留 scope/symbol/timeframe。
   - 新增纯函数 `changeMarketScope(state, newScope)`：切 scope 时退出 selection 上下文（source→watchlist、strategy→watchlist_monitor、eventId→null），保留 timeframe。
2. `MarketWorkspacePage.tsx`：
   - `handleSelectSymbol` 改为调用 `selectInstrumentFromMarketPane(urlState, newSymbol)`。
   - `handleScopeChange` 改为调用 `changeMarketScope(urlState, newScope)`。
3. `MarketInstrumentPane.tsx`：
   - 搜索结果渲染从 `{searchResults.map(...)}` 改为 `{canSearch && searchResults.map(...)}`，关键词不足 2 字符或切 scope 时不渲染缓存结果。
4. `StockResearchWorkspace.tsx`：
   - `barsStatusLabel(tf, isPartial)` 重写：partial 文案包含当前周期（如"盘中 partial bar（15m）"）。
   - quote 状态非实时非降级时统一显示"行情回退"，禁止所有非 1d 周期显示"日线回退"。
5. `marketWorkspaceUrlState.test.ts`：
   - 从 16 用例扩展到 21 用例，新增 `selectInstrumentFromMarketPane` / `changeMarketScope` 状态转换测试（selection+dsa_selector+event_id → watchlist+watchlist_monitor+null）和 timeframe 保留测试。

### E2E 验证（CDP，禁止安装 Playwright）
使用现有 Chromium + Node 22 `fetch`/`WebSocket` 通过 DevTools Protocol 执行。临时脚本 `/tmp/market-workspace-e2e.mjs`，profile `/tmp/panji-cdp-profile`，Vite 服务固定端口只监听 `127.0.0.1`。无法登录时用 `Fetch.enable` 域 mock 必要 API（mock 和脚本只放 `/tmp`）。

**Mock 数据形状修正**（对齐后端 DTO，避免组件抛 TypeError 触发 React Router ErrorBoundary 卸载整页）：
- `MOCK_STRUCTURAL`：`{ primary: {'1d':{}}, secondary: {'15m':{}}, relation: {notes:[]}, meta: {as_of, primary_lookback_bars, secondary_lookback_bars, degraded_reasons} }`（`StockStructuralStatePanel` 第 360 行 `Object.keys(data.primary)` 要求 `primary` 非空）。
- `MOCK_TEMPORAL`：补充 `meta: { as_of, primary_timeframe, secondary_timeframe, degraded_reasons, warmup_notes }`（`TemporalFeaturesCard` 第 309 行 `data.meta.warmup_notes` 要求 `meta` 非空）。
- `MOCK_INDICATORS`：`{ layers: [], data: {}, source_bar_times: [], source_bar_hash: 'e2e-hash' }`（`IndicatorResponse` 要求 `layers` 和 `data`，非 `indicators`）。
- `makeBars(tf)`：返回 `{ items: [...], total, page, page_size, timeframe, adj, data_source, as_of, is_partial, degraded, degraded_reason, last_live_bar_time, freshness_seconds }`（`BarListResponse` 要求 `items`，非 `bars`；每个 Bar 需 `instrument_id`/`trade_date`/`adj_factor`）。
- `MOCK_WATCHLIST`：补充 `WatchlistMonitorStatusItem` 全部 17 个字段（`monitor_status`/`market_session`/`watchlist_item_id`/`market`/`calculation_status`/`freshness_seconds`/`last_bar_time`/`evaluation_status`/`retry_count`/`error_code`/`source_bar_time`/`metrics`/`latest_event`/`updated_at` 等）。

**navigateAndWait 防 stale DOM**：`Page.navigate` 后旧页面 DOM 短暂存续（旧页面 `StockDetailPage` 也用 `StrategyChart`，`.tv-tf-btn` 在旧页面为真会导致提前返回）。修复：URL 变更 + 800ms 延迟 + 连续 2 次条件为真才返回。

**CDP 事件监听**：`Network.requestWillBeSent`（请求日志）、`Network.responseReceived`（状态码≥400 的错误）、`Runtime.consoleAPICalled`（console.error）三个事件用于 E2E 调试。

**E2E 结果（2026-07-11，22/22 PASS）**：
```
PASS | S1: bars请求含 timeframe=15m | bars 15m 请求数=1, url=...timeframe=15m&adj=qfq&page_size=4000
PASS | S1: indicators请求含 timeframe=15m | indicators 15m 请求数=1
PASS | S2: 点击1h按钮成功 | clicked=true
PASS | S2: URL含timeframe=1h | url=...timeframe=1h
PASS | S2: bars请求含 timeframe=1h | bars 1h 请求数=1, url=...timeframe=1h&adj=qfq&page_size=1200
PASS | S2: indicators请求含 timeframe=1h | indicators 1h 请求数=1
PASS | S3a: market空关键词 0次instruments请求
PASS | S3a: market空关键词 0次monitor-status请求
PASS | S3b: 输入1字符 0次instruments请求
PASS | S3c: 输入2字符后发instruments请求 | url=...keyword=00&page_size=50
PASS | S4: 切watchlist发monitor-status请求 | url=.../watchlist/monitor-status
PASS | S4: watchlist scope 0次instruments搜索请求
PASS | S4: 点击自选按钮成功 | clicked=true
PASS | S5: 选左栏股票后URL不含source=selection | url=...symbol=600519.SH
PASS | S5: 选左栏股票后URL不含strategy=dsa_selector | clicked=贵州茅台600519.SH-0.50%
PASS | S5: 选左栏股票后URL不含event_id | url=...symbol=600519.SH
PASS | S6: 收起右栏成功 | collapsed=true, expandVisible=true
PASS | S6: 收起右栏后新股票 0次structural-factors
PASS | S6: 收起右栏后新股票 0次temporal-features
PASS | S7: /capture无UserAppShell | userShell=false
PASS | S7: /capture无AdminAppShell | adminShell=false, navCount=0, bodyLen=35
PASS | S8: /stock/:symbol页面加载有内容 | url=.../stock/000001.SZ, bodyLen=1315
```

### 静态验证结果（最终验收）
- `node --test` 4 个测试文件：**44 passed, 0 failed**（appNavigation 10 + routeStructure 10 + detailNavigation 3 + marketWorkspaceUrlState 21）
- `tsc --noEmit`：**0 errors**
- `eslint` 6 个改动文件：**0 errors**
- `vite build` 到 `/tmp/market-workspace-build`：成功（227 modules, 3.46s），构建后立即删除
- 4 个文档检查：全部 PASS（`check_docs_consistency.py` / `check_architecture.py` / `check_test_allowlist.py` / `update_docs.py --check`）

### 文档更新（最终验收）
- `AGENTS.md` §12.14 新增规则 12（左栏选择上下文重置）、13（搜索结果渲染门控）、14（行情状态文案），原 rule 12 重编号为 15。
- `docs/current/04-frontend-ux.md` §2.4 补充左栏选择上下文重置规则、搜索结果渲染门控、行情状态文案修正。
- `docs/current/05-testing-acceptance.md` 阶段三阻断验收扩展测试用例到 21 个，新增 CDP E2E 8 项场景验收规则和 22/22 PASS 结果。
- `docs/maps/test-coverage-map.md` 更新 marketWorkspaceUrlState 测试从 16 到 21 用例，E2E 从烟测改为 CDP 22 项断言 PASS。
- 不修改 `docs/current/02-data-api-contracts.md` / `docs/maps/api-route-map.md`（无 API 变化）。
- 不修改 `docs/current/03-jobs-integrations-operations.md` / `docs/maps/worker-job-map.md`（无任务变化）。

### 风险（最终验收更新）
1. `StockDetailPage` 仍保留独立实现（直接调用 hooks 自行组装数据），存在数据组装逻辑重复 → 下一独立 PR 迁移。
2. `event_id` URL 参数已解析、保留并传递到 `MarketWorkspacePage`，但工作区尚未消费 → 下一阶段。
3. `WatchlistPage` 文件保留但 App.tsx 路由表不再引用 → 后续阶段决定是否删除。
4. `source` 已从 URL 解析并传入 `StockResearchWorkspace`，但 `ScreenerPage` 目前跳转目标仍为 `/stock/:symbol`（非 `/market`），从 Screener 进入工作区需后续调整导航目标。
5. E2E mock 数据形状对齐后端 DTO，但不替代真实生产 E2E（mock 不验证后端真实响应）。

### 磁盘清理
- 删除 `/tmp/market-workspace-e2e.mjs`、`/tmp/panji-cdp-profile`、`/tmp/market-workspace-build`、`/tmp` 下诊断日志。
- 不删除任何用户文件或 Docker 数据。

### Git Commit（最终验收）
（见 git log）
