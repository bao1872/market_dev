# CHANGE-20260702-010：Phase D - 剩余 Alignment 缺口修复

## 1. 基本信息

- 日期：2026-07-02
- 状态：committed（代码实现）；文档更新待提交
- 需求提出人：项目负责人
- 需求出处：用户于 2026-07-02 确认执行 Phase D 全部修复（方案 A），并将 CI `continue-on-error` 改为阻断；2026-07-02 进一步要求三层 Ruff 门禁、文档 Commit 不自引用、ALIGN-014 在 CI 全绿前保持开放。
- Git 分支：`fix/release-remaining-alignment-gaps`
- Phase D Base Code Commit（implementation_base_commit）：`64ed75cce80f5b3f2b5ab95f96b52aac11475e3e`
- Verified Implementation Commit（verified_implementation_commit）：`4e146f0bcc1d3d05e51f4fe968913eac74651778`
- 文档更新 Commit：本次 docs commit 生成后补记，不在当前文件中自引用
- 最终 release HEAD / PR Merge Commit：合并后补记

## 2. 修改前理解

- `StrategyBatchService.publish_run` 显式允许 `partial_failed` 状态发布，与自动发布门禁 `_check_quality_gates` 不一致。
- `MonitorBatchService` 直接调用 `bar_repository.get_bars` / `get_recent_bars`，未复用 `MarketDataAggregationService`，导致 monitor 链路缺少统一缓存、盘中 partial bar 聚合、数据源诊断和尾部补齐。
- `monitor_batch_service.py` 第 71 行注释仍写 `# 3600`，但 `NODE_CLUSTER_LOW_BARS` 已按 250×16 修正为 4000。
- `.github/workflows/ci.yml` 中 `ruff` 和 `type-check` job 设置 `continue-on-error: true`，失败不阻断 workflow。

## 3. 修改目标

- 统一 DSA 发布门禁：`publish_run` 仅允许 `completed` 状态进入 `published`，`partial_failed` 即使 `succeeded_count > 0` 也拒绝。
- monitor 行情统一走 `MarketDataAggregationService`，保持 Node Cluster 输入根数不变（1d=250、15m=4000、1m=2）。
- 修正 `monitor_batch_service.py` 中 `_15MIN_LOOKBACK_BARS` 的注释为 `# 4000 = 250 * 16`。
- CI 中 `ruff` / `type-check` 改为阻断（移除 `continue-on-error: true`）。
- 同步更新测试与当前设计文档。

## 4. 实际修改

### 代码文件

- `backend/app/services/strategy_batch_service.py`（修改：`publish_run` 状态检查仅允许 `completed`，拒绝 `partial_failed`）
- `backend/app/services/monitor_batch_service.py`（修改：新增 `_fetch_md_bars` 统一调用 `MarketDataAggregationService().get_bars`；替换所有 `bar_repository.get_bars` / `get_recent_bars` 调用；更新 `_15MIN_LOOKBACK_BARS` 注释）
- `backend/app/services/market_data_aggregation_service.py`（修改：支持 `1m` 原始分钟线；`get_bars` 处理 `timeframe="1m"`）
- `backend/app/repositories/bar_repository.py`（新增：`_query_minute_bars` 函数）
- `.github/workflows/ci.yml`（修改：将单一 `ruff` job 拆分为三层：`ruff-new-files` 阻断任务仅检查相对 base 新增的 Python 文件；`ruff-baseline-regression` 阻断任务按 `(filename, code, message, count)` 诊断集合比较，禁止历史债务新增/增加；`ruff-full-repository-report` 非阻断任务扫描全仓库并上传 JSON 报告）
- `tools/quality_baselines/ruff.json`（更新：基线 commit `64ed75c`、诊断总数 930、唯一诊断组合 358 项，采用 `(filename, code, message, count)` 精确集合）
- `tools/compare_ruff_baseline.py`（新增：Ruff 诊断基线比较工具）

### 测试文件

- `backend/tests/test_dsa_publish_validation.py`（修改：将 `test_publish_run_accepts_partial_failed_with_succeeded` 改为 `test_publish_run_rejects_partial_failed_with_succeeded`，断言发布 `partial_failed` 时抛出 `ValueError`）
- `backend/tests/test_indicator_contract.py`（修改：`test_monitor_batch_service_no_daily_fetch_days_370` / `test_monitor_batch_service_no_15min_fetch_days_200` 改为检查 `MarketDataAggregationService` 与 `_fetch_md_bars`；将 `indicator_contract as IC` 改为直接导入 `indicator_contract` 以符合 N812）
- `backend/tests/test_access_audit_log.py`、`backend/tests/test_admin_subscription_users.py`、`backend/tests/test_after_close_idempotent_dsa_pipeline.py`、`backend/tests/test_eligible_user_service.py`、`backend/tests/test_feishu_platform_app_only.py`、`backend/tests/test_node_cluster_contract.py`、`backend/tests/test_subscription_expired_e2e.py`（修改：修复 Ruff 历史错误，不引入新诊断）
- `backend/tools/dsa_benchmark.py`（修改：修复 Ruff 排序错误）

### 文档文件

- `docs/current/18-code-doc-alignment.md`（更新：ALIGN-019 保持 `CLOSED`；ALIGN-014 恢复为 `KNOWN_GAP` 并明确区分本地验证完成 / CI 未验证 / 生产未验证；ALIGN-021 更新为三层 Ruff 策略；修正代码基线自引用，记录 `implementation_base_commit`、`verified_implementation_commit`、`branch_name`、`verification_date`）
- `docs/current/14-deployment-operations.md`（更新：新增第 8 节 CI/GitHub Actions，说明 Ruff 三层门禁）
- `docs/current/15-testing-acceptance.md`（更新：完成门禁改为 Ruff New Files 阻断 + Ruff Baseline Regression 阻断 + Ruff Full Repository Report 非阻断 + mypy 阻断）
- `AGENTS.md`（更新：盘迹硬规则新增第 13 条 Ruff 增量阻断策略）
- `docs/changes/records/CHANGE-20260702-010.md`（更新：修正 Commit 自引用，拆分代码实现 Commit 与文档 Commit）
- `docs/changes/CHANGELOG.md`（更新）

## 5. 修改前行为

- admin 手动 `publish_run` 可发布 `partial_failed` 状态的运行，只要 `succeeded_count > 0`。
- monitor 链路直接查 DB 行情，不经过统一聚合服务，缺少缓存、partial bar 补齐和数据源诊断。
- `_15MIN_LOOKBACK_BARS` 注释写 `# 3600`，与 `NODE_CLUSTER_LOW_BARS=4000` 不符。
- CI 中 ruff / type-check 失败不阻断后续 job。

## 6. 修改后行为

- `publish_run` 仅 `completed` 可发布；`partial_failed` 显式拒绝，错误信息明确提示“仅 completed 可发布”。
- monitor 链路统一通过 `MarketDataAggregationService().get_bars` 获取行情，返回 DataFrame 再 `tail(N)` 保留 Node Cluster 所需根数。
- `_15MIN_LOOKBACK_BARS` 注释更新为 `# 4000 = 250 * 16`，与 `indicator_contract` 唯一真源一致。
- CI 中 `Ruff New Files` 阻断相对 base 新增的 Python 文件，必须零错误；`Ruff Baseline Regression` 阻断历史债务新增/增加；`Ruff Full Repository Report` 非阻断但上传完整 JSON 报告；`type-check` 仍阻断。
- `tools/quality_baselines/ruff.json` 记录基线 commit `64ed75c` 的诊断集合（930 个错误、358 项唯一组合），任何 PR 不得使诊断新增或增加；当前已降至 903 个错误、331 项唯一组合，无新增/增加诊断。

## 7. 数据库迁移

- 无新增 migration。
- Alembic 往返验证通过（见第 10 节）。

## 8. 配置变化

- 无新增环境变量。
- CI workflow 行为变化：单一 `ruff` job 拆分为 `ruff-new-files`（阻断新增 Python 文件错误）、`ruff-baseline-regression`（阻断历史债务新增/增加）和 `ruff-full-repository-report`（非阻断全量报告）；`type-check` 仍为阻断；删除/调整会相对 `main` 检查整个大型 feature diff 的 `ruff-changed-files` 逻辑。
- 新增/更新质量基线文件 `tools/quality_baselines/ruff.json` 与比较脚本 `tools/compare_ruff_baseline.py`。

## 9. 兼容性影响

- `publish_run` 对 `partial_failed` 的拒绝是设计口径补全，不兼容旧行为；admin 需等待运行进入 `completed` 或修复失败标的后再发布。
- `MonitorBatchService` 行情来源改为聚合服务，输出语义（最近 N 根升序 DataFrame）保持不变；数据源诊断字段现在可通过 `BarAggregationResult` 获取（当前 `_fetch_md_bars` 仅返回 bars DataFrame，后续可按需扩展）。
- CI 阻断任务改为：新增 Python 文件必须零错误，历史债务不得新增/增加；禁止通过扩大 ignore/per-file-ignores/noqa 掩盖。
- 全仓库历史债务以基线 commit `64ed75c` 的诊断集合为准（930 个错误），任何 PR 不得使其新增或增加；清零前单独在 `chore/ruff-historical-debt` 分支处理。

## 10. 测试与证据

- 后端全量测试：`1106 passed, 0 failed`。
- 专项测试：
  - `tests/test_dsa_publish_validation.py` 10 passed（含 `test_publish_run_rejects_partial_failed_with_succeeded`）
  - `tests/test_node_cluster_contract.py` 8 passed（1d=250、15m=4000、1m=2）
  - `tests/test_indicator_contract.py` 18 passed（含 monitor_batch_service 统一入口断言）
- Ruff 本地验证：
  - 相对 `64ed75c` 新增 Python 文件：0 个
  - 全仓库当前错误：903（基线 930，减少 27）
  - 基线比较：无新增诊断、无数量增加诊断；`compare_ruff_baseline.py` 退出码 0
  - 相对 `origin/main` 的 changed Python 文件 217 个、Ruff 错误 894 个，确认不能继续用 changed-files 全量阻断
- 前端验证：
  - `npx tsc --noEmit` 通过
  - `npm run lint` 0 errors, 20 warnings（warnings 为既有 hooks 依赖提示，非新增）
  - `npm run build` 通过
- Alembic 单一 head：`055_feishu_platform_app_only`。
- Docker Compose 配置检查：`docker compose -f docker-compose.prod.yml config --quiet` 通过（补齐 `ADMIN_FEISHU_*` 环境变量后）。
- 文档检查工具：
  - `python tools/update_docs.py --check` 通过
  - `python tools/check_architecture.py` 0 violations
  - `python tools/check_docs_consistency.py` 全部通过
  - `python tools/check_test_allowlist.py` PASS

## 11. 一致性检查

- 产品与代码：DSA 发布门禁现在自动/手动入口一致，均拒绝 `partial_failed`。
- 前端与 API：无 API 变化。
- 后端与数据模型：无 Schema 变化；行情查询改走聚合服务，数据模型契约不变。
- 参数与配置：`NODE_CLUSTER_LOW_BARS=4000` 仍为唯一真源；`monitor_batch_service.py` 注释与常量一致。
- Worker 与业务流程：monitor 行情获取统一至 `MarketDataAggregationService`，与 bars API、indicator_service、capture snapshot 等消费者一致。
- 测试与当前设计：新增/修改测试覆盖 ALIGN-019 和 monitor 统一入口。

## 12. 遗留问题

- 全仓库 Ruff 历史债务 903 个错误（ALIGN-021）需在独立分支 `chore/ruff-historical-debt` 中清理，清零后 `Ruff Full Repository Report` 改为阻断。
- ALIGN-014 在 GitHub Actions 针对最终 HEAD 全绿前保持 `KNOWN_GAP`；本次修复推送后需等待 CI 结果再决定是否关闭。
- 文档 Commit 不记录自身 SHA，最终 release HEAD / PR Merge Commit 在合并后补记。
- 其他未关闭 ALIGN 项（ALIGN-001、010、012、015）不受本次修改影响，保持 `KNOWN_GAP`。

## 13. 完成检查

- [x] 当前分支与记录一致（`fix/release-remaining-alignment-gaps`）
- [x] 代码符合修改后设计
- [x] 测试验证当前设计（1106 passed）
- [x] CHANGELOG 已更新
- [x] Alignment ALIGN-019 已关闭；ALIGN-014 已恢复为 `KNOWN_GAP`；ALIGN-021 已登记
- [x] 文档检查工具通过（`tools/update_docs.py --check`、`tools/check_architecture.py`、`tools/check_docs_consistency.py`）
- [x] Ruff 增量阻断 CI 三层任务已配置（Ruff New Files / Ruff Baseline Regression / Ruff Full Repository Report）
- [x] 全仓库 Ruff 基线文件 `tools/quality_baselines/ruff.json` 与比较脚本 `tools/compare_ruff_baseline.py` 已创建/更新（基线 930 个错误）
- [x] ALIGN-014 已恢复为 `KNOWN_GAP`，等待 GitHub Actions 全绿证据
