# CHANGE-20260815-004 — 顶部搜索 / 行情与自选职责分离 / 行业概念筛选 / 列设置（PRD Alignment Round 1）

- **日期**：2026-08-15
- **类型**：docs-only（PRD 合同对齐 / 需求事实源对齐，无代码/测试/migration/API/前端/Maps/Runbooks/治理改动）
- **领域**：行情与个股体验模块（PRD 40） + 自选管理模块（PRD 50）
- **状态**：`prd_confirmed`（docs-only；未进入 Implementation，未写任何业务/测试代码、未建表、未 migration、未改 API/前端/Maps/Runbooks/治理、`experiments/` 未跟踪目录保留不动）
- **审计基线**：HEAD = origin/dev = `f8b8b8ec`（2026-08-15，`refreshing_daily` 远程验证 + doc 同步），`git fetch origin` 后确认 HEAD==origin/dev，本地无无关 dirty 文件被触碰
- **下一轮**：本 CHANGE 仅对齐 PRD；业务代码开发由 Round 2（ChatGPT 按代码审计 v2.1 审查本 docs SHA 后）另行指令

## 0. 本轮范围与边界

本轮为「代码审计 v2.1」Round 1：**PRD ALIGNMENT ONLY**。

- 仅修改 PRD canonical 文件（40 / 50）+ 本 CHANGE + CHANGE INDEX。
- 禁止修改 frontend / backend / migration / runtime / tests 业务代码。
- 产品意图：把「盘迹顶部搜索 / 行情与自选职责分离 / 行业概念筛选 / 列设置」正式对齐到现行 PRD。
- 不改变任何现行 PRD 已确认的业务语义；仅新增合同、clarify 现有 owner、明确 NON-GOALS。

## 1. Governance 前置（已执行）

| 项 | 值 |
|---|---|
| branch | `dev` |
| pre-change HEAD | `f8b8b8ec` |
| origin/dev（fetch 后） | `f8b8b8ec` |
| HEAD == origin/dev | 是 |
| 本地 dirty | 仅无关文件：`experiments/`、`backend/tests/test_industry_hierarchy_migration_contract.py`、`backend/tests/test_review_observation_shadow_cli.py`；本轮不处理、不删除、不提交 |

## 2. PRD Owner Map（已确认）

| Requirement 域 | canonical PRD | 既有 section | 处理 |
|---|---|---|---|
| 行情页 / 个股详情 / 导航上下文 / 列设置 | `docs/prd/40-market-stock-experience.md` | MX-01~MX-20、MX-60~MX-81 | 新增 MX-06/07/08/09；扩展 MX-20 列设置隔离语义 |
| 自选管理 / 自选成员 | `docs/prd/50-watchlist-intraday.md` | WI-01~WI-04 | 新增 WI-05 |
| 复盘 / 竞价 / 板块 | 其他 owner（不在本轮范围） | — | 不重复定义 |

结论：watchlist / user preference / navigation 已有现行 owner，本轮沿用，**不重复定义**。

## 3. Code Owner Map（ACTUAL 审计，只读，未改）

| 职责 | ACTUAL owner | 证据 |
|---|---|---|
| Global Header / 模块切换 | `frontend/src/layouts/UserAppShell.tsx`（`top-nav` + `行情｜自选｜复盘`）+ `frontend/src/navigation/appNavigation.ts` | 行情/自选均 `path:/market` + `scope` 参数切换 |
| 股票搜索（当前） | 仅 Market workspace 局部筛选（`MarketWorkspacePage` 的 `keyword`/`filters`/`industry`/`concept`） | 无全局搜索组件；无 `GlobalStockSearch` |
| Market universe | `GET /market/stocks?scope=market&universe=all`（Router 362）+ `buildMarketQuery`（scope=market→universe=all） | MarketWorkspacePage.searchParams |
| Watchlist universe | `GET /market/stocks?scope=watchlist&universe=watchlist` 或 `GET /v1/watchlist`（canonical membership） | Router 363；WI-03 |
| 行业 / 概念筛选 | MarketWorkspacePage 局部 state（`industry`/`concept`）；经 `/api/v1/market/filter-specs` + `StrategyDataTable`/`FilterPopover` 正式 query path | 与 99 字段 typed filtering system 同源 |
| 自选变更 canonical owner | 后端 `backend/app/api/watchlist.py` + `backend/app/models/watchlist.py`（`WatchlistItem`/`WatchlistMembership`）；`GET /v1/watchlist`、`POST/DELETE /v1/watchlist/{instrument_id}`；前端 `useWatchlist` + `useToggleWatchlist`（`frontend/src/hooks/useApi.ts`） | 单一变更源 |
| 个股详情导航 | `frontend/src/features/stock-research/stockDetailNavigation.ts`（`buildStockDetailUrl`/`StockDetailQuery`）+ `detailSourceContext.ts` + `useStockDetailActions.ts` + `StockDetailPage.tsx` | 既有 canonical route builder，全局搜索须复用 |
| 列偏好持久化 | `user_table_view_presets`（`backend/app/models/table_view_preset.py`），按 `table_id` 隔离（`screener`/`watchlist`）；API `GET/PUT/POST/DELETE /v1/me/table-view-presets`；前端 `useTableViewPreset`；存储 `hiddenColumns` + `columnOrder` | Market/Watchlist 隔离**已架构支持** |

## 4. SHOULD / ACTUAL / GAP

| # | Requirement | SHOULD | ACTUAL | GAP | CLASS | SEVERITY | EVIDENCE | DECISION |
|---|---|---|---|---|---|---|---|---|
| 1 | Global Stock Search | 全局证券定位（代码/名称/拼音） | 仅 Market workspace 局部 keyword 筛选，无全局组件 | 缺 Global Search 组件与 Header 层入口 | MISSING | Medium | 无 `GlobalStockSearch` 文件 | PRD 合同（MX-07）；拼音标 FUTURE |
| 2 | Header/Module Navigation | 三层责任（Global Header / Module Nav / WS Controls） | `UserAppShell` 已含 top-nav + 三模块切换 | 架构已符合语义，仅需合同化 | ALIGNED | Low | UserAppShell + appNavigation | 写入 MX-06 |
| 3 | Market base universe | `/market` scope=market | 已是 scope=market/universe=all | 无 gap | ALIGNED | — | Router 362 | MX-08 |
| 4 | Watchlist base universe | 用户 canonical 自选成员 | `scope=watchlist` / `GET /v1/watchlist` | 无 gap | ALIGNED | — | Router 363 / WI-03 | MX-08 |
| 5 | Industry filter scope | workspace-local，不改 base universe | 局部 state，未越界 | 无 gap，记录 ACTUAL | ALIGNED | — | MarketWorkspacePage | MX-09 + MX-03 |
| 6 | Concept filter scope | workspace-local，不改 base universe | 局部 state，未越界 | 无 gap，记录 ACTUAL | ALIGNED | — | MarketWorkspacePage | MX-09 |
| 7 | Watchlist add/remove ownership | 单 canonical owner | `useToggleWatchlist` + watchlist API | 无 gap | ALIGNED | — | useApi + watchlist.py | WI-05 |
| 8 | Stock-detail navigation reuse | 全局搜索复用 canonical 导航 | canonical 导航已存在 | 仅需在 PRD 显式约束复用 | ALIGNED | Low | stockDetailNavigation.ts | MX-07 引用 MX-10~12 |
| 9 | Column visibility | 用户可 show/hide | `TableViewPreset.hiddenColumns` 已支持 | 无 gap | ALIGNED | — | MX-20 + table_view_preset | 继承 |
| 10 | Column reorder | 用户可拖拽排序 | `TableViewPreset.columnOrder` 已支持 | 无 gap | ALIGNED | — | 同上 | 继承 |
| 11 | Column preference persistence owner | 单一 canonical owner | `user_table_view_presets` | 无 gap | ALIGNED | — | table_view_preset + me_table_view_presets API | 继承 |
| 12 | Market/Watchlist preference isolation | 两 workspace 列配置独立 | `table_id`=`screener`/`watchlist` 已隔离 | 无 gap（架构已支持） | ALIGNED | — | table_view_preset.table_id | MX-20 补充 |

非决策性 UNKNOWN 标 DEFER：全局搜索的具体 UI 形态、拼音匹配后端能力是否就绪、Market 行内自选入口是否已存在——均不扩大调查，留给 Round 2 代码对齐。

## 5. 写入 PRD 的目标合同

### PRD 40 新增 / 修改
- **MX-06** 三层 Workspace 责任层级（Global Header / Module Navigation / Workspace Controls）；明确三个用户可见搜索输入：Global Header 的 Stock Search、Workspace Controls 的 Industry Search 与 Concept Search（均为独立可输入检索控件，不锁定像素/颜色）
- **MX-07** 全局股票搜索 = Market-local 股票搜索 UI 的 RELOCATION + RESPONSIBILITY CHANGE（移至 Global Header、不再作表格筛选器、点击进 canonical 详情、可 add/remove 自选；Market 不得保留第二个用户可见 stock-search input；UI 迁移 ≠ 后端 query capability 删除；匹配维度=代码/名称/拼音 FUTURE；MUST NOT=mutate workspace filter state 等语义表述）
- **MX-08** Workspace Base Universe 合同（`WorkspaceResult = BaseUniverse ∩ ActiveWorkspaceFilters`；Market = canonical market universe，Watchlist = canonical active 自选成员 universe；具体 scope/universe 参数移入本 CHANGE §3）
- **MX-09** 行业 / 概念筛选范围（两个独立可输入检索 search controls；workspace-local；不改 base universe；不升级 UQB；不新增 multi-select contract）
- **MX-20** 列设置追加：Market 与 Watchlist 经同一 canonical TableViewPreset preference owner 按 workspace 维度隔离（具体 `table_id` key 留本 CHANGE §3），列设置不改变数据语义与筛选合同

### PRD 50 新增
- **WI-05** 自选成员 canonical persisted owner 与单一变更源（语义合同：persisted membership 唯一 SSOT、client cache/optimistic 不得成为独立事实源、mutation 后客户端状态须收敛、所有入口同一 mutation contract；具体后端文件/模型/endpoint 与前端 hook 见本 CHANGE §3）

### 需求所有权声明更新
- PRD 40 头部「需求所有权」增加：全局股票搜索、Workspace 层级与 base universe 合同、列设置持久化（日期 2026-08-15）

## 6. NON-GOALS（本轮明确暂不包含）

- Universal Query Builder
- 一个框统一股票/行业/概念
- AI search / 自然语言选股
- 用户自定义 AND/OR expression
- 新闻/公告搜索
- 自选复杂分组
- 无必要的 watchlist schema 重构
- stock detail redesign
- 99 字段 typed-filter 重构
- 第一金字塔业务逻辑修改
- mobile redesign
- 视觉品牌重构
- 数据库迁移
- API 实现

## 7. Acceptance / RTM（requirement IDs）

| ID | Requirement | 验证层 |
|---|---|---|
| R1 | Global Search is global（位于 Global Header 层；原 Market-local stock-search UI 已迁移至此，Market workspace 不再保留第二个用户可见 stock-search input） | Code / Frontend behavior / Test |
| R2 | Stock result navigates via canonical detail path | Frontend behavior / Code（复用 stockDetailNavigation）/ Test |
| R3 | Stock result mutates canonical watchlist membership | Code（WI-05）/ API / Test |
| R4 | Global stock search does NOT filter Market/Watchlist table（它不再作为表格筛选器；原 Market-local 股票搜索 UI 已移走，Market 表格无第二个用户可见 stock-search） | Frontend behavior / Code / Test |
| R5 | Market filters stay inside Market base universe | Code / API（scope=market）/ Test |
| R6 | Watchlist filters stay inside watchlist membership universe | Code / API（scope=watchlist）/ Test |
| R7 | Industry filter is workspace-local | Frontend behavior / Code / Test |
| R8 | Concept filter is workspace-local | Frontend behavior / Code / Test |
| R9 | Column visibility is user configurable | Frontend behavior / API（TableViewPreset）/ Test |
| R10 | Column order is user configurable | Frontend behavior / API / Test |
| R11 | Column preference persistence has one canonical owner | Code / API / Test |
| R12 | Market/Watchlist column configuration isolation | Code / API（table_id）/ Test |
| R13 | Header / Module Navigation / Workspace Controls 层级与三个搜索输入 | Code / Frontend behavior / component test / manual frontend acceptance |

**R13 — DONE WHEN**
1. Global Header 在 Module Navigation 上方；
2. Stock Search 位于 Global Header；
3. 行情 / 自选 / 复盘 / 竞价 属于 Module Navigation；
4. Industry Search + Concept Search 位于 Workspace Controls；
5. Market workspace 不存在第二个用户可见 Stock Search；
6. 三个搜索输入职责互不混淆。

## 8. 验证（docs-only 收口）

- `git diff --check`：无尾随空白/冲突标记。
- 仅修改授权 docs：`docs/prd/40-market-stock-experience.md`、`docs/prd/50-watchlist-intraday.md`、`docs/changes/2026/CHANGE-20260815-004.md`、`docs/changes/INDEX.md`。
- requirement ID 不冲突：R1~R13 唯一（R13 为 Round 1.1 新增）。
- 无重复 canonical contract：watchlist/navigation/column 均沿用既有 owner，未新建。
- docs/prd 术语一致：base universe / WorkspaceResult / canonical owner 统一。
- NON-GOALS 明确列出。
- ACTUAL 与 SHOULD 分离已落实：PRD（40/50）仅写语义合同；具体文件/hook/endpoint/scope/table_id 等 ACTUAL 留于本 CHANGE §3 Code Owner Map 与对应 Map，PRD 已移除"当前正式实现/当前为"类描述（MX-06/08、MX-07 MUST NOT、MX-20、WI-05 均已完成清理）。

## 8.1 Round 1.1 合同修正（CHANGE-20260815-004 同一 docs SHA 内修正，append 更新）

治理判断：本仓库 CHANGE 文件允许在同一 docs-alignment SHA 后续修正（非 append-only 历史改写）；故不新建独立 CHANGE，仅在 004 内记录 correction，避免重复 PRD owner。

修正项：

1. **Stock Search ownership correction（MX-07）**：原"不是 /market 表格内置搜索的替代品"会允许两个并存，与"保留三个搜索框"冲突。改为：Global Stock Search = Market-local 股票搜索 UI 的 RELOCATION + RESPONSIBILITY CHANGE；Market 不得保留第二个用户可见 stock-search input；UI 迁移 ≠ 后端 query capability 删除。
2. **Exact three-search UI contract（MX-06/09）**：明确 Global Header=Stock Search；Module Navigation=行情/自选/复盘/竞价；Workspace Controls=Industry Search + Concept Search（两个独立可输入检索控件，非 dropdown/command palette/universal search）。
3. **PRD/ACTUAL separation（MX-06/07/08/09/20、WI-05）**：移除 PRD 中"当前正式实现/当前为"、具体 route/scope/`/market/stocks` 参数、`table_id` key、后端文件/模型/endpoint、前端 hook 名；全部下沉至本 CHANGE §3 Code Owner Map 与 Map。
4. **R13 RTM closure（§7）**：新增 Header/Navigation/Workspace 层级与三个搜索输入 requirement。
5. **WI-05 semantic cleanup（PRD 50）**：移除"乐观更新后回读 canonical"过度具体表述，改为 persisted membership 唯一 SSOT、client cache/optimistic 不得为独立事实源、mutation 后收敛、同一 mutation contract。

本轮（Round 1.1）仍为 docs-only，未改业务代码/测试/migration/API/前端/Maps/Runbooks/治理；`experiments/` 未跟踪目录保留不动。

## 9. FIRST_BLOCKER

NONE。未发现现行 PRD 与本次产品意图存在重大互斥；所有 GAP 为对齐性新增合同，无产品决策冲突。
