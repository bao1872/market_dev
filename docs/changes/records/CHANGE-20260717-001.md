# CHANGE-20260717-001: SMC Pine 逻辑对齐最终收口（warmup/历史分离 + execution gate + trailing NaN + OB 顺序 + EQH/EQL 几何 + Strong/Weak 起点 + golden 测试重做 + 确定性测试 + 导出增强 + ALGORITHM_VERSION v10）

## 变更编号
CHANGE-20260717-001

## 任务名称
SMC Pine 逻辑对齐最终收口：计算历史与展示窗口分离（15m 5000 计算/4000 展示、1mo ≥200）、execution gate 严格复刻 Pine L784/L787、trailing NaN 严格复刻 `math.max(high, na)=na`、OB 顺序 newest-first 复刻 `array.unshift`、EQH/EQL 两端点线几何、Strong/Weak 线起点改 `trailing.lastTopTime/lastBottomTime`、golden 测试重做（EQH 类型/时间戳/容差 0/全链断言）、新增 8 个确定性测试类、导出增强 8 个隐藏 plot、`ALGORITHM_VERSION` v9→v10 强制失效旧 SMC 缓存。

## 需求出处
`ref/PROMPT.md`（SMC Pine 逻辑对齐最终收口）。

## 修改前行为

### Bug #1: warmup/历史未分离
- `indicator_service.py` L766 注释失真："15m≈12000、1h≈3000、1w≈714、1mo≈166"。
- 15m 展示 4000 根时 SMC 仅计算同样 4000 根，无 warmup；pivot/BOS/CHoCH 在窗口左缘丢失。
- 1mo 仅 ~166 根（<200），ATR200 无法初始化，EQH/EQL 阈值（× ATR200）全 NaN 失效。

### Bug #2: CHoCH 规则
- Core 逻辑本身正确（tag 先于 bias 更新），但因 warmup 不足导致窗口左缘 bias/pivot 丢失，CHoCH 可能被误标为 BOS 或漏掉。无 internal/swing × 多空四组确定性测试。

### Bug #3: execution gate
- `smc_pine_core.py` L822: `if show_internal_order_blocks:` 仅由 OB 开关门控 internal structure。
- L825: `self.display_structure(i, False, prev_levels)` swing 完全无门控。
- Pine L784/L787 的 gate 是 `showInternals OR showInternalOB OR showTrend` / `showStructure OR showSwingOB OR showHighLowSwings`，core 缺 `show_internals`/`show_structure`/`show_trend` 参数。

### Bug #4: trailing NaN
- `update_trailing_extremes` L761-766: `self.trailing.top != self.trailing.top or self.highs[i] >= self.trailing.top` — NaN 时用 `self.highs[i]` 凭空初始化。
- Pine L706: `math.max(high, trailing.top)` 当 `trailing.top` 为 `na` 时返回 `na`；trailing 只能由 swing pivot 初始化（L426/L454）。

### Bug #5: OB 顺序
- `smc_pine_core.py` L698: `self.order_blocks_output.append({...})` — oldest-first。
- Pine L519: `array.unshift` — newest-first。前端 `slice(0,5)` 取最旧 5 个，违反"最新 5 个 active internal OB"语义。

### Bug #6: EQH/EQL 几何
- `StrategyChart.tsx` L1581-1592: 用单一 `eq.level` 画水平线；颜色用 `C.blue2`（中性蓝）。
- Pine L396: 两端点线 `line.new(p_ivot.barTime/currentLevel, time[size]/level)`，可能不水平；EQH=bearish 色（L384），EQL=bullish 色（L389）。

### Bug #7: Strong/Weak 起点
- `StrategyChart.tsx` L1598-1619: 线起点 `lastDisplayIdx`（最后可见 bar）。
- Pine L721-727: 线起点 `trailing.lastTopTime/lastBottomTime`，终点 `last_bar_time + 20*(time-time[1])`。

### Bug #8: adapter 文档失真
- `smc_view_adapter.py` L3-4 docstring 含 "约 12000 根 15m time" 和 "不再透传 12000 根 time 数组" 失真数字。

### Bug #9: golden 测试
- `test_smc_tv_parity.py` L142: `"EQH" if eq.get("type","").upper()=="HIGH" else "EQL"` — core 已输出 "EQH"/"EQL"，此映射永远为 False，EQH 全部误映射为 EQL。
- L80: `dt.strftime("%Y-%m-%d")` — 15m 时间戳压缩为日期，多根 bar 映射到同一日期。
- L219-221: OHLC 检查为 no-op（core 不返回 open/high/low/close，`smc_result.get("open",[None]*n)[i]` 总是 None → continue）。
- L286: `abs(tv_ev["bar_index"] - proj_ev["bar_index"]) > 1` — ±1 容差，违反逐 bar 对齐。
- 缺 OB/EQ/全链断言测试；无确定性测试（不依赖 TV CSV fixture）。

## 修改后行为

### 一、warmup/历史分离（Bug #1）
1. **新增常量**（`indicator_service.py` L89-94）：
   - `_SMC_WARMUP_BARS = 1000`（15m 专用 warmup）
   - `_SMC_MONTHLY_MIN_BARS = 200`（1mo 最少 bar 数，ATR200 需要）
   - `_SMC_MONTHLY_LOOKBACK_DAYS = 7000`（1mo 扩展回看，200 月 ≈ 6000 天）
2. **SMC 注入流程重写**（L786-830）：
   - **1d**: `full_daily_bars`（DB 全量日线，已有 ≥500 warmup）
   - **15m**: 独立查询 `bars + _SMC_WARMUP_BARS`（5000）根作为 `smc_bars`，SMC 计算后由 adapter 裁成 `bars`（4000）展示；前复权处理与主 15m 路径一致；查询不足回退 `macd_bars`
   - **1mo**: 若 `len(macd_bars) < 200` 则扩展回看到 `_SMC_MONTHLY_LOOKBACK_DAYS`，重新查询月线并前复权；查询为空回退 `macd_bars`
   - **1h/1w**: `macd_bars`（可获得完整历史）
3. **删除失真注释**：L766 "15m≈12000、1h≈3000、1w≈714、1mo≈166" 替换为准确 warmup 分离说明。

### 二、execution gate 严格复刻 Pine（Bug #3）
1. **`DEFAULT_PARAMS` 新增三参数**（`smc_pine_core.py` L82-87）：
   - `show_internals: True`（对应 Pine `showInternalsInput` L76）
   - `show_structure: True`（对应 Pine `showStructureInput` L84）
   - `show_trend: True`（对应 Pine `showTrendInput` L74）
2. **`run()` 方法门控**（L813-855）：
   - `internal_gate = show_internals or show_internal_order_blocks or show_trend`（Pine L784）
   - `swing_gate = show_structure or show_swing_order_blocks or show_high_low_swings`（Pine L787）
   - L849: `if internal_gate:` 包裹 `display_structure(i, True, prev_levels)`
   - L853: `if swing_gate:` 包裹 `display_structure(i, False, prev_levels)`

### 三、trailing NaN 严格复刻 Pine（Bug #4）
1. **`update_trailing_extremes` 重写**（`smc_pine_core.py` L761-785）：
   ```python
   # Pine 语义：math.max(high, na) = na；只有 trailing.top 非 NaN 时才更新
   if self.trailing.top == self.trailing.top:  # not NaN
       if self.highs[i] >= self.trailing.top:
           self.trailing.top = self.highs[i]
           self.trailing.last_top_time = self.times[i]
   if self.trailing.bottom == self.trailing.bottom:  # not NaN
       if self.lows[i] <= self.trailing.bottom:
           self.trailing.bottom = self.lows[i]
           self.trailing.last_bottom_time = self.times[i]
   ```
2. **初始化只由 swing pivot**：`get_current_structure` 检测到新 swing pivot 时设置 `trailing.top/bottom`（L426/L454），trailing 不再用当前 high/low 凭空初始化。

### 四、OB 顺序 newest-first（Bug #5）
1. **`store_order_block` 修改**（`smc_pine_core.py` L704-710）：
   ```python
   # Pine: array.unshift（最新在头部）— order_blocks_output 也保持 newest-first
   self.order_blocks_output.insert(0, {...})
   ```
2. **adapter 透传**：`smc_view_adapter.py` 无需改动，保持 core 输出的 newest-first 顺序。
3. **前端 `slice(0,5)`**：`smcRendering.ts` 无需改动，已假设 newest-first，取最新 5 个 active internal OB。

### 五、EQH/EQL 两端点线几何（Bug #6）
1. **`StrategyChart.tsx` L1575-1606 重写**：
   ```typescript
   // 两端点 Y：起点用 prev_level，终点用 level（可能不水平）
   const y1 = py(eq.prev_level)
   const y2 = py(eq.level)
   const isEQH = eq.type === 'EQH'
   // Pine L384/L389: EQH=swingBearishColor, EQL=swingBullishColor
   const eqColor = isEQH ? SMC_BEAR_COLOR : SMC_BULL_COLOR
   drawLine(ctx, x1, y1, x2, y2, eqColor, 1, [2, 2])
   // 标签位于两 pivot 中点（Pine L397）
   const midX = (x1 + x2) / 2
   const midY = (y1 + y2) / 2
   const labelYOffset = isEQH ? -3 : 9  // EQH label_down, EQL label_up
   drawText(ctx, eq.type, midX, midY + labelYOffset, eqColor, '8px sans-serif', 'center')
   ```

### 六、Strong/Weak 线起点（Bug #7）
1. **`StrategyChart.tsx` L1608-1654 新增 `timeToDisplayIdx` 辅助函数**：
   ```typescript
   const timeToDisplayIdx = (isoTime: string | null | undefined): number => {
     if (isoTime == null) return 0  // 缺失时 clamp 到窗口左端
     const key = normalizeChartTime(isoTime, timeframe)
     if (key != null) {
       const idx = klineTimeIndex.get(key)
       if (idx != null) return idx
     }
     return 0  // 找不到时 clamp 到窗口左端
   }
   ```
2. **线起点改为 `trailing.last_top_time`/`last_bottom_time`**：
   ```typescript
   // Pine L721: 线起点 = trailing.lastTopTime
   const startIdx = timeToDisplayIdx(trailing.last_top_time)
   const x1 = g.l + (startIdx + 0.5) * step
   const x2 = g.plotRight  // Pine L722: 终点延伸约 20 bar，clamp 到 plotRight
   ```
3. **颜色保留按 strong/weak 区分**（有意视觉差异，Pine 用固定 bearish/bullish 色）。

### 七、adapter 文档清理（Bug #8）
1. **`smc_view_adapter.py` L1-28 docstring 更新**：删除 "约 12000 根 15m time" 失真描述；更新 warmup 分离后的准确描述（15m 5000 计算/4000 展示）；新增 "OB 顺序保持 core 输出的 newest-first（Pine unshift 语义）"。

### 八、golden 测试修复（Bug #9）
1. **`test_smc_tv_parity.py` 修复**：
   - 新增 `_infer_timeframe_from_path` 辅助函数（从 fixture 文件名推断 timeframe）
   - L80: 日内时间戳用 `dt.isoformat()` 替代 `dt.strftime("%Y-%m-%d")`（15m 不再压缩为日期）
   - L142: EQ 类型直接使用 `eq.get("type","")`（core 已输出 "EQH"/"EQL"，不再误映射）
   - L219-221: OHLC no-op 替换为输入 OHLC float 转换 sanity check
   - L286: 容差从 `abs(...) > 1` 改为 `!=`（0 容差，严格逐 bar 对齐）
2. **新增 3 个 parity 测试**：
   - `test_tv_csv_ob_parity`：OB 数量、confirmed_index、bias 断言
   - `test_tv_csv_eq_endpoint_parity`：EQ 两端点 prev_level/level、anchor→second_pivot 区间
   - `test_pine_to_core_to_adapter_to_render_chain`：全链断言（core→adapter→render model）

### 九、新增确定性测试（不依赖 TV CSV fixture）
1. **`test_smc_pine_deterministic.py`（427 行，8 个测试类）**：
   - `TestChoCHRules`：CHoCH 规则（bearish after bullish + tag before bias update）
   - `TestBOSRules`：BOS（bias 延续时非 CHoCH）
   - `TestWarmupConsistency`：5000 计算/4000 展示 vs 4000 计算/4000 展示（重叠窗口一致）
   - `TestOrderBlockOrder`：OB newest-first
   - `TestOrderBlockChain`：core→adapter 字段完整性（顺序、anchor、high/low、mitigation）
   - `TestTrailingNaN`：trailing NaN + last_top_time/last_bottom_time
   - `TestExecutionGate`：internal/swing gate 关闭→事件为空
   - `TestEqualHighLowGeometry`：EQ 两端点 + 区间方向

### 十、导出能力增强
1. **`ref/smc_user_export.pine` L872-880 追加 8 个隐藏 plot**（`display=display.none`）：
   - `_exp_trailing_top` / `_exp_trailing_bottom`（trailing Strong/Weak level）
   - `_exp_trailing_last_top_time` / `_exp_trailing_last_bottom_time`（Strong/Weak 线起点时间）
   - `_exp_swing_high_level` / `_exp_swing_low_level`（swing pivot level）
   - `_exp_int_high_level` / `_exp_int_low_level`（internal pivot level）
2. **真源 `ref/smc_user_source.pine` 不可变**（SHA256 `0bd3d2ad...881f`，843 行，已校验未修改）；仅修改导出副本。

### 十一、ALGORITHM_VERSION v9→v10
1. **`indicator_cache.py` L57**: `ALGORITHM_VERSION = "v9"` → `"v10"`，附 v10 版本注释（warmup/gate/trailing/OB 逻辑变更）。
2. **`test_indicator_cache.py`**: `test_cache_algorithm_version_bumped_to_v9` → `test_cache_algorithm_version_bumped_to_v10`（断言 v10、新 key 含 v10 不含 v9）。
3. **旧 v9 SMC 缓存自动失效**：SMC 输出（事件数量/位置、OB 顺序、窗口左缘 pivot/BOS/CHoCH）与新逻辑不一致，必须强制失效。

### 十二、SMC 状态提示 UI（Pine parity 防误读）
1. **`StrategyChart.tsx` L2659-2666 新增 `smc-completed-bar-hint` overlay**：
   - 仅在 `effectiveLayers.smc === true` 时显示（SMC 图层开启）
   - 文案："SMC 基于最新已完成K线"
   - 位置：`tv-canvas-wrap` 右下角（`bottom: 6, right: 60`），`zIndex: 8`，半透明深色背景，`pointerEvents: 'none'`（不拦截交互）
2. **语义**：SMC 按 Pine 语义基于"最新已完成K线"计算，盘中实时合成K线不参与 parity 计算；提示用户不得将实时合成K线误解为实时 SMC parity。

## 影响模块
- 后端核心：`smc_pine_core.py`（execution gate + trailing NaN + OB 顺序 + DEFAULT_PARAMS 新增 3 参数）
- 后端服务：`indicator_service.py`（warmup/历史分离 + 新增 3 常量）、`smc_view_adapter.py`（docstring 清理）、`indicator_cache.py`（ALGORITHM_VERSION v10）
- 前端：`StrategyChart.tsx`（EQH/EQL 两端点线 + Strong/Weak 起点 + `timeToDisplayIdx` 辅助 + SMC 状态提示 overlay）
- 测试：`test_smc_tv_parity.py`（golden 修复 + 3 新测试）、`test_smc_pine_deterministic.py`（新增 8 测试类）、`test_indicator_cache.py`（v10 断言）
- Pine 导出：`ref/smc_user_export.pine`（+8 hidden plots）
- 文档：`CHANGE-20260717-001.md`（新增）、`CHANGELOG.md`、`smc-user-pine-parity.md`、`02-data-api-contracts.md`、`04-frontend-ux.md`、`05-testing-acceptance.md`、`code-doc-alignment.md`、`backend-module-map.md`、`api-route-map.md`、`test-coverage-map.md`

## 修改文件
- `backend/app/services/indicator_service.py`（warmup/历史分离）
- `backend/app/services/smc_view_adapter.py`（docstring 清理）
- `backend/app/services/indicator_cache.py`（ALGORITHM_VERSION v9→v10）
- `backend/app/strategy_assets/algorithms/features/smc_pine_core.py`（gate + trailing NaN + OB 顺序）
- `backend/tests/test_smc_tv_parity.py`（golden 修复 + 3 新测试）
- `backend/tests/test_smc_pine_deterministic.py`（新增，427 行，8 测试类）
- `backend/tests/test_indicator_cache.py`（v10 断言）
- `frontend/src/components/StrategyChart.tsx`（EQH/EQL + Strong/Weak）
- `ref/smc_user_export.pine`（+8 hidden plots）

## 测试
- 后端 SMC 确定性测试（`test_smc_pine_deterministic.py`）：11 passed / 1 skipped（OB 顺序测试因合成数据不足 2 个 OB 而 skip；CHoCH/BOS/warmup 一致性/OB 全链/trailing NaN/execution gate/EQ 几何）
- 后端 SMC 指标测试（`test_smc_indicator.py`）：44 passed / 1 skipped（短数据测试）
- 后端 SMC view adapter（`test_smc_view_adapter.py`）：31 passed
- 后端 SMC TV parity（`test_smc_tv_parity.py`）：6 skipped（`PINE_PARITY_PENDING`，无真实 TV CSV fixture），不伪造 fixture
- 后端缓存测试（`test_indicator_cache.py`）：10 passed（含 `test_cache_algorithm_version_bumped_to_v10`）
- 后端 SMC 测试汇总：85 passed / 7 skipped（skip 均为 PINE_PARITY_PENDING 或数据不足）
- ruff 改动文件：All checks passed
- mypy 改动文件（4 个生产文件）：0 errors
- 前端 SMC rendering（`smcRendering.test.ts`，node:22-alpine）：40 passed
- tsc --noEmit：0 errors
- eslint 改动文件（`StrategyChart.tsx`）：0 errors / 2 pre-existing warnings（unused eslint-disable，非本轮新增）
- vite build（outDir=/tmp/smc-build-verify 容器内 ephemeral）：Success，255 modules transformed in 5.03s
- docs 检查 4 项：check_docs_consistency PASS / check_architecture 4 pre-existing violations（useApi.ts/MarketWorkspacePage.tsx，非本轮 diff，out of SMC scope）/ check_test_allowlist PASS / update_docs PASS
- git diff --check：0 errors
- 敏感信息检查：代码文件无新增敏感信息
- 大文件检查：diff 中无 >1MB 文件

## Known Gaps
- **PINE_PARITY_PENDING**：无真实 TradingView CSV fixture，代码级修复通过，输出级 parity pending；不伪造 fixture，不声称"完全对齐"。fixture 路径 `backend/tests/fixtures/smc_pine/smc_tv_<symbol>_<tf>.csv`，需用户从 TradingView 导出（使用 `ref/smc_user_export.pine` 的 26 个隐藏 plot）。
- **Strong/Weak 颜色有意视觉差异**：前端按 strong/weak 区分颜色（强高红/弱高绿、强低绿/弱低红），Pine 用固定 bearish/bullish 色；不影响数据与几何，列为有意视觉差异。
- **15m warmup 1000 根**：5000 计算/4000 展示，warmup 窗口左缘少量 bar 可能与完整历史有差异（ATR/pivot 依赖历史），`TestWarmupConsistency` 验证重叠窗口大部分一致。
- **1mo ≥200 根**：扩展回看 7000 天（≈233 月），优先全历史；ATR200 可初始化。

## 生产验证
（部署后填写：容器 SHA 一致性、health、RestartCount、5 周期 SMC 可见性）
