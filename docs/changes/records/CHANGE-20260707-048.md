# CHANGE-20260707-048: Snapshot Run Gate + Instrument-first Backfill

## 变更编号
CHANGE-20260707-048

## 任务名称
Feature Snapshot 运行态安全 + instrument-first backfill 重构

## 需求出处
PR #38 合并部署后用户要求进入下一 PR：
- 修复 scripts 目录未进 Docker 镜像的问题；
- 清理 PR #38 smoke test 写入的 25 行非完整快照；
- 新增 snapshot date/run 级别成功标记，watchlist 只读成功完成的 snapshot 日期；
- 将 backfill 从 date-first 优化为 instrument-first；
- 仍保持小步验证，不全量生产回补。

## 修改前行为
1. `backend/scripts/` 目录未 COPY 到 Docker 镜像，容器内无法直接运行 `python -m scripts.feature_snapshot_backfill`；
2. PR #38 smoke test 在生产 `stock_feature_snapshots` 表写入了 25 行 2026-07-07 的非完整快照（无 succeeded run 标记）；
3. watchlist 只通过 `expected_snapshot_trade_date` 查询 snapshot 是否存在，无法区分"计算中"（running）/"已失败"（failed）/"已成功"（succeeded），可能读取半成品行；
4. backfill 脚本为 date-first 架构：每个交易日遍历全市场，每只股票重复 fetch 1d/15m bars，全量回补非常重。

## 修改后行为
1. `backend/Dockerfile` 新增 `COPY scripts ./scripts`，容器内可直接运行 `python -m scripts.feature_snapshot_backfill --help`；
2. 新增 `stock_feature_snapshot_runs` 表（partial unique index 仅约束 `status='running'`，允许 failed run 与新 retry 并存）；
3. `after_close` / `backfill` 开始时创建 `running` run（独立 session + commit），成功时 `finish_snapshot_run(status='succeeded')` 写 `published_at`，失败时 `finish_snapshot_run(status='failed')` 不写 `published_at`；
4. `/watchlist/monitor-status` 通过 `_has_succeeded_snapshot_run` 判断 `expected_snapshot_trade_date` 是否有 `succeeded` run，无 succeeded run 时返回 `WAITING_SNAPSHOT` 或 `NO_SNAPSHOT`；
5. backfill 脚本重构为 instrument-first：每只股票每周期只调用一次 `load_instrument_bars`，内存中按 `trade_date` slice，复用 `compute_feature_snapshot_for_date(primary_bars=..., secondary_bars=...)` 已有入参；
6. backfill 新增 `--symbols` / `--limit-instruments` 参数支持小样本验证；
7. backfill run gate：每个 trade_date 创建 `succeeded`/`failed` run，`--resume` 跳过已存在 snapshot 且所属日期有 `succeeded` run 的行（双重过滤）。

## 影响模块
- backend Dockerfile
- backend/app/models/stock_feature_snapshot_run.py（新增）
- backend/app/models/__init__.py
- backend/app/services/feature_snapshot_service.py
- backend/app/services/after_close_orchestrator.py
- backend/app/api/watchlist.py
- backend/scripts/feature_snapshot_backfill.py
- backend/alembic/versions/057_stock_feature_snapshot_runs.py（新增）
- backend/tests/（4 个测试文件）

## 修改文件

### 代码
- `backend/Dockerfile`：新增 `COPY scripts ./scripts`
- `backend/app/models/stock_feature_snapshot_run.py`：新增 ORM 模型（19 列 + partial unique index + 3 btree 索引）
- `backend/app/models/__init__.py`：注册 `StockFeatureSnapshotRun`
- `backend/app/services/feature_snapshot_service.py`：新增 `create_snapshot_run` / `finish_snapshot_run` run lifecycle 函数
- `backend/app/services/after_close_orchestrator.py`：feature_snapshot 步骤前后写 run lifecycle（独立 session 保证 run 记录持久化）
- `backend/app/api/watchlist.py`：新增 `_has_succeeded_snapshot_run` helper，snapshot 查询前先检查 succeeded run
- `backend/scripts/feature_snapshot_backfill.py`：重构为 instrument-first，新增 `get_instruments_for_backfill` / `load_instrument_bars` / `backfill_instrument_first`
- `backend/alembic/versions/057_stock_feature_snapshot_runs.py`：新增迁移

### 测试
- `backend/tests/test_feature_snapshot_run_service.py`：新增 6 个用例（create/finish/idempotent/failed retry/published_at/metadata）
- `backend/tests/test_watchlist_monitor_status_snapshot.py`：新增 4 个 run gate 用例（running/failed/succeeded/no_run）
- `backend/tests/test_after_close_orchestrator.py`：新增 2 个 run lifecycle 用例（success_creates_succeeded_run / failure_creates_failed_run）
- `backend/tests/test_feature_snapshot_backfill.py`：重构为 21 个用例（instrument-first + run gate + --symbols/--limit-instruments）

### 文档
- `docs/current/02-data-api-contracts.md`：新增 13.7 `stock_feature_snapshot_runs` 表契约；13.3 增加 run gate 说明
- `docs/current/03-jobs-integrations-operations.md`：2.3 feature_snapshot 步骤增加 run lifecycle；2.4 重写为 instrument-first 架构 + run gate + 小样本验证流程
- `docs/current/05-testing-acceptance.md`：更新测试用例数（21+11+11+6），新增 run gate / run lifecycle / instrument-first 测试描述
- `docs/maps/database-model-map.md`：新增 `stock_feature_snapshot_runs` 表入口
- `docs/maps/backend-module-map.md`：更新 feature_snapshot / feature_snapshot_backfill 行
- `docs/maps/worker-job-map.md`：更新 worker-after-close 行，新增 scripts.feature_snapshot_backfill 行
- `docs/maps/test-coverage-map.md`：新增 run gate / run lifecycle / instrument-first 测试覆盖映射

## 文档更新
见上方"修改文件 > 文档"章节。

## 测试证据

### 后端测试（test DB port 5433）
```
APP_ENV=test TEST_DATABASE_URL=postgresql+psycopg://bz:bz@localhost:5433/bz_stock_test \
  .venv/bin/python -m pytest \
    tests/test_after_close_orchestrator.py \
    tests/test_feature_snapshot_backfill.py \
    tests/test_feature_snapshot_run_service.py \
    tests/test_watchlist_monitor_status_snapshot.py -v

============================== 49 passed in 2.49s ===============================
```

### Ruff
```
.venv/bin/ruff check app/services/after_close_orchestrator.py \
  app/services/feature_snapshot_service.py app/api/watchlist.py \
  app/models/stock_feature_snapshot_run.py scripts/feature_snapshot_backfill.py \
  tests/test_after_close_orchestrator.py tests/test_feature_snapshot_backfill.py \
  tests/test_feature_snapshot_run_service.py tests/test_watchlist_monitor_status_snapshot.py
All checks passed!
```

### Mypy
新增 backend/app 生产文件 `stock_feature_snapshot_run.py` mypy 零错误。
`after_close_orchestrator.py` Phase 7 新增代码（2 处 `db.get` 调用）添加 None check，0 新增 mypy 错误（baseline 保持 8+7=15 历史债务）。

### Alembic 迁移（test DB）
- `alembic upgrade head` 成功创建 `stock_feature_snapshot_runs` 表；
- `alembic downgrade -1` 成功删除表；
- `alembic upgrade head` 再升级幂等。

## Git 分支
`feat/snapshot-run-gate-instrument-backfill`

## Git Commit
（PR 创建后填写）

## 数据库迁移
- 新增迁移：`057_stock_feature_snapshot_runs`（`down_revision='056_stock_feature_snapshots'`）
- 迁移内容：CREATE TABLE `stock_feature_snapshot_runs`，含 partial unique index `uq_snapshot_runs_active_key`（仅约束 `status='running'`）与 3 个 btree 索引
- 部署阶段：`alembic upgrade head`（test DB 已验证 upgrade/downgrade/upgrade 链路）

## 配置变化
无新增环境变量。

## 风险
1. **生产数据清理**：PR #38 smoke test 写入的 25 行 2026-07-07 snapshot 需在部署前清理（`delete from stock_feature_snapshots where trade_date='2026-07-07'`），避免 watchlist 长期显示半成品；
2. **全量 backfill 未执行**：禁止直接全量回补，需小样本验证后再逐步扩大；
3. **run 表体积**：每次 after_close / backfill 创建 run 记录，需观察 `stock_feature_snapshot_runs` 表增长（预计每日 1 行 after_close + backfill 期间多行）。

## 遗留问题
1. **全量 backfill 未执行**：等待用户确认后逐步扩大；
2. **前端 UI 三态展示**：未实现（本 PR 不做前端）；
3. **task status 联动**：`SNAPSHOT_FAILED` 状态未与 `scheduler_job_runs` 联动（run 表已记录 `failed`，但 orchestrator 仍按 `feature_snapshot` 失败处理）。

---

## [Blocker Fix] Run Gate 严格化 + Scope 区分（追加）

### Blocker 来源
PR #39 review 阶段发现 `_has_succeeded_snapshot_run` 仅检查 `status='succeeded'`，存在两个安全漏洞：
1. 未检查 `published_at` 是否非空（异常状态 succeeded run 可能被读取）；
2. 未区分 run scope（小样本 backfill `--symbols`/`--limit-instruments` 即使 succeeded + published_at 也会污染生产 watchlist SUCCEEDED 状态）。

### 修改内容（追加 commit）

#### 代码
- `backend/app/api/watchlist.py`：`_has_succeeded_snapshot_run` 增加 `published_at IS NOT NULL` + `metadata_["scope"].astext == "full"` 两个查询条件；
- `backend/app/services/feature_snapshot_service.py`：`create_snapshot_run` 新增 `scope: str | None = None` kwarg，注入到 `metadata_["scope"]`（如未显式设置）；
- `backend/app/services/after_close_orchestrator.py`：`create_snapshot_run(scope="full")` + `finish_snapshot_run(metadata={"scope": "full", ...})`（succeeded / failed 两处）；
- `backend/scripts/feature_snapshot_backfill.py`：
  - 新增 `SCOPE_FULL` / `SCOPE_SAMPLE` 常量 + `_resolve_run_scope(symbols, limit_instruments)` helper（任一过滤启用 → `sample`，都未启用 → `full`）；
  - `backfill_instrument_first` 新增 `scope: str = SCOPE_FULL` 参数，传播到 `create_snapshot_run(scope=scope)` + `finish_snapshot_run(metadata={"scope": scope, ...})`；
  - `main()` 调用 `_resolve_run_scope(args.symbols, args.limit_instruments)`，dry-run / 正式模式都传入 scope。

#### 测试
- `backend/tests/test_watchlist_monitor_status_snapshot.py`：新增 3 个用例（共 14 个）
  - `test_monitor_status_succeeded_run_null_published_at_blocks_read`：succeeded + published_at=None → 不可读；
  - `test_monitor_status_backfill_full_scope_run_allows_read`：backfill + scope='full' + published_at → 可读；
  - `test_monitor_status_backfill_sample_scope_run_blocks_read`：backfill + scope='sample' + published_at → 不可读；
  - 修改 `_make_succeeded_run` helper 添加 `metadata_={"scope": "full"}`，保证现有 4 个 run gate 测试在新严格 gate 下仍通过。
- `backend/tests/test_feature_snapshot_backfill.py`：新增 4 个用例（共 25 个）
  - `test_resolve_run_scope_returns_sample_when_symbols_set`；
  - `test_resolve_run_scope_returns_sample_when_limit_instruments_set`；
  - `test_resolve_run_scope_returns_full_when_no_filter`；
  - `test_backfill_instrument_first_propagates_sample_scope_to_run_metadata`：验证 `create_snapshot_run` 收到 `scope='sample'` kwarg + `finish_snapshot_run` 的 metadata 含 `'scope': 'sample'`。

#### 文档
- `docs/current/02-data-api-contracts.md`：Run gate 段落改写为三条件（status / published_at / scope）；`metadata_` 字段补 `scope` 必查键说明；Run lifecycle 表新增 `metadata_.scope` 列。
- `docs/current/03-jobs-integrations-operations.md`：CLI 参数表 `--symbols`/`--limit-instruments` 补 `scope='sample'` 触发说明；Run gate 段落增加 scope 区分规则；生产小样本验证流程增加"小样本 run 不得发布到 watchlist"步骤。
- `docs/current/05-testing-acceptance.md`：watchlist 测试用例数 11→14，新增 publish gate 严格化 3 用例；backfill 测试用例数 21→25，新增 scope 区分 4 用例。
- `docs/maps/backend-module-map.md`：`feature_snapshot` / `feature_snapshot_backfill` 行补 scope 必传 + 区分规则。
- `docs/maps/worker-job-map.md`：`worker-after-close` / `scripts.feature_snapshot_backfill` 行补 scope 区分说明。
- `docs/maps/test-coverage-map.md`：Run gate 行补 [Blocker Fix] 严格化 + scope 区分；新增 `[Blocker Fix - scope 区分]` 行。

### Gate 查询最终条件
```python
stmt = select(StockFeatureSnapshotRun.id).where(
    StockFeatureSnapshotRun.trade_date == trade_date,
    StockFeatureSnapshotRun.schema_version == schema_version,
    StockFeatureSnapshotRun.status == "succeeded",
    StockFeatureSnapshotRun.published_at.is_not(None),
    StockFeatureSnapshotRun.metadata_["scope"].astext == "full",
).limit(1)
```

### Sample / Full Run 元数据样例
```json
// after_close succeeded run（full scope）
{
  "source": "after_close_orchestrator",
  "scope": "full"
}

// backfill succeeded run（full scope，不带 --symbols/--limit-instruments）
{
  "source": "backfill",
  "batch_size": 20,
  "failure_threshold": 0.3,
  "scope": "full"
}

// backfill succeeded run（sample scope，--symbols 000100,603303）
{
  "source": "backfill",
  "batch_size": 20,
  "failure_threshold": 0.3,
  "scope": "sample"
}
```

### 新增测试列表
1. `test_monitor_status_succeeded_run_null_published_at_blocks_read`
2. `test_monitor_status_backfill_full_scope_run_allows_read`
3. `test_monitor_status_backfill_sample_scope_run_blocks_read`
4. `test_resolve_run_scope_returns_sample_when_symbols_set`
5. `test_resolve_run_scope_returns_sample_when_limit_instruments_set`
6. `test_resolve_run_scope_returns_full_when_no_filter`
7. `test_backfill_instrument_first_propagates_sample_scope_to_run_metadata`

### 质量门禁结果
```
# pytest（test DB port 5433）
APP_ENV=test TEST_DATABASE_URL=postgresql+psycopg://bz:bz@localhost:5433/bz_stock_test \
  .venv/bin/python -m pytest \
    tests/test_feature_snapshot_backfill.py \
    tests/test_after_close_orchestrator.py \
    tests/test_feature_snapshot_run_service.py \
    tests/test_watchlist_monitor_status_snapshot.py -q
56 passed in 2.85s

# ruff
.venv/bin/ruff check app/api/watchlist.py app/services/feature_snapshot_service.py \
  app/services/after_close_orchestrator.py scripts/feature_snapshot_backfill.py \
  tests/test_watchlist_monitor_status_snapshot.py tests/test_feature_snapshot_backfill.py
All checks passed!

# mypy（无新增错误，133 errors 全部为 baseline 历史债务，baseline 242 total）
.venv/bin/mypy app/services/feature_snapshot_service.py app/services/after_close_orchestrator.py \
  app/api/watchlist.py scripts/feature_snapshot_backfill.py

# 文档检查
python tools/check_docs_consistency.py  # PASS
python tools/check_architecture.py       # PASS
python tools/check_test_allowlist.py     # PASS
python tools/update_docs.py --check      # PASS
```

### Known Gap（追加）
1. **`finish_snapshot_run` 的 metadata 完全替换 create 时的 metadata**：调用方必须在 finish 时再次包含 scope，否则 watchlist gate 会因缺失 scope 而拒绝读取。已在代码注释和文档中显式说明，但无运行时校验（依赖调用方约定）。
2. **`_get_succeeded_trade_dates`（resume 判断）未加 scope 检查**：resume 只看 `status='succeeded'`，不看 scope。理论上 sample run 也会被 resume 跳过，但因 sample run 默认 `metadata_.scope='sample'` 不会进入 watchlist gate，仍可被 resume 复用（设计意图：sample 验证后转 full 不应重复跑）。如需严格区分，可后续追加 scope 检查。
