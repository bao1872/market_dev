# CHANGE-20260704-021

## 变更编号

CHANGE-20260704-021

## 任务名称

worker/notification/capture 边界审计 + 后续小 PR 拆分计划

## 需求出处

用户于 2026-07-04 要求：合并 PR #8 后进入边界审计阶段，审计 worker.py / outbox_relay / delivery_worker / stock_detail_feishu_service / stock_capture_service / capture / feishu 相关代码，输出可执行的后续拆分计划。本轮不做大规模重构，不改生产代码。

## 修改前行为

- 无架构审计文档；
- `docs/maps/worker-job-map.md` 未记录 `_notify_monitor_status` 绕过 Outbox 管道的直接发送路径；
- `docs/maps/notification-flow-map.md` 未记录监控启动/异常通知的直接发送路径；
- `docs/maps/test-coverage-map.md` 未记录 `_notify_monitor_status` 的测试缺口；
- `docs/current/code-doc-alignment.md` 未登记 `_notify_monitor_status` 绕过 Outbox 的架构债；
- CHANGE-020 状态为 committed，Head Commit 待合并后填写。

## 修改后行为

- 新增 `docs/architecture-audits/AUDIT-20260704-worker-notification-capture-boundaries.md`，回答 12 个审计问题，附后续小 PR 拆分计划；
- `docs/maps/worker-job-map.md` 补充 `_notify_monitor_status` 直接发送路径说明；
- `docs/maps/notification-flow-map.md` 补充第 6 节"监控启动/异常通知（直接发送路径）"；
- `docs/maps/test-coverage-map.md` 补充 `_notify_monitor_status` 测试缺口；
- `docs/current/code-doc-alignment.md` 新增 ALIGN-025（`_notify_monitor_status` 绕过 Outbox）；
- CHANGE-020 状态更新为 merged，Head Commit 补全为 `30ddc8a`；
- 新增 CHANGE-021 索引行和本记录。

## 影响模块

- `docs/architecture-audits/`（新增目录 + 审计文档）
- `docs/current/` 文档闭环（code-doc-alignment 新增 ALIGN-025）
- `docs/maps/` 实现地图补充（worker-job-map、notification-flow-map、test-coverage-map）
- `docs/changes/` 变更记录闭环（CHANGE-020 补全、CHANGE-021 新增）
- 不影响任何生产业务代码

## 修改文件

- `docs/architecture-audits/AUDIT-20260704-worker-notification-capture-boundaries.md`（新增）
- `docs/current/code-doc-alignment.md`（新增 ALIGN-025）
- `docs/maps/worker-job-map.md`（补充 _notify_monitor_status 直接发送路径说明）
- `docs/maps/notification-flow-map.md`（补充第 6 节监控直接发送路径）
- `docs/maps/test-coverage-map.md`（补充 _notify_monitor_status 测试缺口）
- `docs/changes/CHANGELOG.md`（CHANGE-020 补全 + CHANGE-021 新增）
- `docs/changes/records/CHANGE-20260704-021.md`（本记录）

## 文档更新

- `docs/current/code-doc-alignment.md`：新增 ALIGN-025（`_notify_monitor_status` 绕过 Outbox，P2，待产品决策）
- `docs/maps/worker-job-map.md`：补充 `_notify_monitor_status` 直接发送路径
- `docs/maps/notification-flow-map.md`：补充第 6 节监控直接发送路径
- `docs/maps/test-coverage-map.md`：补充 `_notify_monitor_status` 测试缺口

## 测试证据

本轮为 docs-only 审计，不改代码，不运行代码测试。

治理检查：
- `python tools/check_docs_consistency.py`：通过
- `python tools/check_architecture.py`：0 violations, 11 passed
- `python tools/check_test_allowlist.py`：通过
- `python tools/update_docs.py --check`：通过

## Git 分支

`chore/boundary-audit-worker-notification-capture`

## Git Commit

待合并后补全 squash merge commit（分支 `chore/boundary-audit-worker-notification-capture`）。

## 数据库迁移

无。不新增 migration，不修改历史 migration。

## 配置变化

无。不改 docker-compose / 环境变量 / 部署脚本。

## 风险

- 无。纯文档审计，不改生产代码。
- ALIGN-025 是新登记的已知 gap，不是新引入的问题（代码 TODO 已标记）。

## 遗留问题

- ALIGN-025（`_notify_monitor_status` 绕过 Outbox）：待产品决策（降级路径 vs 一致性）
- ALIGN-010/012/015/021：不在本轮处理范围
- 审计推荐的 PR-B 至 PR-F 为计划，未实现
