# 90 禁止恢复

- 多策略组合；
- 飞书 Webhook；
- MonitoringPlan 作为普通自选前提；
- 详情双行情源；
- quote→bar；
- FVG；
- 业务层直接行情 repository；
- Node VA 过滤 Peak；
- 四链绕过 Canonical Registry；
- `ref/` 运行时依赖；
- 前端重算指标；
- `/market` 主 K 线；
- 为每个小任务强制 feature/PR/人工部署审批；
- main/dev 同时运行两套应用；
- TRAE CN 只能部署、不能开发的旧定位。
