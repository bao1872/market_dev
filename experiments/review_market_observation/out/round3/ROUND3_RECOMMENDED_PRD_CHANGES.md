# ROUND 3 — RECOMMENDED PRD CHANGES (提案)

> **仅为实验提案。不修改正式 PRD。**
> 不写最终 frontend。不写正式实现方案。

---

## 1. 应保留的 Current Review 设计

- 家族 Retain/RetainAsSummary: ****
- 覆盖/部分覆盖的 components: **5 / 27**
  - 这些 component 与 Round 2 primitive 对齐良好或部分对齐。

## 2. 应降为 Summary Layer 的设计

## 3. 可能需要拆分/重构的 family

- **P**: verdict=RESTRUCTURE_CANDIDATE
  - 当前跨 3 个 primitive 维度: ['BREADTH', 'MIXED', 'PRICE']
  - component 内相关性低，加权平均会混合独立信号
  - 建议：按 primitive 维度拆分为多个子观察指标，family 保留 summary 角色

- **Q**: verdict=RESTRUCTURE_CANDIDATE
  - 当前跨 3 个 primitive 维度: ['DIFFUSION', 'STATE', 'TRANSITION']
  - component 内相关性低，加权平均会混合独立信号
  - 建议：按 primitive 维度拆分为多个子观察指标，family 保留 summary 角色

- **U**: verdict=RESTRUCTURE_CANDIDATE
  - 当前跨 3 个 primitive 维度: ['DIFFUSION', 'PARTICIPATION', 'TRANSITION']
  - component 内相关性低，加权平均会混合独立信号
  - 建议：按 primitive 维度拆分为多个子观察指标，family 保留 summary 角色

- **C**: verdict=RESTRUCTURE_CANDIDATE
  - 当前跨 1 个 primitive 维度: ['CONCENTRATION']
  - component 内相关性低，加权平均会混合独立信号
  - 建议：按 primitive 维度拆分为多个子观察指标，family 保留 summary 角色

## 4. 应补入正式 Observation 层的 primitive

- **STATE**: PARTIALLY_SUPPORTED
  - 直接表达 components: ['uptrend_member_ratio', 'main_structure_up_ratio', 'short_structure_up_ratio']
  - 仅在 MIXED component 中触及: ['price_position_median', 'price_position_median', 'price_position_median', 'price_position_median', 'price_position_median', 'price_position_median', 'structure_net_event_rate', 'structure_net_event_rate', 'structure_net_event_rate', 'structure_net_event_rate', 'structure_net_event_rate', 'structure_breakdown_diffusion', 'structure_breakdown_diffusion', 'structure_breakdown_diffusion', 'structure_breakdown_diffusion', 'structure_breakdown_diffusion', 'top5_amount_contribution', 'top5_amount_contribution', 'top5_amount_contribution', 'top5_amount_contribution', 'trend_segment_volume_improvement', 'trend_segment_volume_improvement', 'trend_segment_volume_improvement', 'trend_segment_volume_improvement']

- **TRANSITION**: PARTIALLY_SUPPORTED
  - 直接表达 components: ['structure_net_event_rate', 'momentum_enhancing_coverage']
  - 仅在 MIXED component 中触及: ['new_high_ratio', 'multi_dim_improving_ratio']

- **BREADTH**: MISSING_PRIMITIVE
  - 直接表达 components: ['advance_ratio']

- **CONCENTRATION**: PARTIALLY_SUPPORTED
  - 直接表达 components: ['top5_price_change_contribution', 'top10pct_event_contribution', 'member_change_hhi', 'leader_median_diff', 'top5_amount_contribution']
  - 仅在 MIXED component 中触及: ['price_position_median', 'uptrend_member_ratio', 'main_structure_up_ratio']

- **PARTICIPATION**: MISSING_PRIMITIVE
  - 直接表达 components: ['fresh_structure_event_coverage', 'non_head_participation_ratio', 'leader_follower_common_confirm_ratio', 'volume_expansion_ratio', 'amount_expansion_ratio', 'volume_percentile20_median', 'amount_percentile200_median']

- **PRICE**: PARTIALLY_SUPPORTED
  - 直接表达 components: ['scope_return_1d', 'new_high_ratio', 'price_position_median']

- **CROSS_HORIZON_DIVERGENCE**: MISSING_PRIMITIVE

## 5. 可能冗余的旧 component

- **uptrend_member_ratio**: 与同 family 其他 component spearman ≥ 0.7
- **main_structure_up_ratio**: 与同 family 其他 component spearman ≥ 0.7
- **short_structure_up_ratio**: 与同 family 其他 component spearman ≥ 0.7
- **volume_expansion_ratio**: 与同 family 其他 component spearman ≥ 0.7
- **amount_expansion_ratio**: 与同 family 其他 component spearman ≥ 0.7
- **volume_percentile20_median**: 与同 family 其他 component spearman ≥ 0.7
- **amount_percentile200_median**: 与同 family 其他 component spearman ≥ 0.7

> 需人工确认：冗余候选是同维度重复表达（可去重）还是虽相关但有独立语义（保留）。

## 6. 结论仍证据不足

- 无法判断 component: ['scope_return_1d', 'trend_price_alignment_ratio', 'fresh_structure_event_coverage', 'non_head_participation_ratio', 'leader_follower_common_confirm_ratio', 'top5_price_change_contribution', 'member_change_hhi', 'leader_median_diff', 'price_amount_efficiency_median']

> 主要原因：Round 2 primitive 列与 Review component 的维度映射 需要更大样本或更多 primitive 标签列来确认。

---

*Generated by Round 3 alignment analysis.*
