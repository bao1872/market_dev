# CHANGE-20260709-010: ruff strategy_assets C408/N806 债务清零

## 变更编号

CHANGE-20260709-010

## 任务名称

ruff baseline 中 strategy_assets C408 和 N806 债务清零 + instrument_seed.py N806 修复

## 需求出处

mypy app baseline 清零后，ruff baseline 剩余 889 个错误，其中 strategy_assets 的 C408（440）和 N806（174）+ instrument_seed.py N806（1）共 615 个可安全清理。

## 修改前行为

- `tools/quality_baselines/ruff.json` 记录 889 个错误；
- strategy_assets 中 440 个 C408（`dict()` → `{}`）和 174 个 N806（算法变量 camelCase 命名）；
- `instrument_seed.py` 中 1 个 N806（`BATCH_SIZE` 局部变量）。

## 修改后行为

- `tools/quality_baselines/ruff.json` total=274（删除 615 个 C408/N806 条目）；
- C408 全部通过 `ruff --fix --unsafe-fixes` 自动修复（`dict()` → `{}`）；
- N806 处理方式：
  - `instrument_seed.py`：`BATCH_SIZE` → `batch_size`（普通局部变量重命名）；
  - 3 个大量算法变量文件（`amp_plotly.py` 66、`amp_pine_full_replica.py` 51、`smc_probability_expo_pytdx_v2.py` 22）：per-file `# ruff: noqa: N806` + 注释说明 "kept to match upstream algorithm naming"；
  - 4 个少量算法变量文件（各 4 个 N806）：inline `# noqa: N806` + 注释 "kept to match upstream algorithm naming"；
- 不改变算法输出、不改变默认参数、不改变返回结构。

## 影响模块

- `backend/app/strategy_assets/algorithms/features/` 下 22 个文件（C408 auto-fix）
- `backend/app/strategy_assets/algorithms/features/amp_plotly.py`（per-file noqa）
- `backend/app/strategy_assets/algorithms/features/amp_pine_full_replica.py`（per-file noqa）
- `backend/app/strategy_assets/algorithms/features/smc_probability_expo_pytdx_v2.py`（per-file noqa）
- `backend/app/strategy_assets/algorithms/features/dsa_bbmacd_24factors_viewer.py`（inline noqa）
- `backend/app/strategy_assets/algorithms/features/dynamic_swing_anchored_vwap.py`（inline noqa）
- `backend/app/strategy_assets/algorithms/features/liquidity_zones_plotly.py`（inline noqa）
- `backend/app/strategy_assets/algorithms/features/merged_dsa_atr_rope_bb_factors.py`（inline noqa）
- `backend/app/services/instrument_seed.py`（变量重命名）
- `tools/quality_baselines/ruff.json`

## 修改文件

28 个文件修改 + 2 个 docs 文件 + 1 个 CHANGE + 1 个 CHANGELOG。

## 文档更新

- `docs/current/05-testing-acceptance.md`：新增 §5.1.3 Ruff baseline 债务治理规则
- `docs/current/03-jobs-integrations-operations.md`：新增 §11 Ruff baseline 债务治理（说明 ruff 清理不需要构建/部署/重启、长命令用 /tmp 日志、磁盘 <15GB 停止）
- `docs/changes/CHANGELOG.md`：新增 CHANGE-010 索引

## 测试证据

- pytest: 94 passed（dsa segments, bundle consistency, factor separation, selector, bars, watchlist）
- ruff: C408=0, N806=0（strategy_assets + instrument_seed）；剩余 170 个其他规则错误（N815/W293/F841 等）
- mypy: `app/` 0 errors（未受影响）
- docs checks: check_docs_consistency / check_architecture / check_test_allowlist / update_docs --check 全部通过

## Git 分支

`refactor/ruff-strategy-assets-debt`

## Git Commit

（提交后填写）

## 数据库迁移

无

## 配置变化

无

## 风险

- 低风险：C408 只做 `dict()` → `{}` 字面量转换，不改语义
- N806 的 per-file noqa 和 inline noqa 不改变任何代码逻辑
- `instrument_seed.py` 变量重命名只影响局部变量，不改 API

## 遗留问题

- ruff baseline 剩余 274 个错误（N815 68、W293 27、F841 19、E741 12、B905 11、N803 10、E402 10、N802 6、F401 3、其他）由后续 PR 分批处理
- ruff baseline 中有 2 个条目（`app/models/strategy_run.py` F821、`app/services/chart_bars_service.py` F821）已在 PR #60 中修复但未从 ruff baseline 删除，后续清理
