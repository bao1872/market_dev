# Alignment

## KNOWN_GAP

1. 当前仓库仍使用旧 `docs/` 时，需要迁移到 `maps/`。
2. GitHub Actions 自动部署尚未在腾讯云真实落地。
3. `/opt/panji-deploy` 目录、受限 deploy user 和 forced command 尚需初始化。
4. 现有 `deploy_live_runtime.sh` 必须先审查再作为固定脚本底层。
5. Capability V2 仍为 WIP。
6. 自动变更分类初期应保守，未知文件默认 BLOCKED。
