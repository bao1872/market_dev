# reports/archive/

任务结束并被后续工作取代后的历史报告。

按 `YYYY/MM/` 子目录归档，例如 `reports/archive/2026/07/`。

迁移规则：

1. 不改写原始报告内容；
2. 可以在文件顶部增加 `Legacy Report Metadata`；
3. 状态、原路径、迁移日期必须保留；
4. 在 `reports/INDEX.md` 建立索引；
5. 不把历史报告设置为 `LATEST`。

详见 `reports/README.md`。
