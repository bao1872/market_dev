# CHANGE-20260704-018

## 变更编号

CHANGE-20260704-018

## 任务名称

v2 docs 治理收口 + v2 结构检查加强

## 需求出处

用户于 2026-07-04 要求：PR #4 和 PR #5 合并后做安全和治理收口。
包括：删除 ~/.bashrc 中的 GitHub PAT、补全 CHANGE/CHANGELOG merge commit、关闭 ALIGN-024、
更新 MANIFEST 基线、加强 v2 docs structure 检查（8 个 map 全检）、补测试。

## 修改前行为

- `~/.bashrc` 第 100-101 行明文写入了 GitHub PAT（GH_TOKEN 和 GITHUB_TOKEN）；
- `docs/current/code-doc-alignment.md` ALIGN-024 仍在 KNOWN_GAP 表，描述为未来时（"本 PR 将会"）；
- `docs/current/MANIFEST.md` 实现核对基线为 `40dd2287...`（PR #3），未更新到当前 main HEAD；
- `docs/changes/records/CHANGE-20260703-017.md` Git Commit 字段为 "待提交后填写"；
- `docs/changes/CHANGELOG.md` CHANGE-017 行 Head Commit 为 "待合并后填写"；
- `tools/check_architecture.py` `check_v2_docs_structure()` 只检查 docs/maps/ 至少 1 个 .md 文件，不验证 8 个指定 map；
- 无 `tools/tests/test_check_architecture.py`，v2 结构检查无测试覆盖。

## 修改后行为

- `~/.bashrc` 已删除 token 行，本机不再存储 PAT（用户需到 GitHub 手动 revoke）；
- `docs/current/code-doc-alignment.md` ALIGN-024 移到 CLOSED 摘要（PR #5 已合并，v2 结构已落库）；
- `docs/current/MANIFEST.md` 实现核对基线更新为 `cafbdc4217301d8bf00ff9d42aeabbef43eb58fb`（当前 main HEAD）；
- `docs/changes/records/CHANGE-20260703-017.md` Git Commit 填入 `cafbdc4217301d8bf00ff9d42aeabbef43eb58fb`；
- `docs/changes/CHANGELOG.md` CHANGE-017 行 Head Commit 填入 `cafbdc4`，状态改为 merged；
- `tools/check_architecture.py` 新增 `V2_REQUIRED_MAP_FILES`（8 个），`check_v2_docs_structure()` 改为全检 8 个 map；
- 新增 `tools/tests/test_check_architecture.py`，覆盖 6 个场景。

## 影响模块

- `docs/current/` 文档闭环（code-doc-alignment、MANIFEST）
- `docs/changes/` 变更记录闭环（CHANGE-017、CHANGELOG）
- `tools/check_architecture.py` v2 结构检查加强
- `tools/tests/test_check_architecture.py` 新增测试
- 不影响任何生产业务代码

## 修改文件

- `docs/current/code-doc-alignment.md`（ALIGN-024 移到 CLOSED）
- `docs/current/MANIFEST.md`（基线更新到 cafbdc4）
- `docs/changes/records/CHANGE-20260703-017.md`（Git Commit 补全）
- `docs/changes/CHANGELOG.md`（CHANGE-017 补全 + CHANGE-018 新增）
- `docs/changes/records/CHANGE-20260704-018.md`（本记录）
- `tools/check_architecture.py`（V2_REQUIRED_MAP_FILES + maps 全检逻辑）
- `tools/tests/test_check_architecture.py`（新增 6 个测试场景）

## 文档更新

- `docs/current/code-doc-alignment.md`：ALIGN-024 从 KNOWN_GAP 移到 CLOSED 摘要
- `docs/current/MANIFEST.md`：实现核对基线和 v2 文档包生成基线更新到 cafbdc4

## 测试证据

- `python tools/check_architecture.py`：11 checks, 0 violations
- `python tools/check_docs_consistency.py`：全部通过（MANIFEST baseline cafbdc4）
- `python tools/check_test_allowlist.py`：通过
- `python tools/update_docs.py --check`：通过
- `python -m pytest tools/tests/test_check_docs_consistency.py -q`：全部通过
- `python -m pytest tools/tests/test_check_architecture.py -q`：6 个测试全部通过

## Git 分支

`chore/docs-v2-governance-finalize`

## Git Commit

待提交后填写。

## 数据库迁移

无。不新增 migration，不修改历史 migration。

## 配置变化

- 本机 `~/.bashrc` 删除了 GitHub PAT（GH_TOKEN 和 GITHUB_TOKEN）
- 不改 docker-compose / 环境变量 / 部署脚本

## 风险

- token 已在聊天记录中暴露，用户必须到 GitHub 手动 revoke
- MANIFEST 基线更新到 cafbdc4 后，check_docs_consistency.py 验证该 SHA 是 HEAD 祖先（cafbdc4 是当前 HEAD，通过）
- v2 结构检查从 "≥1 map" 加强为 "8 map 全检"，未来删除任何 map 文件会被 CI 阻断

## 遗留问题

- ALIGN-023（Worker heartbeat 僵尸 running）：代码已实现，待生产部署后验证
- ALIGN-010/012/015/021：不在本轮处理范围
- GitHub PAT 已暴露，用户需手动 revoke
