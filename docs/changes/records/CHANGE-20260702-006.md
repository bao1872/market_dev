# CHANGE-20260702-006：Phase 7 全量测试与构建链路验证，修复测试旧术语断言

## 1. 基本信息

- 日期：2026-07-02
- 状态：committed
- 需求提出人：项目负责人
- 需求出处：用户于 2026-07-02 明确要求，依据 `/root/web_dev/advice.md` 完成 Phase 7 全量测试、构建链路验证，并推送最新代码。
- Git 分支：`fix/release-feishu-marketdata-dsa`
- Base Code Commit：`ed476a050b1c562a994f82e23540d9c0492850c6`
- Head/Merge Commit：`3dfeaca8c4fd7ed3cf6f14373aeedb98f9c6b8b2`

## 2. 修改前理解

- 当前产品行为：Phase 2-5 的权限、飞书图片、行情聚合、DSA 门禁代码已提交，但尚未经过修复分支的完整后端/前端/构建链路验证。
- 当前前端流程：前端构建与契约测试脚本 `test:contract` 之前未在 Phase 7 完整执行。
- 当前后端流程：`/me/entitlements` 对无订阅用户返回 `detail="用户无订阅记录"`，但 `tests/test_me_entitlements.py` 仍按旧 Membership 术语断言 "会员记录" / "无会员"，导致全量测试 1 failed。
- 当前数据/API/Worker/第三方关系：本次仅修改测试断言与注释，不影响业务代码、数据模型、API 契约、Worker 或第三方集成。
- 已阅读的当前设计文档：
  - `docs/README.md`
  - `docs/current/10-permissions-security.md`
  - `docs/current/15-testing-acceptance.md`
  - `docs/current/18-code-doc-alignment.md`
- 发现的代码—文档—测试冲突：`tests/test_me_entitlements.py` 的断言与 `/me/entitlements` 实际返回的订阅术语不一致，属于 Phase 6 旧术语清理的残留测试缺口。

## 3. 修改目标

- 当前问题：Phase 7 后端全量测试因旧术语断言失败 1 例；前端契约测试脚本名称为单数 `test:contract`。
- 修改后行为：
  - 后端 pytest 全量 0 failed / 0 error；
  - Alembic upgrade → downgrade → upgrade 通过且 head 唯一；
  - 前端 `npm ci` / `tsc` / `lint` / `build` / `test:contract` 全部通过；
  - Docker Compose 生产配置 `config --quiet` 通过。
- 验收标准：
  - `pytest -q --tb=short tests/` 1079 passed；
  - `alembic heads` 唯一且为 `054_strategy_run_items_reason_code`；
  - 前端构建无错误；
  - `docker compose -f docker-compose.prod.yml --env-file /etc/market-dev/market.env config --quiet` 退出码 0。
- 明确不修改：
  - 不修改 DSA/Node Cluster/MACD/布林带公式参数；
  - 不修改 `/me/entitlements` 业务返回文案；
  - 不修改前端业务代码；
  - 不修改 Docker Compose 服务结构；
  - 不直接合并或推送 main。

## 4. 影响范围

- 产品需求：无用户可见功能变化。
- 业务规则和流程：无。
- 前端：无代码变化；仅验证构建链路。
- 后端：仅 `backend/tests/test_me_entitlements.py` 的注释与断言更新。
- 数据模型和迁移：无。
- API：无接口契约变化。
- 权限：无。
- Worker/第三方：无。
- 参数和配置：无。
- 部署：验证 `docker-compose.prod.yml` 配置语法可用。
- 测试：修复旧术语断言，使全量测试通过。

## 5. 实际修改

- 代码文件：`backend/tests/test_me_entitlements.py`
  - 将 "会员记录" / "无会员" 相关注释与 docstring 更新为 "订阅记录" / "无订阅"。
  - `test_entitlements_no_membership_returns_404` 断言改为 `"无订阅记录" in detail or "无订阅" in detail`。
- 数据库迁移：无。
- 配置变化：无。
- 兼容性和回滚：仅测试断言调整，可安全回滚。

## 6. 文档更新

- `docs/current/15-testing-acceptance.md`：测试策略已要求后端全量 pytest 与 Alembic 往返，本次验证结果与该要求一致，无需修改。
- `docs/current/18-code-doc-alignment.md`：本次修复属于 ALIGN-013 旧术语清理的测试侧收尾，未新增 KNOWN_GAP。

## 7. 测试与证据

- 修复前失败证据：`tests/test_me_entitlements.py::test_entitlements_no_membership_returns_404` 期望 "会员记录" / "无会员"，实际返回 `"用户无订阅记录"`。
- 新增/更新测试：未新增测试，仅修复既有测试断言。
- 执行命令与结果：
  - 后端全量测试：`APP_ENV=test TEST_DATABASE_URL=postgresql+psycopg://bz:bz@127.0.0.1:5432/bz_stock_test pytest -q --tb=short tests/` → `1079 passed, 6 warnings in 93.10s`。
  - Alembic 往返：`alembic upgrade head && alembic downgrade base && alembic upgrade head && alembic heads` → head 唯一 `054_strategy_run_items_reason_code`。
  - 静态检查：`python -m compileall backend/app`、`python tools/check_architecture.py`、`python tools/check_test_allowlist.py`、`git diff --check` 均通过。
  - 前端构建：`npm ci`、`npx tsc --noEmit`、`npm run lint`、`npm run build`、`npm run test:contract` 均通过（36 个契约测试通过）。
  - Docker Compose 配置检查：`docker compose -f docker-compose.prod.yml --env-file /etc/market-dev/market.env config --quiet` 退出码 0。
- 手工或生产验收：本次不涉及。

## 8. 完成检查

- [x] 当前分支与记录一致，未直接修改 main
- [x] 代码符合修改后设计
- [x] 前端与 API 一致（无变化）
- [x] 后端与数据模型一致（无变化）
- [x] 权限、参数、Worker 和部署一致（无变化）
- [x] 测试验证当前设计
- [x] CHANGELOG 已更新
- [x] Alignment 已更新或关闭（测试侧旧术语清理完成）
- [ ] PR 回答所有设计与测试问题（留待 Phase 8）

## 9. 遗留事项

- 继续推进 Phase 8：建立 `release/docs-aligned-candidate` 合并候选并创建 PR。
