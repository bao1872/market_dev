{
  "meta": {
    "slice": "BOARD-REVIEW-CONSOLIDATION / Slice 1R (Source-Truth Correction)",
    "generated_from_head": "d6ad65a0f83025bc038bbfd33a4e17e32729008f",
    "current_state_corrected": "MUST split into two concepts: (A) Scope ENUMERATION — which scopes exist — CURRENTLY depends on BoardAnalysisSnapshot via _list_board_scopes_by_hierarchy (SELECT BoardAnalysisSnapshot WHERE trade_date=T AND board_analysis_run_id=source_board_run_id). (B) Scope MEMBERSHIP RESOLUTION — who is in each scope — uses MarketBoard + resolve_board_membership_at(T) (already PIT, independent of Board analytics). The previous Slice 1 misread (B) as (A)."
  },
  "scope_enumeration_current": {
    "owner": "Board runtime (BoardAnalysisSnapshot)",
    "mechanism": "review_orchestrator._list_board_scopes_by_hierarchy(trade_date, source_board_run_id) → SELECT BoardAnalysisSnapshot WHERE trade_date=T AND board_analysis_run_id=source_board_run_id",
    "covers": ["industry_l1", "industry_l2", "industry_l3", "concept"],
    "dependency": "BoardAnalysisRun must exist AND be published as market_aggregation pointer (data_run_id = BoardAnalysisRun.id)"
  },
  "scope_membership_resolution_current": {
    "owner": "Review scope service",
    "mechanism": "resolve_scope_members → discover_pit_available_boards (MarketBoard) + resolve_board_membership_at(T) (PIT membership)",
    "dependency": "MarketBoard taxonomy + PIT membership service — NOT BoardAnalysisSnapshot analytics",
    "note": "This is already Board-analytics-independent. Deleting Board runtime does NOT break membership resolution."
  },
  "target_design": {
    "scope_enumeration_owner": {
      "responsibility": "list industry_l1/l2/l3/concept scopes",
      "target_implementation": "MarketBoard / taxonomy + PIT-available scope registry (REPLACES BoardAnalysisSnapshot enumeration)",
      "change_required": "YES — this is the Slice 2 first cut: enumerate from MarketBoard taxonomy, not BoardAnalysisSnapshot",
      "parity_required": "old vs new discovery exact parity on: scope_type, scope_key, scope_name, taxonomy_version, taxonomy_compatibility_key, membership_version, scope count"
    },
    "pit_membership": {
      "guarantee": "membership(T) via resolve_board_membership_at / PIT membership service (already PIT, no historical backfill)",
      "change_required": "NONE"
    },
    "batch_performance": {
      "forbidden_design": "N boards × per-board membership SQL (N+1)",
      "required_design": "batch membership owner — single batch load T-day PIT membership for all in-scope boards",
      "status": "DESIGN ONLY — Slice 1R does not implement; if current membership service has N+1 risk, Slice 2 introduces batch owner"
    },
    "lineage_preservation": {
      "must_keep": ["taxonomy_version", "taxonomy_compatibility_key", "membership_version"],
      "reason": "Deleting Board runtime must NOT delete lineage. ScopeDefinition still needs taxonomy/membership version to trace PIT(T).",
      "constraint": "C4 from source_board_run_dependency.json"
    }
  },
  "slice2_first_cut": {
    "action": "Replace scope enumeration source from BoardAnalysisSnapshot to MarketBoard/taxonomy + PIT-available scope registry; keep membership resolution unchanged.",
    "how": [
      "1. review_orchestrator._list_board_scopes_by_hierarchy → enumerate from MarketBoard (no source_board_run_id)",
      "2. create_run unique constraint: replace source_board_run_id with (taxonomy_version, membership_version)",
      "3. ScopeDefinition.source_board_snapshot_id → permanent None / later drop column",
      "4. market_aggregation pointer identity → taxonomy+membership identity (DEFER schema)",
      "5. after_close: remove board stage, stock_core → review directly"
    ],
    "gate_before_review_without_board": "old discovery vs new discovery exact parity on (scope_type, scope_key, scope_name, taxonomy_version, taxonomy_compatibility_key, membership_version, scope count) MUST match before Review can be created/computed without BoardAnalysisRun."
  }
}
