# CHANGE-20260705-033

## 变更编号

CHANGE-20260705-033

## 任务名称

时序特征 V1（Temporal Features V1）+ 个股详情页结构状态面板隐藏开关

## 需求出处

- 用户 `/plan` 指令：在 PR #21（V1.8 结构状态因子）+ PR #22（MANIFEST baseline）已合并的基础上，新增 `Temporal Features V1` 后端服务/API 与前端结构状态面板隐藏开关。
- 关键约束（`/root/web_dev/advice.md`）：
  - 复用 V1.8 structural factors，不重写 DSA/SQZMOM/BB/ATR/Node；
  - V1 只支持 `as_of=latest`，不实现历史回放截断；
  - 单股票 1d + 15m，不新增 worker/大表/全市场循环；
  - 字段无法计算返回 null，组级异常写入 `degraded_reasons`，warmup 写入 `warmup_notes`；
  - 所有 anchor 取值必须 point-in-time，不得使用未来 bar；
  - 15m 不使用 15m DSA 位置类字段作为核心输入；
  - 前端结构状态面板默认隐藏，用户开关 + localStorage + 截图/参数强制隐藏。
- 设计文档：`.trae/documents/temporal-features-v1-and-hide-toggle.md` + `.trae/documents/temporal-features-v1-stage-6-9-completion.md`（用户确认）。

## 修改前行为

- V1.8 结构状态因子面板在 `/stock/:symbol` 默认显示，仅在 `capture=feishu` 截图模式和窄屏时隐藏。
- 不存在 Temporal Features API，无法获取时序变化量/持续度/派生关系。

## 修改后行为

### 验收缺口修复（同 PR 内）

1. **as_of 校验**：API 层加 `_ALLOWED_AS_OF = {"latest"}`，`as_of != "latest"` 返回 400。
2. **组级异常隔离**：`compute_temporal_features` 内部用 `_safe_compute` 包装 daily/m15/derived 三个组，单组抛异常 → 该组全 null dict + `degraded_reasons` 记录 `{group_name} failed: {exc}`，不影响其他组或整体 API 返回。
3. **前端 toggle 定位**：toggle 按钮从 `tv-workspace` 直接子元素移入 `tv-chart-column` 内部（`position: relative` 已存在），定位上下文稳定，避免在 `hide-structural-state` 单列模式下定位漂移。
4. **测试补强**（service 5 个新测试 + API 1 个新测试 + frontend 1 个新测试）：
   - `test_swing_anchor_low_when_bsl_less_than_bsh` — bsl<bsh→anchor=low 验证
   - `test_swing_anchor_high_when_bsh_less_equal_bsl` — bsh<=bsl→anchor=high 验证
   - `test_m15_position_change_since_swing_anchor_manual_calc` — 手算公式验证
   - `test_anchor_percentile_invariant_after_modification` — anchor 之后数据不影响 anchor 处 percentile
   - `test_m15_response_exception_isolation` — mock m15_response 抛异常 → 整体 200 + m15_response 全 null + degraded
   - API `test_invalid_as_of_returns_400` — as_of!=latest 返回 400
   - 前端 `Toggle button is inside tv-chart-column for stable absolute positioning` — toggle 在 tv-chart-column 内部 + tv-chart-column 有 position: relative

### 后端 `temporal_feature_service.py`（新建）

- `compute_temporal_features(session, instrument_id, primary_timeframe="1d", secondary_timeframe="15m", adj="qfq", as_of="latest")` 返回：
  - `daily_context`（9 字段）：`daily_dsa_dir`、`daily_dsa_segment_duration_percentile`、`daily_dsa_slope_atr_per_bar`、`daily_dsa_efficiency_0_1`、`daily_price_position_in_swing_0_1`、`daily_distance_to_swing_high_atr`、`daily_distance_to_node_above_atr`、`daily_sqzmom_change_since_segment_start`、`daily_volume_percentile_change_since_segment_start`；
  - `m15_response`（9 字段）：`m15_price_position_in_swing_0_1`、`m15_position_change_since_swing_anchor`、`m15_distance_to_swing_high_atr`、`m15_distance_to_swing_low_atr`、`m15_sqzmom_change_since_swing_anchor`、`m15_sqzmom_abs_percentile`、`m15_sqz_off`、`m15_bb_bandwidth_change_since_swing_anchor`、`m15_volume_percentile_change_since_swing_anchor`；
  - `derived_relation`（3 字段）：`m15_position_relative_to_daily`、`m15_response_direction_relative_to_daily`（aligned/counter/null）、`m15_response_intensity`（mean(abs)）；
  - `meta`：`as_of`、`primary_timeframe`、`secondary_timeframe`、`degraded_reasons`、`warmup_notes`。
- 复用 V1.8 `compute_structural_factors` 获取 primary/secondary factors；调用 `_compute_all_factors_for_bars` + `_fetch_bars` 在 temporal service 内部使用。
- 历史序列重算（point-in-time，无未来函数）：
  - SQZMOM：`compute_sqzmom_lb` 取 `val_list[idx]`；
  - volume_percentile：`percentile_rank(volumes[idx], volumes[:idx+1], 120)`；
  - BB bandwidth：重算 bollinger bandwidth 序列再 `percentile_rank`；
  - DSA 历史 segments：`compute_dsa_bundle` + `_find_bar_index_by_time` 定位起止 bar index。
- swing anchor 规则：`bars_since_swing_low < bars_since_swing_high` → anchor=swing_low_bar；否则 anchor=swing_high_bar；`anchor_bar_idx = len(bars) - 1 - min(bsh, bsl)`。
- 每个组（daily/m15/derived）独立 try/except，单组失败返回 null + `degraded_reasons`，不阻塞整体返回。

### 后端 `temporal_features.py`（新建 API）

- `GET /api/v1/instruments/{instrument_id}/temporal-features`，参数 `primary_timeframe`/`secondary_timeframe`/`adj`/`as_of`；
- 非法参数返回 400；不存在 instrument 返回 200 + `degraded_reasons`（与 `/structural-factors` 一致，不报 404）；
- 在 `app/main.py` 注册 `temporal_features_router`。

### 前端 `StockDetailPage.tsx`（修改）

- 结构状态面板默认隐藏，用户点击「显示结构状态」开关后显示，`localStorage.showStructuralState` 持久化；
- toggle 按钮位置改为图表上方独立 toolbar 行（`.structural-state-toolbar` + `.structural-state-toggle-btn`），按钮右对齐，文案动态切换（隐藏时「显示结构状态」，显示时「隐藏结构状态」）；
- URL 参数 `?hideStructuralState=1` / `?capture=1` / `?capture=feishu` 任意一个命中即强制隐藏按钮和面板且禁用开关按钮（`toggleStructuralState` 回调 early return），忽略 `localStorage`；
- 截图模式（`capture=feishu`）默认只渲染 K 线和基础信息；
- 新增 `.structural-state-toolbar` + `.structural-state-toggle-btn` 样式 + `.hide-structural-state` 单列布局样式。

### 前端 Temporal Features UI（端到端核查后补齐）

- 新增 `getTemporalFeatures` API 函数 + `TemporalFeaturesResponse` 类型（endpoints.ts）；
- 新增 `useTemporalFeatures` hook（useApi.ts），交易时段 60s 轮询；
- `StockStructuralStatePanel` 末尾新增 `TemporalFeaturesCard` 折叠卡片，渲染 temporal-features API DTO：
  - `daily_context`（9 字段）+ `m15_response`（9 字段）+ `derived_relation`（3 字段）+ `meta`；
  - null 字段显示「-」；
  - `warmup_notes` / `degraded_reasons` 有内容时显示提示条；
  - 受同一个结构状态开关控制（默认随面板隐藏，用户打开面板后显示）。

### 端到端核查 + API smoke test（同 PR 内补充）

- 根因确认：PR #23 未合并 + 生产容器使用 main 分支 → 用户看不到任何前端变化属预期；
- API smoke test（feat 分支临时容器 + 真实 instrumentId `000c53ee-...` / symbol `603897`）：
  - `GET /api/v1/instruments/{id}/temporal-features?as_of=latest` → 200，返回完整 DTO（daily_context 9 字段 + m15_response 9 字段 + derived_relation 3 字段 + meta，duration_percentile=null + warmup_notes 提示 segments 不足）；
  - `GET /api/v1/instruments/{id}/temporal-features?as_of=2025-01-01` → 400，错误信息「不支持的 as_of: 2025-01-01, V1 只支持: ['latest']」；
  - 生产容器（main）→ 404（印证根因）。

### 成本/节点位置语义修复（同 PR 内补充）

**问题**：`cost_position.position_0_1` 实际来自 `vp_result.position_0_1(last_close)`，含义是 VP 全价格范围 lowest~highest 中的位置，不是节点区间位置，也不是 VA 位置。前端显示成「位置[0,1]」会误导（截图案例 close=147.62, lower=123.22, upper=147.63，position_0_1=0.705 但节点区间位置应接近 1.000）。

**修复**：
- 后端保留原 `position_0_1` 字段（VP 全区间语义不变），新增 4 个字段：
  - `node_interval_position_0_1` = `clip((close - lower) / (upper - lower), 0, 1)`（节点区间位置，clip 到 [0,1]）
  - `node_interval_position_raw` = `(close - lower) / (upper - lower)`（不 clip，诊断用）
  - `cost_position_zone` = close 相对节点的位置分类（below_lower_node / between_nodes / above_upper_node / below_upper_node / above_lower_node / null）
  - `value_area_zone` = close 相对 VA 的位置分类（below_va / inside_va / above_va / null）
- 后端新增 `val_price` / `vah_price` 字段（原值供前端显示）
- 前端成本卡字段顺序调整 + 标签重命名：
  - 「位置 [0,1]」→「VP全区间位置[0,1]」（原 position_0_1）
  - 新增「节点区间位置[0,1]」（node_interval_position_0_1）
  - 「VA 位置 [0,1]」→「VA位置raw」（value_area_position_0_1 不 clip）
  - 新增「VA状态」（value_area_zone）
  - 新增「成本区间状态」（cost_position_zone）
  - 新增 VAL / VAH 原值显示
- 截图案例验证：close=147.62, lower=123.22, upper=147.63 → `node_interval_position_0_1`≈1.000（接近 upper），`position_0_1`≈0.705（VP 全区间，保持不变）。

## 影响模块

- `backend/app/services/temporal_feature_service.py`（新建）
- `backend/app/api/temporal_features.py`（新建）
- `backend/app/main.py`（注册 router）
- `backend/app/services/structural_factor_service.py`（新增 cost_position 位置语义修复字段 + 3 个辅助函数）
- `backend/tests/test_temporal_feature_service.py`（新建）
- `backend/tests/test_temporal_features_api.py`（新建）
- `backend/tests/test_structural_factor_service.py`（新增 18 个 cost_position 位置语义测试）
- `frontend/src/pages/StockDetailPage.tsx`
- `frontend/src/styles/global.scss`
- `frontend/src/api/endpoints.ts`（新增 TemporalFeaturesResponse 类型 + getTemporalFeatures 函数）
- `frontend/src/hooks/useApi.ts`（新增 useTemporalFeatures hook）
- `frontend/src/components/StockStructuralStatePanel.tsx`（新增 TemporalFeaturesCard 折叠卡片）
- `frontend/scripts/contract-tests/structural-state-toggle.test.ts`（扩展至 12 个测试）
- `frontend/package.json`（`test:contract` 脚本追加新测试文件）
- `docs/current/02-data-api-contracts.md`
- `docs/current/04-frontend-ux.md`
- `docs/current/05-testing-acceptance.md`
- `docs/maps/api-route-map.md`
- `docs/maps/frontend-route-map.md`
- `docs/maps/test-coverage-map.md`
- `docs/changes/CHANGELOG.md`
- `docs/changes/records/CHANGE-20260705-033.md`（本文件）

## 修改文件

同上"影响模块"。

## 文档更新

- `docs/current/02-data-api-contracts.md`：新增第 11 节「时序特征 API 契约 V1」，含端点、响应结构、字段表（9+9+3）、计算约束、降级策略、限制。
- `docs/current/04-frontend-ux.md`：line 72 扩展结构状态面板说明，新增「V1 默认隐藏」「强制隐藏」「截图模式」子项。
- `docs/current/05-testing-acceptance.md`：新增 Temporal V1 测试要求（后端服务/API、前端 toggle contract test、验收规则）。
- `docs/maps/api-route-map.md`：新增 `temporal_features` 行。
- `docs/maps/frontend-route-map.md`：更新 `/stock/:symbol` 行，补充 V1 默认隐藏 + 强制隐藏 + 截图模式说明。
- `docs/maps/test-coverage-map.md`：新增 3 行索引（temporal service、temporal API、toggle contract test）。
- `docs/changes/CHANGELOG.md`：顶部插入 V1 Temporal 条目。
- `docs/changes/records/CHANGE-20260705-033.md`：本文件。

## 测试证据

- 后端服务测试：`pytest tests/test_structural_factor_service.py tests/test_temporal_feature_service.py tests/test_temporal_features_api.py -q` → **88/88 passed**（含 18 个新增 cost_position 位置语义测试：zone 分类 6 种 + node_interval_position 公式 + clip + raw + null + 截图案例 close=147.62→1.000 + position_0_1 保持 VP 全区间语义）。
- 前端契约测试：`npm run test:contract` → **86/86 passed**（含 `structural-state-toggle.test.ts` 12 个 + `structural-state-panel.test.ts` 新增成本卡位置语义标签修复测试：禁止含糊「位置 [0,1]」、必须含「VP全区间位置[0,1]」「节点区间位置[0,1]」「VA状态」「VA位置raw」「VAL」「VAH」）。
- 前端：`npx tsc --noEmit` → 0 errors。
- 前端：`npm run lint` → 0 errors, 34 warnings（历史债务，无新增）。
- 前端：`npm run build` → 成功。
- 后端：`ruff check app/api/temporal_features.py app/services/temporal_feature_service.py app/services/structural_factor_service.py` → All checks passed。
- 后端：`mypy app/api/temporal_features.py app/services/temporal_feature_service.py app/services/structural_factor_service.py` → 新增/修改文件无错误（105 errors 均为历史债务，由 baselines 管控）。
- 文档：`python tools/check_docs_consistency.py` → 通过（MANIFEST baseline `f16adba3d440b65acef1961b9befdcbb4263850a`，本 PR 不修改基线，合并后另开 baseline PR）。
- 文档：`python tools/check_architecture.py` → 通过。
- 文档：`python tools/check_test_allowlist.py` → PASS。
- 文档：`python tools/update_docs.py --check` → 契约核对通过。
- API smoke test（feat 分支临时容器 + 真实 instrumentId `000c53ee-cdee-488a-9d85-333ef137b823` / symbol `603897`）：
  - `GET /temporal-features?as_of=latest` → 200 + 完整 DTO；
  - `GET /temporal-features?as_of=2025-01-01` → 400 + 错误信息；
  - 生产容器（main）→ 404（印证根因）。

## Git 分支

`feat/temporal-features-v1`

## Git Commit

- Base Commit：`7fb0a91`（PR #22 squash merge 后 main HEAD，MANIFEST baseline 更新为 `f16adba`）
- Head/Squash Merge Commit：commit 后填写

## 数据库迁移

无

## 配置变化

无

## 风险

- 低：仅新增只读 API + 前端 UI 开关，不修改任何现有 V1.8 字段计算逻辑。
- 复用 V1.8 `compute_structural_factors` 与 `_compute_all_factors_for_bars`，若 V1.8 因子计算失败则 temporal 对应字段也降级为 null（符合异常隔离设计）。
- 历史序列重算（SQZMOM/BB/volume_percentile）单股票 250-500 bars，性能 <100ms，不影响响应时间。
- 前端 `localStorage` 跨会话保持用户选择，强制隐藏参数优先级最高，避免截图误显示面板。

## 遗留问题

- 无已知 OPEN alignment。本次新增代码、测试、文档均一致。
- MANIFEST baseline 更新留待本 PR 合并后另开 docs PR 补齐（按 advice.md line 163 指示）。
