# Backend Module Map

> 目的：让新 agent 知道真实后端代码在哪里。本文是实现地图，不重复产品规则。

## 1. 应用入口

| 职责 | 文件 |
|---|---|
| FastAPI app | `backend/app/main.py` |
| Lifespan seed/recovery | `backend/app/main.py` |
| DB session | `backend/app/db.py` |
| API deps | `backend/app/core/deps.py` |
| Security/JWT | `backend/app/core/security.py` |
| Settings | `backend/app/config.py` / `backend/app/config.production.py` |

## 2. 模块映射

| 模块 | API | Service | Repository/Model | 测试/备注 |
|---|---|---|---|---|
| access/auth | `api/auth.py`, `api/me.py`, `api/plans.py` | `access_control_service.py`, `plan_service.py`, `subscription_service.py` | `models/user.py`, `models/subscription.py`, `models/plan.py` | 权限修改必须覆盖 active/expired/admin |
| market_data | `api/bars.py`, `api/market.py`, `api/calendar.py`, `api/instruments.py` | `market_data_aggregation_service.py`, `bars_coverage_service.py`, `calendar_seed.py`, `market_status_service.py` | bar/instrument/calendar models & repositories | 页面、指标、截图必须同源；覆盖率统一由 `BarsCoverageService` 计算；quote 实时性统一由 `market_status_service.compute_market_session` 判断，仅 `MORNING_SESSION`/`AFTERNOON_SESSION` 触发 pytdx；`market_data_aggregation_service.py` 负责 1d partial daily bar 合成（交易时段 `include_realtime=true` 时 `data_source=hybrid`/`is_partial=true`/`last_live_bar_time` 非空，收盘后/非交易时段 `is_partial=false`）；同时服务 `/bars` 实时展示与 `worker-monitor` 的 live 1m 输入，两条业务链路分离，但均要求 `start_time`/`end_time` 同为 `Asia/Shanghai` aware datetime；**市值字段（CHANGE-20260713-010）**：`bars.py._compute_market_cap_fields` 从 `Instrument.total_share`/`float_share` 与当前价格计算市值，数据缺失时 `market_cap_degraded_reason="market_cap_data_unavailable"`，禁止用户请求时访问第三方；**MDAS v2 SSOT 唯一出口（CHANGE-20260717-002）**：`MarketDataAggregationService.get_bars` 是后端唯一行情读取出口，业务/API/indicator/SMC/strategy_batch/feature_snapshot/structural_factor/temporal_feature/monitor/capture/chart_bars 全部经 MDAS；MDAS v2 请求参数含 `timeframe`/`adj`/`include_realtime`/`completed_only`/`start_date`/`end_date`/`limit`/`warmup_bars`/`adjustment_as_of`，返回 bars 及 `market_data_contract_version`/`source_bar_hash`/`adj_factor_hash`/`adjustment_as_of`/`completed_through`/source/degraded 诊断字段；缓存键含全部参数和版本；`_apply_adj_factor_core` 用 `pd.merge_asof` 向量化映射权威因子到 `_adj` 列；架构守护 AST 测试在 `backend/tests/test_market_data_ssot_architecture.py`（5 项），禁止业务层导入 repository 私有 `_query_*`/`_get_adj_factor_df`/`apply_adj_factor*`/旧 `bar_repository.get_bars` 或自行 resample 周/月；架构边界详见 `docs/current/01-system-architecture.md` §4.1 |
| adjustment_factor | - | `services/adjustment_factor_service.py`（`AdjustmentFactorService.get_factor_series`/`rebuild_factor_series`/`detect_company_action_change`） | `models/bars_daily.py`（`adj_factor` 列），`models/company_action.py` | **复权因子唯一服务（CHANGE-20260717-002）**：`get_factor_series(session, instrument_id, as_of=date)` 返回只含 `trade_date <= as_of` 的截断因子序列（point-in-time，禁止未来除权事件泄漏）；`rebuild_factor_series` 从最早受影响日期完整重建该股票日线 factor 序列并原子 upsert（禁止只更新最近 5 根）；公司行为集合或 fingerprint 变化时触发重建，成功后精确失效该股票 MDAS/indicator 缓存；失败不得用 `1.0` 伪装成功，必须返回 degraded 状态和原因；优先复用 `bars_daily.adj_factor` 作为当前权威因子序列，不创建新表；禁止信任 15m/60m/1m 行内旧 `adj_factor` 作为复权真源（pytdx hybrid bar 自带 `adj_factor=1.0` 错误），列可保留兼容但读取时由权威日线因子覆盖；公式 `qfq_price = raw_price × factor(bar_date) / factor(as_of)`；详见 `docs/changes/records/CHANGE-20260717-002.md` |
| kline_aggregator | - | `services/kline_aggregator.py`（`convert_kline_frequency`） | - | **周/月聚合唯一入口（CHANGE-20260717-002）**：周/月 K 线必须「日线完成复权后聚合」（`raw → daily qfq → aggregate to 1w/1mo`）；仅 MDAS 可导入 `kline_aggregator`，业务层禁止直接调用；MDAS 自身 resample 仅允许 1m→15min/60min 日内聚合；算法实现层（`strategy_assets/algorithms/`）在已获取 bars 上自行 resample 计算算法内部特征（如 SMC PDH/PDL）不属于行情出口聚合，不受此约束 |
| technical_indicators | `api/indicators.py` | `services/indicator_service.py`（`compute_all_indicators`，含 `include_smc: bool=False` 参数 + **CHANGE-20260716-001 required_inputs 优化**：`_REQUIRED_INPUTS` 映射 + `_determine_required_bars()` 按需加载 15min/minute，避免 1d 请求无条件读取 750 天日内数据 + **CHANGE-20260716-001 SMC source diagnostics**：`smc_source_bar_hash` 等字段）、`services/indicator_cache.py`（`ALGORITHM_VERSION="v11"`，cache key `indicator:{version}:{tf}:{adj}:{bars}:{symbol}`，`include_smc=true` 追加 `:smc` 后缀隔离；**CHANGE-20260717-001**：v9→v10，SMC warmup/execution gate/trailing NaN/OB 顺序逻辑变更，旧 v9 缓存强制失效；**CHANGE-20260718-004**：v10→v11，Node Cluster engine 唯一入口 + 三链统一 + `_SCHEMA_VERSION` 2→3，旧 v10 缓存强制失效）、`services/smc_view_adapter.py`（**CHANGE-20260716-001**：SMC view adapter，输出有界 DTO 并重基准索引；**CHANGE-20260717-001**：15m 5000 计算/4000 展示，adapter 裁剪展示窗口，Redis/API 不得返回完整计算历史 time 和全部 pivots） | `strategy_assets/algorithms/features/sqzmom_lb.py`（`compute_sqzmom_lb`）、`strategy_assets/algorithms/features/smc_pine_core.py`（`compute_smc_pine`，**唯一 Pine 语义核心**，生产+测试共用）、`strategy_assets/algorithms/features/smc_indicator.py`（薄包装委托层，`_SMCState = _SMCPineState` 别名） | **SMC 按需启用 + Pine parity（CHANGE-20260715-001 → CHANGE-20260715-002 → CHANGE-20260716-001 crossover 修正）**：`include_smc` 默认 `false`，`false` 时不计算 SMC 且响应不含 `data.smc`；`true` 时计算并返回 SMC，cache key 追加 `:smc` 后缀与默认路径隔离；SMC 只进入 `/stock/:symbol` 个股详情指标链，不进入 `/market`、DSA、Node、Capture、盘中监控、选股；**FVG 完全排除**（不计算、不返回、不缓存、不渲染）；**Pine 语义核心** `smc_pine_core.py` 实现 Pine 原语（`pine_rma` Wilder / `pine_atr` / `pine_cumulative_mean_range` bar0=NaN / `pine_highest/lowest` / `pine_crossover/crossunder`）；**CHANGE-20260716-001 crossover/crossunder level_curr/level_prev 快照**：`displayStructure` 接收独立快照，crossover=`close_curr > level_curr && close_prev <= level_prev`，NaN→False；`_SMCPineState` 状态机按 Pine 执行顺序；events 使用 `internal: bool` 替代旧 `kind`；anchor/second_pivot/confirmed 三时间点（second_pivot 为视觉线端点，confirmed 因果/回放使用）；`swing_bias` 直接返回 `state.swing_trend.bias`（{1,-1,0}）；**warmup 契约（CHANGE-20260717-001 warmup/历史分离）**：1d 使用 `full_daily_bars`（DB 全量日线 ≥500 warmup）；**15m 独立查询 `bars+_SMC_WARMUP_BARS`（5000 计算/4000 展示，adapter 裁剪展示窗口，pivot/BOS/CHoCH 在窗口左缘不丢失）**；1mo 扩展回看 7000 天确保 ≥200 根（ATR200 可初始化）；1h/1w 复用 `macd_bars`（可获得完整历史）；**CHANGE-20260717-001 execution gate/trailing NaN/OB 顺序**：`internal_gate = show_internals OR show_internal_order_blocks OR show_trend`（Pine L784），`swing_gate = show_structure OR show_swing_order_blocks OR show_high_low_swings`（Pine L787），不再只由 OB 开关门控；trailing NaN 严格复刻 `math.max(high, na)=na`，trailing.top/bottom 为 NaN 时不更新，只由 swing pivot 初始化；OB `store_order_block` 用 `insert(0,...)` newest-first 复刻 Pine `array.unshift`（L519），前端 `slice(0,5)` 取最新 5 个 active internal OB；**SMC 完整历史只在核心内计算，view adapter 输出有界 DTO**（前端 `smcToDisplay` 按时间过滤展示区外事件）；用户 Pine 代码（`ref/smc_user_source.pine`，SHA256 0bd3d2ad，843 行）为原创作品并授权盘迹使用（导出能力见 `ref/smc_user_export.pine`）；`bars` 参数最大 4000；indicator overlay alignment 回归见 `current/05-testing-acceptance.md` §3.5/§3.14/§3.21 |
| node_cluster | - | `services/node_cluster_engine.py`（`compute_node_cluster_profile` / `derive_state_for_price` / `detect_crossover_signals` / `NodeClusterProfileResult` 不可变结果数据类，**CHANGE-20260718-004 唯一业务入口**）、`contracts/indicator_semantics.py`（冻结语义合同：Node Cluster 输入/输出/字段含义/contract_fingerprint；**Section 2.1 语义真源**）、`services/unified_volume_profile.py`（底层 VP，**仅 engine 可导入**）、`services/luxalgo_volume_profile_pytdx_15m_aligned.py`（底层 15m VP，**仅 engine 可导入**） | - | **Node Cluster 唯一计算内核（CHANGE-20260718-004）**：三层架构 = `indicator_contract.py`（数值参数）+ `indicator_semantics.py`（语义合同）+ `node_cluster_engine.py`（计算内核，唯一业务入口）；业务模块（`structural_factor_service`/`feature_snapshot_service`/`monitor_batch_service`/`volume_node_monitor`/`indicator_service`）只导入 `node_cluster_engine`，**禁止直接导入** `compute_unified_volume_profile`/`luxalgo_volume_profile_pytdx_15m_aligned`；**三链统一**（盘后链 `_compute_cost_position_factors` / 详情链 `indicator_service` / 监控链 `volume_node_monitor` 均通过 `compute_node_cluster_profile(df_1d, df_15m, adjustment_as_of, adj_factor_hash)` 一次调用，profile_hash 一致）；**修复盘后链单周期 VP 缺陷**：`_compute_cost_position_factors` 旧实现只传单一周期 bars 给 `compute_unified_volume_profile`，新实现通过 engine 显式传入 `profile_df=bars_15m`；**不可变结果**：`NodeClusterProfileResult` 为 frozen dataclass，含 `profile_hash` + `_vp_result` 私有字段 + 鸭子类型适配器（`profile_df`/`peak_df` 委托 `_vp_result`）；**版本升级**：`indicator_cache.ALGORITHM_VERSION` v10→v11（旧缓存自动失效），`_SCHEMA_VERSION` 2→3（旧快照不可见）；**架构守护 AST 测试** `backend/tests/test_node_cluster_architecture.py`（5 项）扫描 `app/` 全部生产文件，禁止除 engine 外导入底层 VP；详见 `docs/current/08-indicator-calculation-contracts.md` + `docs/maps/indicator-computation-map.md` |
| instrument_share_capital_sync | - | `services/instrument_share_sync_service.py`（`InstrumentShareCapitalSyncService.sync_share_capitals`） | `models/instrument.py`（`total_share`/`float_share`/`share_as_of` 列，migration 063） | **股本数据每日同步（CHANGE-20260713-010）**：通过 `pytdx.get_finance_info` 同步 SH/SZ 标的 `total_share`/`float_share`，BJ 跳过，批次 500，写 `share_as_of=trade_date`；pytdx 同步调用通过 `asyncio.to_thread` 包装避免阻塞事件循环；`worker.py.scheduled_share_capital_sync` 每日 18:00 触发；市值计算只读 DB 字段不联网；价格与股本必须同一 `as_of`；股本缺失时 quote 端点返回 null market_cap 字段 |
| screening | `api/strategies.py`, `api/strategy_runs.py` | `strategy_batch_service.py`, strategy runtime | `strategy_*` models | 发布门禁关键模块；`strategy_batch_service.py` 新增 _classify_computable_universe 方法（_DSA_MIN_HISTORY_BARS=60），run 级总超时从 STRATEGY_RUN_TOTAL_TIMEOUT_SECONDS 环境变量读取（默认 7200），execute_run 保留预置 skipped_count；**`strategy_result_repository`（CHANGE-20260713-006）** 支持 industry/concept 筛选，通过 `_apply_run_item_filters` 调用共享 `board_filter_helper.build_board_filter_conditions(instrument_id_col=StrategyRunItem.instrument_id, ...)` 构造 EXISTS 子查询，`items`/`filtered_total` 两处同步应用（`source_total` 不受筛选影响，恒等于 `run.total_instruments`）；`universe_total` 由 `selector_query_service` 计算（universe=watchlist 时为自选股范围内 run_items 数，universe=all 时等于 source_total）；**latest_change_pct 契约（CHANGE-20260714-001）**：`strategy_result_repository` 新增 `CHANGE_PCT_METRIC_KEY = "change_pct"` 常量（sort 与 metric_filters 两处共用单点定义）；新增 `_build_latest_change_pct_subquery(run_id)` 构造 `bars_daily` 表 window function 子查询（`ROW_NUMBER() OVER (PARTITION BY instrument_id ORDER BY trade_date DESC)` 取最新两根完成日线，计算 `(close - prev_close) / prev_close * 100`）；新增 `_fetch_latest_change_pct_map(session, instrument_ids)` 批量获取指定 instrument_ids 的 `latest_change_pct`/`latest_change_trade_date` 映射（避免 N+1）；`RunItemResultRow` 新增 `latest_change_pct`（float\|null）和 `latest_change_trade_date`（date\|null）字段；`change_pct` 作为 `sort_by`/`metric_filters` 特殊 key 走 `bars_daily` 子查询，不在 manifest `filterable`/`sortable` 白名单中也允许；新增 `stock_name`/`stock_name_op` 参数支持（`contains`/`not_contains`/`eq` 匹配 `Instrument.name`，与 `keyword` 语义分离）；`items`/`filtered_total` 两处同步应用 `stock_name`/`stock_name_op` 筛选条件 |
| excel_export | `api/strategy_runs.py::POST /strategy-runs/{run_id}/results/export` | `services/excel_export_service.py`（`generate_xlsx`/`extract_row_data`/`_sanitize_formula_injection`/`MAX_EXPORT_ROWS=10000`） | `schemas/export.py`（`ExportRequest`/`ExportColumn`） | **Excel 导出（CHANGE-20260713-010）**：复用 `query_published_selector_results` 同一筛选/排序构造器，禁止复制查询逻辑；标准库 `zipfile`+`xml.etree` 生成真实 .xlsx（禁止 `openpyxl`/`xlsxwriter`）；公式注入防护 `=`/`+`/`-`/`@` 前缀单引号；百分比 `numFmt 0.00%`；`stock` 列 `payload_key=null` 不导出操作列；内存或 `SpooledTemporaryFile`，响应后释放；测试 `tests/test_excel_export_api.py`（21 用例）+ `tests/test_excel_export_service.py`（9 用例） |
| board_filter | - | - | `backend/app/repositories/board_filter_helper.py` | 共享 EXISTS 条件构造器；`build_board_filter_conditions(instrument_id_col, industry, concept) -> list[ColumnElement[bool]]` 供 `market_stocks_service`（`Instrument.id`）和 `strategy_result_repository`（`StrategyRunItem.instrument_id`）共用；industry+concept 同时提供时为 AND 语义（两个 EXISTS 条件 AND 连接，`MarketBoardMembership` JOIN `MarketBoard`，`type='industry'`/`'concept'`）；**PR #77 收口**：industry 改关键词 ilike 匹配（`MarketBoard.name.ilike('%keyword%', escape='\\')`，匹配完整路径任意一级），`_normalize_keyword` NFKC + trim，`_escape_ilike_pattern` 转义 `\`/`%`/`_`；concept 保持 `MarketBoard.name == concept` 精确匹配；空值不生成条件；market stocks/StrategyRunResults/行情/自选/Excel 复用同一 helper |
| board_sync | - | `services/board_sync_service.py`（`sync_boards` / `BoardSnapshot` 暂存 + 原子切换 + 完整性门禁 + **PR #77：source=wencai 显式返回**）、`services/wencai_board_provider.py`（`WencaiBoardProvider`，pywencai 唯一板块分类数据源，查询 `同花顺概念，行业分类`，`asyncio.to_thread` 包装，Referer 头，3 次重试）、`services/after_close_orchestrator.py`（`syncing_boards` 步骤，软失败不阻断 DSA/snapshot/publish）、`config.py`（**PR #77：`_resolve_board_sync_enabled()` 环境变量解析**） | `models/market_board.py`（`MarketBoard`/`MarketBoardMembership`，migration 062），Redis key `board_sync:status`（TTL 7 天） | **板块同步（CHANGE-20260716-007 + PR #77 收口）**：pywencai 为唯一数据源（删除 `qstock_fetcher.py`/`ths_adapter.py`/`test_qstock_fetcher.py`）；`syncing_boards` 作为 `after_close_orchestrator` 步骤执行（`refreshing_daily → syncing_boards → checking_coverage`），非交易日/`mode=dsa_only`/`BOARD_SYNC_ENABLED=false` 跳过；**BoardSnapshot 原子切换**：暂存集合 → 门禁校验 → 事务内 TRUNCATE+INSERT；**绝对门禁**（raw≥5000/唯一性≥99.9%/行业≥200/概念≥300/关系≥60000/解析率≥95%）+ **相对门禁**（降幅>20% 拒绝，首次不做）；**软失败**：失败保留上一成功版本，`/market/boards` 返回 `stale=true`；**Redis 状态**：`record_sync_status()`/`get_sync_status()` 写入 `board_sync:status`，记录 `last_attempt_status`/`last_success_at`/`source`；**PR #77**：`sync_boards()` 成功返回 dict 显式带 `source: "wencai"`；`config.py` `_resolve_board_sync_enabled()` 优先级「环境变量 > CONFIG_FILE > 默认 False」；`Dockerfile` 安装 Node.js（pywencai `get_token()` 需 `subprocess.run(['node', ...])`）；249 只未解析股票全 DB_NOT_EXIST（BJ 234 + SH 13 + SZ 2）；测试 `test_wencai_board_provider.py`（53）+ `test_board_sync.py`（17 重写，含 source=wencai）+ `test_after_close_board_sync.py`（10）+ `test_board_filter_helper.py`（23 PR #77）+ `test_board_sync_enabled_config.py`（12 PR #77）；详见 `current/03-jobs-integrations-operations.md` §2.6 |
| watchlist | `api/watchlist.py` | `feature_snapshot_service.compute_feature_snapshot_for_date` / `upsert_snapshot` / `compute_for_trade_date` / `build_summary_payload` / `_truncate_bars_to_trade_date` / `_resolve_expected_snapshot_trade_date`（async） | `user_watchlist_items`, `stock_feature_snapshots`, `monitor_evaluations`, `strategy_events` models | 到期权限和额度检查；`GET /watchlist/monitor-status` 响应 `metrics` 唯一来自 `stock_feature_snapshots.summary_payload`（`_source='feature_snapshot'`），不再走 `MonitorSnapshotService` 实时计算或 `MonitorState.payload` fallback；`MonitorEvaluation` 仅用于展示评估状态字段（evaluation_status/retry_count/error_code/source_bar_time）；新增 `calculation_status` 三态（SUCCEEDED/WAITING_SNAPSHOT/NO_SNAPSHOT）；`_resolve_expected_snapshot_trade_date` 复用 `calendar_service.get_previous_trading_day_async` / `get_most_recent_trading_day_async`，禁止硬编码周末 |
| monitoring | `api/monitor_states.py`, `api/strategy_events.py` | monitor scheduler/services, `eligible_user_service.py` (`filter_monitor_eligible_recipients`/`is_user_eligible_for_monitor`), `services/smc_monitor.py`（**CHANGE-20260720-001 §二 新增 SmcMonitor**：主输入已完成前复权日线，1m 仅触发检测；调用 `canonical_adapters.compute_smc_adapter`；FVG 完全排除；3 个事件 `smc_bos_retest`/`smc_choch_retest`/`smc_order_block_first_touch`；稳定 `smc_entity_id` + 去重维度含 `instrument/event_type/entity/touch_episode`），`services/watchlist_monitor_service.py`（**CHANGE-20260720-001 §二 三合一**：`WatchlistMonitor` 聚合 `self._bb` + `self._vn` + `self._smc` 三个子监控；单子监控失败只标记该项 `degraded`，不阻塞其他子监控） | monitor/evaluation/event models | 只处理 completed 1m Bar；监控资格：active admin 放行，active member + 有效 subscription 放行，disabled admin / 无订阅普通用户排除；`monitor_batch_service`/`event_recipient_service`/`outbox_relay`/`delivery_worker` 四处统一使用该口径；`monitor_batch_service` 1m 输入 `include_realtime=True` 并剔除最后一根未完成 bar，日线/15m 输入 `include_realtime=False`；**三监控日线主周期（CHANGE-20260720-001 §二）**：BB/VN/SMC 三类监控使用已完成前复权日线为主结构，15m 仅 Node 成交量分配辅助，1m 仅触发检测；`MonitorState` 升级命名空间 `bb`/`node_cluster`/`smc`/`market`，单子监控失败只标记该项 degraded 不阻塞 |
| feature_snapshot | - | `services/feature_snapshot_service.py`（含 `create_snapshot_run` / `finish_snapshot_run` run lifecycle）, `services/after_close_orchestrator.py` (步骤 3.5), `services/calendar_service.py` (`get_previous_trading_day_async` / `get_most_recent_trading_day_async`) | `models/stock_feature_snapshot.py`, `models/stock_feature_snapshot_run.py`, migration `056_stock_feature_snapshots` + `057_stock_feature_snapshot_runs` | 复用 `_compute_all_factors_for_bars` / `_compute_relation` / `_compute_daily_context` / `_compute_m15_response` / `_compute_derived_relation` / `bollinger()` 不复制公式；point-in-time 截断 `index.date <= trade_date`；upsert 幂等；单股失败写 `degraded_reasons` 不阻断批次；失败比例超 30% 抛 `RuntimeError`；盘后 orchestrator 在 `quality_gate` 与 `publishing` 之间执行；`compute_for_trade_date` 不内部 commit，由 caller 决定 commit/rollback；run lifecycle（Phase 8 新增）：`create_snapshot_run(scope=...)` 创建 `running` run（独立 session + commit），`finish_snapshot_run(metadata={'scope': ...})` 终态为 `succeeded`（写 `published_at`）/ `failed`（不写）；watchlist 通过 `_has_succeeded_snapshot_run` 严格判断 `status='succeeded' + published_at IS NOT NULL + metadata_['scope']='full'`；**[Blocker Fix] scope 必传**：after_close 固定 `scope='full'`；普通 backfill 不带过滤条件时 `scope='full'`；小样本 backfill 带 `--symbols`/`--limit-instruments` 时 `scope='sample'`（watchlist 不可读） |
| feature_snapshot_backfill | - | `scripts/feature_snapshot_backfill.py`（CLI 调用层，无业务计算） | - | 历史交易日批量回补；**instrument-first 架构（Phase 8 新增）**：每只股票每周期只调用一次 `load_instrument_bars`，内存中按 `trade_date` slice；核心计算复用 `feature_snapshot_service.compute_feature_snapshot_for_date`；支持 `--start/--end=latest/--batch-size/--resume/--dry-run/--failure-threshold/--symbols/--limit-instruments/--workers`；`--resume` 跳过已存在 snapshot 且所属日期有 `succeeded` run 的行（双重过滤）；run gate：每个 trade_date 创建 `succeeded`/`failed` run；**[Blocker Fix] scope 区分**：`_resolve_run_scope(symbols, limit_instruments)` 决定 scope（任一过滤启用 → `sample`，都未启用 → `full`），传播到 `create_snapshot_run(scope=...)` + `finish_snapshot_run(metadata={'scope': ...})`；`--dry-run` 不创建 run；与 `backend/scripts/backfill_all_periods.py` 同目录同调用方式；**multiprocessing（CHANGE-049 + Blocker Fix v2）**：`--workers N`（N>1）启用并行模式，新增 top-level 可 pickle worker 函数 `_worker_process_instruments(chunk, trade_dates, db_url, ...)`（独立 `async_engine` + `async_sessionmaker`，pool_size=1/max_overflow=0/pool_pre_ping=True），主进程通过 `backfill_instrument_first_parallel()` 编排（`ProcessPoolExecutor` + `asyncio.gather(return_exceptions=True)`，按 chunk 顺序映射 results，worker 抛 `BaseException` 时整个 chunk 计 failed）；worker 内 **per-date commit**（`upsert → db.commit() → success++`，异常 `rollback + failed++`，下一 date 继续用干净事务，commit 失败不计 success）；`parse_args()` 对 `--workers < 1` 调 `parser.error()` 抛 `SystemExit`，`--workers > os.cpu_count()` 时 `warnings.warn()` + 自动 cap |
| notifications | `api/notifications.py`, `api/stock_detail_feishu.py` | `outbox_relay.py`, `delivery_worker.py`, `stock_detail_feishu_service.py`, `channel_adapter.py`, `feishu_card_builder.py`, `message_builder.py` | notification/outbox/delivery models | 飞书、图文、重试；消息时间使用 `format_shanghai_datetime`；`delivery_worker.py` 对 `monitor_event`/`strategy_event`/`monitor_chart` 投递前用 `is_user_eligible_for_monitor` 复核 |
| coverage | - | `bars_coverage_service.py` | `bars_daily`, `instruments` | 统一 A 股覆盖率口径，返回 `coverage`（展示）与 `coverage_raw`（阈值判断），供 scheduler/orchestrator/overview 使用 |
| capture | `api/capture.py` | `stock_capture_service.py` | `capture_jobs` | Capture Token 隔离；**CHANGE-20260720-001 §三**：`CaptureJob` 新增 `indicator_view` 字段，缓存键含 `indicator_view`，输出文件名含 `indicator_view`，幂等键含 `indicator_view`；三套 `FEISHU_CAPTURE_PRESETS`（`node_cluster`/`bollinger`/`smc`）layers 互斥（除共享 candlestick） |
| stock_context | `api/stock_context.py` | `services/atomic_fact_contract_service.py`（`compute_atomic_facts` / `compute_recent_changes` 纯函数，新快照与旧 fallback 共用同一公式）、`services/feature_snapshot_service.build_summary_payload`（追加 `atomic_fact_contract_v1` 字段） | **双合同**：冻结研究合同 `contracts/atomic_fact_contract_v1.json`（V4.13 原字段，14 Core / 10 Aux / 1 Rejected，**不含 public_key/public_label**）+ 产品展示合同 `contracts/atomic_fact_presentation_v1.json`（按 Fact ID 映射 publicKey/publicLabel/visualKind/valuePrecision/groupTitle/secondaryLabel，14 Core + 8 Aux，排除 T3/T6/V1）；生产服务同时读取两份合同（frozen 决定事实/顺序/公式/阈值/路径，presentation 决定产品文案与 UI 类型）；`stock_feature_snapshots.summary_payload` 作为事实真源，Context API **persisted-first**（优先读已持久化 `summary_payload.atomic_fact_contract_v1`，缺失/版本不符/结构不匹配 → 同纯函数 fallback 不回写）；as_of point-in-time 过滤（SQL 层 `trade_date <= as_of` 再 `DESC LIMIT`），GET 零写入；底层 `structural_factor_service`/`temporal_feature_service`/`state_event_service` 保留（仅剥离普通用户 context 表达层）；用户 DTO `PublicAtomicFactItem` 不含 factId/sourcePath，缺失事实从数组省略（分母固定 14）；V1 累计成交量比永不进用户 payload；T3/T6 `ui_enabled=false`/`feature_flag=false` 默认不计算；契约详见 `current/07-atomic-fact-contract-v1.md` |
| jobs/admin | `api/admin_after_close.py`, admin APIs | scheduler recovery, job services, `after_close_pipeline_service.py`（聚合 pipeline 状态：8 步骤时间线 + watchlist_ready + data_freshness + events） | `scheduler_job_runs`, `worker_heartbeats`, `stock_feature_snapshot_runs`, `job_run_events` | 管理任务与可观察性 |
| beta/admin | `api/public_beta.py`, `api/admin_beta_applications.py` | `beta_application_service.py`, `beta_application_notifier.py` | beta application models | 管理员通知特殊路径 |
| research | - | `app/research/feature_causality_registry.py`（`FeatureSpec` + `FeatureCausalityRegistry` + `build_default_registry`，33 字段）, `app/research/feature_computer.py`（per-bar 计算模块，复用 ATR/BB/SQZMOM/swing/DSA SSOT）, `app/research/research_matrix_writer.py`（DB 写入 + 三道硬阈值 + monthly run 生命周期 + 批量 upsert）, `app/models/research_feature_matrix.py`（`ResearchFeatureMatrixRun` + `ResearchFeatureMatrixRow` ORM）, `scripts/research_feature_matrix_backfill.py`（CLI 入口） | `research_feature_matrix_runs`, `research_feature_matrix_rows` | 研究特征矩阵 DB 主存储；不接入 watchlist，不写 `stock_feature_snapshots`；4 命名空间 `causal`（16）/`confirmed_delay`（4）/`hindsight`（6）/`label`（7）= 33 字段；`hindsight.*`/`label.*` 禁止进入回测；DSA 双轨 `causal.dsa_confirmed_*` vs `hindsight.dsa_finalized_*`；Node Cluster 只 `hindsight.node_cluster_*`；三道硬阈值：磁盘 < 15GB / 单月 > 3GB / 失败率 > 5% 停止；按月分批 + `--resume` 幂等 upsert（`ON CONFLICT (instrument_id, trade_date) DO UPDATE`）；parquet 仅可选 debug 导出；详见 `current/06-research-feature-matrix.md` |

## 3. 高风险热点

| 文件 | 风险 | 处理原则 |
|---|---|---|
| `backend/app/worker.py` | 多 worker 类型集中，修改容易影响生产调度 | 先补测试和 maps，再小步拆 |
| `outbox_relay.py` + `delivery_worker.py` | 影响站内/飞书投递 | 任何修改必须覆盖 target_channel_id、admin、expired |
| `market_data_aggregation_service.py` | 影响页面、指标、截图、监控 | 不允许页面自建第二套行情语义；**CHANGE-20260717-002 SSOT 唯一出口**：业务层禁止直接调用 repository 私有 `_query_*`/`_get_adj_factor_df`/`apply_adj_factor*`/旧 `bar_repository.get_bars`，禁止自行周/月聚合或二次复权；架构守护 AST 测试 `backend/tests/test_market_data_ssot_architecture.py`（5 项）在 CI 中扫描 `app/` 全部生产文件 |
| `adjustment_factor_service.py` | 影响所有 qfq 价格正确性 | **CHANGE-20260717-002**：禁止信任 15m/60m/1m 行内 `adj_factor` 列；rebuild 必须完整重建（禁止只更新最近 5 根）；失败不得用 1.0 伪装；as_of 必须截断因子序列禁止未来除权事件泄漏 |
| `node_cluster_engine.py` + `indicator_semantics.py` | 影响 Node Cluster 三链（盘后 / 详情 / 监控）一致性与所有结构状态因子 | **CHANGE-20260718-004**：业务模块禁止直接导入底层 `compute_unified_volume_profile`/`luxalgo_volume_profile_pytdx_15m_aligned`，只允许导入 `node_cluster_engine`；`NodeClusterProfileResult` 不可变（frozen dataclass）；修改语义合同必须同时升级 `ALGORITHM_VERSION`（v11）+ `_SCHEMA_VERSION`（3）使旧缓存/快照失效；架构守护 AST 测试 `backend/tests/test_node_cluster_architecture.py` 在 CI 中扫描 `app/` 全部生产文件 |
| `strategy_batch_service.py` | 影响发布批次 | 不得放宽完整性门禁 |

## 4. AI 修改规则

1. API 不复制 Service 规则；
2. Repository 不判断产品权限；
3. Strategy Runtime 不读取用户资格；
4. Delivery 不重算事件；
5. Monitoring 只生成事件，不直接决定飞书格式；
6. Capture 使用专用 token，不读普通登录态。
