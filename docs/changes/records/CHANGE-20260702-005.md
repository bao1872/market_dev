# CHANGE-20260702-005：Phase 6 文档对齐与旧术语清理

## 1. 基本信息

- 日期：2026-07-02
- 状态：committed
- 需求提出人：项目负责人
- 需求出处：用户于 2026-07-02 明确要求，在分支 `fix/release-feishu-marketdata-dsa` 上执行 Phase 6：全局扫描旧术语、更新当前设计文档、关闭已修复 Alignment、运行静态检查并推送。
- Git 分支：`fix/release-feishu-marketdata-dsa`
- Base Code Commit：`a331a406ddf2e7b7837a43788f4372436425c6d1`
- Head/Merge Commit：`e36f2c6a0cff77b355906a5f80bfdd0634761f77`

## 2. 修改前理解

- 当前产品行为：Phase 2-5 已实现权限/订阅资格、飞书图片/Capture Token、统一行情聚合、DSA 发布门禁，但当前设计文档与代码注释中仍残留旧术语（plan_contract、Membership、/me/membership 等）或缺乏关闭证据。
- 当前前端流程：登录/注册/续期页面与设置页保留部分“会员”文案；API 端点 `/me/membership` 及字段 `membership_*` 为 V1.6 遗留命名，语义实际等价于 Subscription。
- 当前后端流程：`/me/membership` 返回 Subscription 状态；`plans` 表替代旧 `plan_contract.py` 成为套餐唯一事实源；`market_data_aggregation_service.py` 为行情唯一事实源；DSA 通过 Run Quality Gate 自动发布。
- 当前数据/API/Worker/第三方关系：Subscription 替代 memberships；Feishu message group 区分 card/image/overall 状态；Capture/Outbox/Delivery 为独立 Worker；统一行情聚合服务为消费者提供一致快照。
- 已阅读的当前设计文档：
  - `docs/README.md`
  - `docs/current/00-project-overview.md`
  - `docs/current/01-product-requirements.md`
  - `docs/current/03-business-rules.md`
  - `docs/current/10-permissions-security.md`
  - `docs/current/11-jobs-integrations.md`
  - `docs/current/12-strategy-indicator-contracts.md`
  - `docs/current/18-code-doc-alignment.md`
- 发现的代码—文档—测试—生产冲突：
  - `backend/app/main.py`、auth.py、me.py 等模块的注释仍使用“会员”指代 Subscription；
  - `backend/app/schemas/user.py` 中 `next_route` 注释仍写 `/membership-expired`（实际代码返回 `/subscription-expired`）；
  - `tools/update_docs.py` 生成的 `docs/数据结构.md` 仍链接到不存在的 `策略与指标口径.md` 及旧锚点。

## 3. 修改目标

- 当前问题：旧术语与文档口径不一致， ALIGN-002 至 ALIGN-013 等多项 Alignment 需要关闭证据。
- 修改后行为：
  - 当前设计文档明确 Subscription 资格链、到期冻结动作、DSA 职责分离与发布门禁、飞书 message group 状态、行情聚合唯一事实源；
  - 代码注释统一用“订阅”描述技术实体，并明确标注 V1.6 遗留 API 路径/字段名；
  - ALIGN-002 至 ALIGN-013（除保留 KNOWN_GAP 项）写入关闭证据。
- 验收标准：
  - `python tools/update_docs.py` 通过；
  - `python tools/update_docs.py --check` 通过；
  - `python tools/check_architecture.py` 通过；
  - `python tools/check_docs_consistency.py` 通过；
  - `python tools/check_test_allowlist.py` 通过；
  - 全局扫描命中清单记录在 CHANGE 中；
  - 新增 CHANGE-20260702-005 并更新 CHANGELOG。
- 明确不修改：
  - 不修改 DSA/Node Cluster/MACD/布林带公式参数；
  - 不删除历史 Alembic 迁移；
  - 不改写 API 路径/字段名（仅标注遗留命名），避免破坏现有测试与前端契约；
  - 不修改生产数据或配置。

## 4. 影响范围

- 产品需求：无用户可见功能变化，仅文案与文档口径对齐。
- 业务规则和流程：`docs/current/03-business-rules.md` 细化 BR-SUB-001/002，明确允许/禁止动作。
- 前端：仅更新 `frontend/src/api/endpoints.ts` 注释，明确遗留字段命名。
- 后端：更新 `backend/app/main.py`、auth.py、me.py、admin_subscription.py、subscription_service.py、schemas/subscription.py、schemas/user.py 注释，无逻辑变更。
- 数据模型和迁移：无。
- API：无接口契约变化。
- 权限：无权限逻辑变化。
- Worker/第三方：无代码变化，文档补充消费者列表与 Feishu 状态字段。
- 参数和配置：无。
- 部署：无变化。
- 测试：无测试代码变化。

## 5. 实际修改

- 代码文件：
  - `backend/app/main.py`：更新 `/me/membership` 描述，标注为 V1.6 遗留路径；
  - `backend/app/api/auth.py`：模块/端点/函数 docstring 中“会员”改为“订阅”，标注遗留路径/字段；
  - `backend/app/api/me.py`：注释中“会员”改为“订阅”；
  - `backend/app/api/admin_subscription.py`：列表查询 docstring 改为订阅账户列表；
  - `backend/app/services/subscription_service.py`：常量注释改为订阅默认天数；
  - `backend/app/schemas/subscription.py`：类 docstring 标注遗留命名；
  - `backend/app/schemas/user.py`：修正 `next_route` 注释为 `/subscription-expired`，字段描述标注遗留命名；
  - `frontend/src/api/endpoints.ts`：多处接口/函数注释标注 `membership_*` 为 V1.6 遗留命名；
  - `tools/update_docs.py`：修正 `docs/数据结构.md` 中对策略指标文档的链接，指向 `current/12-strategy-indicator-contracts.md#4-行情和图表契约`。
- 数据库迁移：无。
- 配置变化：无。
- 兼容性和回滚：仅注释与文档变更，可安全回滚。

## 6. 文档更新

- `docs/current/03-business-rules.md`：
  - BR-SUB-001 增加资格链；
  - BR-SUB-002 明确到期/无订阅会员的允许动作与禁止动作清单。
- `docs/current/10-permissions-security.md`：
  - 增加 Subscription 资格链 JWT → AccessContext → User → Role → Subscription → Feature → Quota → Ownership；
  - 细化 Capture Token 规则（独立 storage key、短期、最小权限、不覆盖 Access Token）。
- `docs/current/11-jobs-integrations.md`：
  - 明确统一行情聚合服务为消费者列表；
  - 增加 Feishu message group 状态字段表与 `partial_failed`/image-only retry 规则；
  - 截图章节改为“截图与投递健康”，强调服务不可用时不得伪造成功。
- `docs/current/12-strategy-indicator-contracts.md`：
  - 新增 2.1 职责分离（Universe Builder / Feature Computation / Run Quality Gate / Selector Query）；
  - 2.3 计算原则明确所有 computable 股票必须写入 StrategyResult，单股超时/异常/DB 失败/预算失败计 failed；
  - 2.5 发布完整性细化自动发布条件，增加前端显示字段表（含 source_total/filtered_total）；
  - 2.6 资源预算增加 2026-07-02 350 只股票基准测试结果。
- `docs/current/18-code-doc-alignment.md`：
  - 增加“关闭证据”列；
  - ALIGN-002 至 ALIGN-013 按 Phase 6 要求关闭或保留 KNOWN_GAP；
  - ALIGN-001/010/012/014/015 保留 KNOWN_GAP 并注明原因。
- 生成文档：`docs/数据结构.md` 通过 `tools/update_docs.py` 重新生成。

## 7. 测试与证据

- 执行命令与结果：
  - `python tools/update_docs.py`：通过，生成 `docs/数据结构.md`/`docs/操作手册.md`/`docs/指标参数基线.md`；
  - `python tools/update_docs.py --check`：通过；
  - `python tools/check_architecture.py`：0 violations，10 checks passed；
  - `python tools/check_docs_consistency.py`：31 个文档文件全部 PASS；
  - `python tools/check_test_allowlist.py`：PASS，0 个 xfail。
- 关联测试证据（历史）：
  - Phase 2 权限测试 144 passed；
  - Phase 3 Feishu 测试 11 passed；
  - Phase 4 行情测试 49 passed；
  - Phase 5 `tests/test_strategy_batch.py` 7 passed；
  - `backend/reports/dsa_benchmark_20260702.md` 350 只股票基准测试报告。
- 全局扫描命中清单（排除 docs/archive/、backend/alembic/versions/、node_modules、.git、__pycache__）：
  - `plan_contract` / `PLAN_CONTRACTS`：47 行，主要集中在历史迁移（044/045/048）、迁移测试与说明替换计划的注释中；当前代码注释已明确为“替代旧 plan_contract.py”；
  - `membership`：198 行，主要为前端 UI 组件/CSS 类名、后端遗留 API 路径/字段/测试，以及历史迁移；当前运行时实体为 Subscription，遗留命名已标注；
  - `127.0.0.1:5432`：7 行，均为示例配置、测试回退 URL 或工具命令示例；
  - 裸 `postgresql://`：15 行，为工具驱动转换、测试回退 URL 或注释示例；
  - `注册即全功能` / `没有功能额度限制` / `无需选择套餐`：0 行；
  - `update_docs.py 尚未实现`：0 行；
  - `/me/membership`：9 行，为 V1.6 遗留 API 路径，已标注语义等价于 Subscription；
  - `strategy_author`：27 行，集中在 docs、check_architecture.py 与历史迁移 051/052，运行时无引用；
  - `Role(name="user")`：0 行。

## 8. 完成检查

- [x] 当前分支与记录一致，未直接修改 main
- [x] 代码注释符合修改后设计
- [x] 前端与 API 一致（遗留命名已标注）
- [x] 后端与数据模型一致
- [x] 权限、参数、Worker 和部署一致
- [x] 测试验证当前设计（静态检查全绿）
- [x] 当前设计文档已更新
- [x] CHANGELOG 已更新
- [x] Alignment 已更新或关闭
- [ ] Head/Merge Commit 待本记录提交后补全
- [ ] PR 回答所有设计与测试问题（留待 Phase 8）

## 9. 遗留事项

- ALIGN-001/010/012/014/015 保留 KNOWN_GAP，待 Phase 7-9 完成 release 候选、生产 E2E、CI 与部署验证后关闭。
