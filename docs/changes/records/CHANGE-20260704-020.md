# CHANGE-20260704-020

## 变更编号

CHANGE-20260704-020

## 任务名称

关闭 ALIGN-023：worker-watchdog 生产验证 stale running 清零

## 需求出处

用户于 2026-07-04 要求：合并 PR #7（worker-watchdog 生产服务）→ 部署最新 main → 验证 watchdog 启动并清理 stale running → 成功后创建关闭 ALIGN-023 的 Ready PR。

## 修改前行为

- ALIGN-023 在 `docs/current/code-doc-alignment.md` KNOWN_GAP 表中，描述为"待部署后验证 stale running 清零再关闭"；
- `docs/current/03-jobs-integrations-operations.md` 中 ALIGN-023 段落为"待部署后验证"；
- 生产 `worker_heartbeats` 存在 38 条 stale running 僵尸记录；
- CHANGE-019 的 Head Commit 为"待合并后填写"，状态为 committed。

## 修改后行为

- ALIGN-023 从 KNOWN_GAP 表移到 CLOSED 摘要，摘要为"worker-watchdog 生产服务已部署(67105c2)，38 条 stale running 已自动清理为 stopped，stale running=0"；
- `docs/current/03-jobs-integrations-operations.md` 中 ALIGN-023 段落更新为"生产部署 67105c2 后验证：watchdog 启动即标记 38 条僵尸心跳为 stopped，stale running 已清零（ALIGN-023 已关闭）"；
- CHANGE-019 Head Commit 补全为 `67105c2`，状态改为 merged；
- 新增 CHANGE-020 索引行和本记录。

## 影响模块

- `docs/current/` 文档闭环（code-doc-alignment、03-jobs-integrations-operations）
- `docs/changes/` 变更记录闭环（CHANGE-019 补全、CHANGE-020 新增）
- 不影响任何生产业务代码、compose、schema、数据库

## 修改文件

- `docs/current/code-doc-alignment.md`（ALIGN-023 从 KNOWN_GAP 移到 CLOSED）
- `docs/current/03-jobs-integrations-operations.md`（ALIGN-023 段落更新为已验证）
- `docs/changes/CHANGELOG.md`（CHANGE-019 补全 + CHANGE-020 新增）
- `docs/changes/records/CHANGE-20260704-020.md`（本记录）

## 文档更新

- `docs/current/code-doc-alignment.md`：ALIGN-023 从 KNOWN_GAP 移到 CLOSED 摘要
- `docs/current/03-jobs-integrations-operations.md`：ALIGN-023 段落更新为生产已验证

## 测试证据

### 生产验证证据（非 Mock）

1. **PR #7 合并**：squash merge，main HEAD = `67105c2`
2. **部署**：`docker compose --env-file /etc/market-dev/market.env -f docker-compose.prod.yml build backend frontend worker-capture` + `up -d`，14 服务全部 Up
3. **worker-watchdog 容器启动**：`trading-worker-watchdog` Up，镜像 `market-dev-backend:67105c2`
4. **watchdog 日志**：
   ```
   2026-07-04 06:15:58,365 [worker] INFO: Worker 启动: type=watchdog, interval=5s
   2026-07-04 06:15:58,366 [worker] INFO: [Recovery] 看门狗启动（间隔=60s）
   2026-07-04 06:15:58,769 [worker] INFO: [WorkerHeartbeat] 标记 38 个僵尸心跳为 stopped（阈值=600s）
   2026-07-04 06:15:58,772 [worker] INFO: [Recovery] 看门狗清理: 38 个僵尸心跳
   ```
5. **部署前 heartbeat**：46 running, 0 stopped
6. **部署后 heartbeat**：17 running（新部署 fresh 心跳）, 38 stopped（watchdog 自动清理）
7. **stale running 查询**：`SELECT ... WHERE status='running' AND heartbeat_at < NOW() - INTERVAL '600 seconds'` 返回 0 行
8. **任务链路正常**：scheduler_job_runs 全部 succeeded，outbox 全部 processed，message_deliveries 全部 success，capture_jobs 无新增 failed

### 治理检查

- `python tools/check_docs_consistency.py`：通过
- `python tools/check_architecture.py`：0 violations, 11 passed
- `python tools/check_test_allowlist.py`：通过
- `python tools/update_docs.py --check`：通过

## Git 分支

`chore/close-align-023-worker-watchdog`

## Git Commit

待提交后填写。

## 数据库迁移

无。不新增 migration，不修改历史 migration。不手动改数据库。

## 配置变化

无。不改 docker-compose / 环境变量 / 部署脚本。

## 风险

- 无。纯文档变更，关闭已有生产证据的 KNOWN_GAP。

## 遗留问题

- ALIGN-010/012/015/021：不在本轮处理范围
- GitHub PAT 已在聊天记录中暴露，用户需手动 revoke（旧 ghp_kzYR...m0f + 新 ghp_2cnu...3aT）
