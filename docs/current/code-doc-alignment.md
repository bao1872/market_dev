# Code / Docs / Production Alignment

> 本文件只记录“当前确认设计已经明确，但实现、测试、部署或生产表现尚未一致”的问题。历史经过进入 `changes/`。

## 当前 KNOWN_GAP

| ID | 领域 | 当前证据 | 目标 | 优先级 |
|---|---|---|---|---|
| ALIGN-010 | 飞书图文 E2E | 生产已有图文成功记录，但 partial_failed、仅重试图片、失败状态生产 E2E 尚未系统验证 | 独立 card/image 状态、partial_failed、仅重试图片、真实 E2E | P1 |
| ALIGN-012 | 管理页面 E2E | AdminJobsPage 与部分管理 API 已存在，Worker 心跳可观察性已补齐（`GET /admin/worker-heartbeats` + 前端 Tab + 测试）；用户启停、订阅变更、任务与审计生产操作未完整验收 | 所有管理按钮真实 API、审计完整、生产 E2E 通过 | P1 |
| ALIGN-015 | 服务健康与业务能力 | CORE_ONLY 不包含 capture/outbox/delivery；服务不全会造成业务部分可用 | 部署能力与业务功能匹配；服务不可用时不假成功 | P1 |
| ALIGN-021 | Ruff/Mypy 历史债务 | 全仓 Ruff/Mypy Full Report 仍有历史债务（baseline 930/242 → 当前 861/227），非阻断展示。2026-07-04 已完成 P0/P1/P2 分级审计（见 `docs/changes/records/CHANGE-20260704-029.md`，原审计文档 CHANGE-20260718-002 删除）：P0=0，P1=6 项（after_close_orchestrator/worker None 处理、bars_metrics 私有属性、chart_bars 死代码、strategy_run import、metrics 版本兼容），P2=849/189 项保留本条 | 独立债务分支清零，再改为完全阻断 | P2 |
| ALIGN-025 | `_notify_monitor_status` 绕过 Outbox | `worker.py:1087-1191` 直接调用 `adapter.send()` 绕过 Outbox/Delivery Worker，缺少重试/幂等/静默时段规避/可查询状态；代码 TODO 已标记，待产品决策（降级路径 vs 一致性） | 待产品决策后确定目标状态 | P2 |
| ALIGN-030 | 部分标的历史 bars 覆盖不足 | 生产 DB 中 `000001` 仅 5 根日线、66 根 15m（约 6 个交易日），导致个股详情 K 线显示最近几天；`600519`/`300750` 日线 846 根、15m 约 8000 根数据充足。2026-07-04 已对 `000100`（TCL科技）执行单标回补：日线 846 根、15m 8000 根、60m 2000 根，API `page_size=4000` 可正常返回 4000 根。全市场仍有约 81 只 active 标的日线 < 50 根，需后续统一回补决策。2026-07-04 全市场约 187 只 active 标的日线 < 60 根（BSE_920 97 只、主板 85 只、其他 5 只），已通过 _DSA_MIN_HISTORY_BARS=60 前置分类为 skipped/insufficient_history | 完成全市场历史行情回补，或明确排除/标记该类标的；所有页面显示标的需满足 Node Cluster 最小输入 | P1 |
| ALIGN-031 | DSA-only 大量 failed | 1881 只 failed 全部 reason_code=timeout，根因为 run 级总超时 600s 与编排层 7200s 冲突；historical bars 不足标的未前置分类；execute_run 覆盖 skipped_count | 修复后需生产验证 failed_count 大幅下降 | P1 |
| ALIGN-033 | `strategy_run_items.result_id` 未回填 | PR #14 batch service 写入 `strategy_results` 后未更新 `strategy_run_items.result_id`（始终为 None）。PR #15 的 `query_run_items_with_results` 已改用 `(run_id, instrument_id)` 关联 `strategy_results` 绕过此问题。但 `result_id` 字段仍为 None，需后续修复 batch service 在写入 results 后回填 `result_id` | 修复 `strategy_batch_service._write_results_to_db` 写入 results 后回填 `result_id` | P2 |
| ALIGN-034 | admin monitor 资格修复待生产验证 | 代码已实现 `filter_monitor_eligible_recipients`/`is_user_eligible_for_monitor`，monitor_batch/event_recipient/outbox_relay 三处已切到监控资格过滤；测试覆盖 active admin、active member+subscription、disabled admin、无订阅普通用户。生产环境尚未重新 build/restart 验证真实监控 universe 与投递链路。 | 部署后检查 monitor_batch universe 包含 admin 自选股，monitor 日志无 admin 被过滤，outbox/delivery 为 admin 生成 MessageDelivery | P1 |
| ALIGN-035 | quote 可信化与 pytdx 连接保护待生产验证 | 代码已实现 QuoteResponse 可信字段、午休统一口径、Redis 短缓存、pytdx 单例+线程锁；测试与本地 ASGI 验证通过。生产环境尚未验证真实 pytdx 连接在交易时段的成功/fallback 行为、断线重连、以及容器日志的可观测性。 | 部署后在交易时段 curl /quote，确认 pytdx 成功/降级字段正确，日志可见区分日志，页面状态徽章非固定“实时行情” | P1 |
| ALIGN-036 | delivery_worker monitor 资格修复待生产验证 | 代码已实现 `MONITOR_SOURCE_TYPES`（`app/constants/monitor_source_types.py` 单点真源）与 `is_user_eligible_for_monitor` 在 `delivery_worker.py` 投递前复核；`outbox_relay.py` 与 `delivery_worker.py` 共享同一 source_type 集合；测试覆盖 active admin/active member/disabled admin/plain user。生产环境尚未验证真实 monitor_event 能生成 MessageDelivery 并实际投递。 | 部署后检查 monitor_event 来源的 MessageDelivery 为 active admin 与 active member 生成，disabled admin/plain user 被标记 dead/USER_INELIGIBLE | P1 |
| ALIGN-037 | 1d partial daily bar 与 live 1m monitor 待生产验证 | 代码已实现 `MarketDataAggregationService` 交易时段合成 partial daily bar、`monitor_batch_service` 使用 live 1m 输入；测试覆盖交易/非交易场景。生产诊断已确认 2026-07-07 盘中 `MarketDataAggregationService` 构造的 `live_start` 为 naive datetime、`live_end` 为 aware Asia/Shanghai datetime，传入 `pytdx_adapter.get_minute_bars` 后触发 `can't subtract offset-naive and offset-aware datetimes`，导致 worker-monitor 全天无法获得 1m 数据、无 monitor_evaluations/strategy_events/通知。PR #35 修复后部署验证，发现 `pytdx_adapter.get_minute_bars` 内部将拉取到的 1m 数据 `datetime` 列显式 `tz_localize(None)`，但 aware 输入未同步转为 naive，导致比较时抛出 `Invalid comparison between dtype=datetime64[us] 和 Timestamp`。新增 `fix/pytdx-adapter-aware-minute-comparison` 修复并重新部署验证。 | 部署后交易时段 curl `/instruments/{id}/bars?timeframe=1d`，确认最后一根为当日 partial bar；查看 monitor worker 日志包含 `minute_last_bar_time` 与 `minute_is_partial`，且无 offset-naive/offset-aware 或 datetime64/Timestamp 比较异常 | P1 |
| ALIGN-040 | 统一行情工作区代码未部署/生产未验收 | 当前修复分支 `fix/pr74-context-layers-remove-consensus` 已重构 `/market`：移除 `MarketInstrumentPane`/`StockResearchWorkspace`/`ResearchContextPanel`/`MarketStockTable`，改为 `MarketToolbar`（仅 scope 分段按钮）+ `StrategyDataTable`（复用 `getTrendSelectionColumns` DSA 列定义，数据来自 `usePublishedRuns` + `useStrategyRunResults`）+ `AtomicFactsPanel`（可收起，首次默认收起）；`/market` 明确禁止挂载 K 线。`/market` URL 契约简化为 `scope/selected`（MarketWorkspacePage 管理）+ `sort/dir/keyword/filters/page/page_size`（StrategyDataTable 内置 screenerUrlState 管理）。`/stock/:symbol` 仍复用 `StockResearchWorkspace`（K 线研究区）。`/admin/stock-debug` 独立管理员调试路由保留。列设置与配置 CRUD 支持 `columnOrder`（localStorage + preset config）。P0 列对齐：`reorderVisibleColumns` 纯函数提取到 `columnOrdering.ts`，`columnAlignment.test.ts` 31 项测试全通过。前端 tsc/eslint/contract 220/build 全通过。待部署验证。 | 部署后：1) 生产 `/market` 验证 DSA 列表显示真实字段 + 列对齐 + 右栏默认收起；2) `/admin/stock-debug/:symbol` 验证管理员可见原始因子/JSON、普通用户 403；3) `/stock/:symbol` 复用共享图表区 + 单一工具栏（无 tv-strategy-legend）+ MACD 默认关闭；4) returnTo 外部 URL 被拒绝；5) preset CRUD 跨 session 可读取 | P1 |
| ALIGN-042 | ScreenerPage/MessagesPage 导航 URL 仍用旧契约 | `detailNavigation.ts` 的 `buildMarketEntryFromScreener` 仍生成 `/market?scope=market&symbol=...&source=selection&strategy=dsa_selector&returnTo=...`（旧契约），`buildMarketEntryFromMessage` 仍生成 `/market?symbol=...&event_id=...`。但 `/market` 的 `MarketWorkspacePage` 新契约只读取 `scope/selected`（MarketWorkspacePage 管理）+ `sort/dir/keyword/filters/page/page_size`（StrategyDataTable 管理），不读取 `symbol/source/strategy/event_id`。`marketWorkspaceUrlState.ts` 已简化为仅 `scope/selected`，`eventId`/`event_id` 已清除。文档已更新为目标契约（ScreenerPage → `/stock/:symbol`，MessagesPage → `/stock/:symbol?event_id=...`），但 `detailNavigation.ts` 代码尚未对齐。 | 修复 `buildMarketEntryFromScreener`/`buildMarketEntryFromMessage` 导航到 `/stock/:symbol` | P2 |
| ALIGN-043 | /market 列表自选操作与行内导航待生产验证 | CHANGE-20260713-005 已实现：`action` 列改名"自选"（onToggleWatchlist 模式 + 加入/移除自选按钮 + pending Set），股票名称改为 `<a>` 链接进入 `/stock/:symbol?returnTo=`（stopPropagation+preventDefault），股票单元格不再显示行内涨跌幅，watchlist 单次请求+Set 禁止 N+1。前端测试 columns.test.ts 13 项全通过。待部署生产验证：1) 普通用户点击股票名称跳转 /stock/:symbol 且 returnTo 正确；2) 加入/移除自选按钮立即更新且无 N+1；3) watchlist scope 移除自选后行消失；4) 批次信息普通用户不可见、admin 可展开。 | 部署后生产 E2E 验证自选操作 + 行内导航 + 批次信息权限 | P1 |
| ALIGN-044 | /market 单一搜索 SSOT 待生产验证 | CHANGE-20260713-005 已实现：MarketToolbar 顶部唯一搜索框（Enter/blur/清空提交），StrategyDataTable searchable={false} + externalKeyword/onKeywordChange 受控模式，单一 keyword 状态进入 URL。marketToolbarSearch.test.ts 8 项全通过。待部署生产验证：1) /market 只有一个搜索框；2) Enter/失焦/清空均触发搜索；3) preset 应用/重置同步顶栏 keyword；4) 无请求循环。 | 部署后生产 E2E 验证单一搜索行为 | P1 |
| ALIGN-045 | 后端 keyword pinyin_initials 匹配待生产验证 | CHANGE-20260713-005 已实现：`strategy_result_repository.query_results` 3 处 or_ 分支同步匹配 `Instrument.symbol`/`Instrument.name`/`Instrument.pinyin_initials`。test_strategy_results_keyword.py 3 项全通过（代码/名称/拼音首字母）。待部署生产验证：1) /market 搜索"600519"/"茅台"/"gzmt" 均能匹配贵州茅台；2) items 与 total 条件一致。 | 部署后生产验证 keyword 三字段匹配 | P1 |
| ALIGN-046 | 消息数量 SSOT 待生产验证 | CHANGE-20260713-005 已实现：MessagesPage 使用 useUnreadCount 作为未读 SSOT，"全部"显示后端 total（非 items.length），页头"共 X 条 · 未读 Y 条"，AccountMenu unread>0 进入 /messages?filter=unread + badge。messagesCounts.test.ts 8 项全通过。待部署生产验证：1) 未读数与 AccountMenu 共用 /messages/unread-count；2) 全部数量来自独立后端 total；3) 标记已读后同步归零。 | 部署后生产 E2E 验证消息数量一致性 | P1 |
| ALIGN-047 | 消息跳转目标待生产验证 | CHANGE-20260713-005 已实现：单只股票消息 → `/stock/:symbol?event_id=...&returnTo=/messages`，selection_composite → `/market`。待部署生产验证跳转目标正确。 | 部署后生产 E2E 验证消息跳转 | P1 |
| ALIGN-048 | K 线 Pointer Events 拖拽待生产验证 | CHANGE-20260713-005 已实现：StrategyChart 使用 Pointer Events（setPointerCapture/releasePointerCapture），dragRef {startClientX,startViewport,pointerId}，4px 阈值抑制 click，grab/grabbing cursor。chartDrag.test.ts 7 项源码契约全通过。待部署生产验证：1) 拖动后起止 bar 时间改变；2) 可拖回最右端；3) 节点/事件点击不被误触；4) 鼠标移出 canvas 后仍可继续拖动。 | 部署后生产 E2E 验证 K 线拖拽行为（1440/1920） | P1 |
| ALIGN-049 | 用户文案契约待生产验证 | CHANGE-20260713-005 已实现：CHART_LAYER_MANIFEST sqzmom→"挤压动量"、node→"筹码共识价"；StrategyChart 节点标签 POC 峰→"核心共识价"、峰→"共识价"，POC 中心线显示"核心共识价"，tooltip POC→"核心共识价"/PEAK→"共识价"，缺失提示"筹码共识价暂不可用"。内部字段名 n.poc/profile.pocPrice/row.is_poc/is_peak 不变。chartLabels.test.ts 5 项 + indicatorManifest.test.ts 12 项全通过。待部署生产验证文案显示且内部 key 不变。 | 部署后生产 E2E 验证用户文案 | P1 |
| ALIGN-050 | /market 行业/概念筛选待生产验证 | CHANGE-20260713-006 已实现 + CHANGE-20260716-007 PR #77 收口 + PR #77 收口第二轮：新建 `board_filter_helper.py` 共享 EXISTS 条件构造器（market_stocks_service + strategy_result_repository 共用），`/strategy-runs/{run_id}/results` 增加 industry/concept Query 参数（AND 语义），`TableViewPresetConfig` 增加 industry/concept 可选字段（不新增表/migration），前端 URL state + MarketToolbar + StrategyDataTable 受控 props 集成。`boards.available=false` 时输入禁用+提示。**PR #77 收口**：industry 改关键词 ilike 匹配（`MarketBoard.name.ilike('%keyword%', escape='\\')`，NFKC + trim + 转义 `\`/`%`/`_`，匹配完整路径任意一级）；删除原生 datalist，改用 `BoardFilterCombobox.tsx`（行业允许任意关键词、概念精确匹配、键盘导航、aria、高亮、12 条建议上限）；`StrategyDataTable` preset 校验跳过 industry 关键词检查。**PR #77 收口第二轮 P0 前端修复**：`openPanel()`/`handleInputChange` 改 `activeIndex=-1`（Enter 不再自动选中第一条）；NFKC 规范化（`.normalize('NFKC')`）与后端 `_normalize_keyword` 对齐；`useId()` 唯一 listbox/option ID；`suggestionRank()`（exact=0/prefix=1/contains=2 + `localeCompare('zh-Hans-CN')` 稳定排序）；清除按钮移除 `tabIndex={-1}` 恢复键盘可达；`hasInputNoMatch` 无结果反馈（「无匹配行业」/「未找到该概念」）；SCSS 行业面板 360-480px / 概念面板 240px / `.comboboxPanel` `min-width: 100%`；建议项 `<li>` 增加 `title` 属性显示完整路径。**PR #77 收口第二轮 P1 后端修复**：`board_filter_helper.py` concept 在 `==` 比较前应用 `_normalize_keyword()`（NFKC + trim，与 industry 共用同一规范化函数）；`config.py` `_resolve_board_sync_enabled()` 严格解析（truthy/falsy 集合、非法值 `RuntimeError` fail-fast、bool/str 类型处理、修复 `bool("false")` bug）；`after_close_orchestrator.py` 新增 `_record_board_sync_outcome()` 写入 `job_run_events` + `SchedulerJobRun.metadata_json.board_sync_result`（success/failure/skip 三分支）；`market_stocks_service.py` 新增 `_get_board_sync_status_from_job()` Redis 缺失时回退 job metadata，`source="unknown"` 语义；删除 `backend/scripts/classify_unresolved_stocks.py` 一次性调查脚本。后端 test_board_filter_helper.py 32 项（+8 concept NFKC）+ test_board_sync_enabled_config.py 28 项（+16 严格解析）+ test_board_sync_status_fallback.py 14 项（新增 Redis 回退）全通过；前端 wencaiBoardSyncContract.test.ts 37 项（+15 P0/SCSS）全通过；EXPLAIN ANALYZE 生产数据：「电子」0.819ms / 「半导体」0.589ms / 「电子+光刻机」AND 1.044ms。待部署生产验证：1) `/market` 顶部显示 BoardFilterCombobox（无原生 datalist）；2) 输入「半导体」可筛选所有相关三级行业；3) 输入一级行业词可获得跨子行业结果；4) 概念「光刻机」仍正确（NFKC 规范化精确匹配）；5) industry+concept 为 AND；6) URL/preset/Excel 一致；7) 下拉样式符合盘迹视觉（行业 360-480px / 概念 240px）；8) Enter 无激活建议不自动选中第一条；9) 全角/半角 concept 输入匹配一致；10) Redis 缺失时 `/market/boards` 从 job metadata 回退状态；11) `source="unknown"` 不被前端误判为「从未同步」。 | 部署后生产 E2E 验证行业关键词筛选 + BoardFilterCombobox P0 修复 + concept NFKC 规范化 + Redis 回退 | P1 |
| ALIGN-051 | 盘迹品牌视觉 V1.0 待生产验证 | CHANGE-20260713-006 已实现：`variables.scss` 更新为莹感绿 token 体系（#00F6C2 主色，#0A0F14 背景，#F2F6F8 文本，红涨绿跌不变），BrandLogo 重写为四节点折线+末端高亮共识节点，global.scss/MarketWorkspace.module.scss/AccountMenu/UserAppShell/MarketInstrumentPane 硬编码颜色清理（共 100+ 处替换），品牌资产复制到 `frontend/src/assets/brand/`。tsc 0 错误。待部署生产验证：1) Logo 显示四节点折线+莹感绿末端；2) 主色为莹感绿（主按钮/选中 tab/focus）；3) 背景为深石墨黑；4) 涨红跌绿不变；5) 无旧蓝色残留。 | 部署后生产 E2E 验证品牌视觉 V1.0（1440/1920） | P1 |
| ALIGN-052 | 管理员入口与 AdminRoute 权限契约待生产验证 | CHANGE-20260713-007 已实现：`getAccountMenuItemsForVariant(isAdmin, 'user')` 当 `is_admin=true` 时显示"管理后台"链接到 `/admin`；非管理员（`is_admin=false`）DOM 完全不渲染管理后台入口（非 CSS 隐藏）；`AdminRoute` 以 `user.is_admin` 为唯一权限真源，新增 `accessLoading` 状态防止 auth hydration 未完成时提前判定 false；`getAccountMenuItemsForVariant(_, 'admin')` 显示"返回行情"链接到 `/market`，不重复显示"管理后台"。appNavigation.test.ts 覆盖 4 个 variant 组合。待部署生产验证：1) 普通用户 `AccountMenu` DOM 无管理后台入口；2) 管理员 `AccountMenu` 显示"管理后台"且可进入 `/admin`；3) 非管理员直接访问 `/admin/*` 被 `AdminRoute` 重定向到 `/market`；4) 刷新 `/admin/*` 时 `accessLoading` 期间显示 loading 不闪烁重定向。 | 部署后生产 E2E 验证管理员入口与权限 | P1 |
| ALIGN-053 | 批准 Logo 与视觉 V1.0 token 待生产验证 | CHANGE-20260713-007 已实现：`BrandLogo` 改用批准 PNG 资产（`logo_symbol_128.png`/`logo_horizontal_dark.png`），手绘 SVG 已停用；`variables.scss` V1.0 token 体系（品牌 #00F6C2/#39F5CF/#00B28A；背景 #0A0F14/#111A23/#161F29；文本 #F2F6F8/#A4B0BC/#6B7785；状态 success #00F6C2/warning #FFB020/danger #FF4D4F/info #36BFFA）；LandingPage/BetaApplicationModal 按钮改为品牌绿渐变；global.scss focus/selection/toggle 改为 `var(--brand)`。brandLogo.test.ts + visualTokens.test.ts 覆盖。待部署生产验证：1) Logo 显示批准 PNG 资产；2) 主按钮品牌绿渐变；3) focus/selection 为品牌色；4) token 体系符合 V1.0 配色。 | 部署后生产 E2E 验证批准 Logo 与视觉 V1.0 | P1 |
| ALIGN-054 | 数量契约四层语义待生产验证 | CHANGE-20260713-007 已实现：`/strategy-runs/{run_id}/results` 响应含四层数量：`source_total`（published run 原始总量，不受业务筛选影响）、`universe_total`（all/watchlist 范围总量，业务筛选前）、`filtered_total`（keyword+industry+concept+metric_filters 后总量）、`items`（filtered_total 当前页）；`len(items) <= filtered_total`；`items`/`filtered_total` 必须应用完全相同的 keyword/industry/concept/metric_filters 条件；SQL 数量固定，禁止 N+1。test_strategy_results_industry_concept.py 新增数量语义测试。待部署生产验证：1) 无筛选时 `source_total == universe_total == filtered_total`；2) keyword 筛选时 `filtered_total < universe_total` 且 `source_total` 不变；3) industry/concept 筛选时四层语义正确；4) `len(items) <= filtered_total`。 | 部署后生产 E2E 验证数量契约四层语义 | P1 |
| ALIGN-055 | K 线右侧留白与交互契约待生产验证 | CHANGE-20260713-008 已实现：`StrategyChart` 右侧留白 `RIGHT_PADDING_RATIO = 0.20`；`effectivePlotW = plotW * (1 - RIGHT_PADDING_RATIO)`；`step = effectivePlotW / display.length`；保留 Pointer Events 拖拽、滚轮锚点缩放、双击复位、移动端双指缩放。chartRightPadding.test.ts 覆盖源码契约。待部署生产验证：1) K 线最右端有 20% 留白；2) 留白区域可拖拽；3) 滚轮缩放锚点正确；4) 双击复位保留留白；5) 留白不改变事件/节点点击位置。 | 部署后生产 E2E 验证 K 线右侧留白与交互（1440/1920） | P1 |
| ALIGN-057 | 个股详情市值显示待生产验证 | CHANGE-20260713-010 已实现：`QuoteResponse` 新增 `total_market_cap`/`float_market_cap`/`market_cap_as_of`/`market_cap_source`/`market_cap_degraded_reason` 5 个字段；后端 `_compute_market_cap_fields` 从 `Instrument.total_share`/`float_share` 与当前价格计算市值，数据缺失返回 `market_cap_data_unavailable`；`InstrumentShareCapitalSyncService.sync_share_capitals` 每日 18:00 通过 `pytdx.get_finance_info` 同步 SH/SZ 股本（BJ 跳过）；migration 063 添加 `instruments.total_share`/`float_share`/`share_as_of` 列；前端 `StockQuoteStrip` 暴露 `formatMarketCap`（区分万/亿/万亿元，空值 `--`）+ `QuoteMetric` 子组件，展示 8 项指标（现价/涨跌/开盘/最高/最低/成交额/总市值/流通市值），总市值与流通市值放在成交额右侧；`change010Contract.test.ts` 覆盖源码契约。待部署生产验证：1) `/stock/:symbol` 顶部报价条显示 8 项指标；2) 总市值/流通市值在成交额右侧且 tooltip 显示数据日期；3) 股本缺失时显示 `--` 不伪造；4) 18:00 worker 同步后 quote 端点返回真实市值。 | 部署后生产 E2E 验证市值显示 | P1 |
| ALIGN-058 | /market Excel 导出待生产验证 | CHANGE-20260713-010 已实现：`POST /strategy-runs/{run_id}/results/export` 端点复用 `query_published_selector_results` 筛选构造器；`excel_export_service` 使用标准库 `zipfile`+`xml.etree` 生成真实 .xlsx（禁止 `openpyxl`/`xlsxwriter`），`MAX_EXPORT_ROWS=10000` 超 422，公式注入防护 `=`/`+`/`-`/`@` 前缀单引号，百分比 `numFmt 0.00%`；响应头 `X-Source-Total`/`X-Universe-Total`/`X-Filtered-Total`/`X-Export-Rows` 四层语义；文件名 `盘迹_DSA_YYYYMMDD_筛选结果.xlsx` RFC 5987 编码；前端 `MarketWorkspacePage.handleExport` 复用 `convertFiltersToMetricFilters` 与 `buildStrategyResultQueryParams` 同源；`stock` 列 `payload_key=null` 不导出操作列；`test_excel_export_api.py` 21 项 + `test_excel_export_service.py` 9 项全通过。待部署生产验证：1) `/market` 工具栏导出按钮可见；2) 点击下载真实 .xlsx 文件可正常打开；3) 行数 = `X-Filtered-Total`；4) 排序/筛选/keyword 同步生效；5) 10000 上限 422；6) 公式注入被防护。 | 部署后生产 E2E 验证 Excel 导出 | P1 |
| ALIGN-059 | 右栏小 K 线待生产验证 | CHANGE-20260713-010 已实现，CHANGE-20260714-001 扩展为五周期：`MarketRightPanel`（`MiniKlineCard` 顶部 + `AtomicFactsPanel` 底部）替代原 `EventStatePanel` 单独使用；`MiniKlineCard` 使用 `lightweight-charts` v4 `createChart` + `addCandlestickSeries` 渲染简化 K 线（无指标/Node/事件/工具栏），**五周期 segmented control** 切换 `15m`/`60m`/`1d`/`1w`/`1mo`（标签"15分/60分/日/周/月"），默认 `1d`；`attributionLogo=false` 关闭水印；`useMiniKlineData` 定义 `BARS_COUNT` = `{15m: 120, 60m: 120, 1d: 80, 1w: 60, 1mo: 48}` + `refetchInterval: false`；面板收起时 `MarketWorkspacePage` 不挂载 `MarketRightPanel`，bars/context 请求均为 0；展开后只请求活动周期不预取五周期；chart 实例仅创建一次（`useEffect []`），`ResizeObserver` + 卸载清理 `disconnect()`+`chart.remove()`+ref 清空；`change010Contract.test.ts` 覆盖源码契约。待部署生产验证：1) `/market` 右栏顶部显示小 K 线；2) 切换五周期生效；3) 收起时无 bars/context 请求；4) 切回已加载周期不重复请求；5) 卸载组件无 canvas 残留。 | 部署后生产 E2E 验证右栏小 K 线 | P1 |
| ALIGN-060 | 股票名称筛选 alias 待生产验证 | CHANGE-20260713-010 已实现：`DataTableColumn.filterAlias?: 'keyword'` 字段允许列头筛选与顶部搜索共用唯一 keyword 真源；`stock` 列设置 `filterAlias: 'keyword'` + `filterable: true`；`StrategyDataTable` `KeywordFilterPopover` 组件 `onApply`/`onClear` 双向同步（`setGlobalQuery` + `onKeywordChange`）；列头激活状态基于 `effectiveKeyword`；`isKeyword` flag 区分不进入 `filters` state；URL sync `replace: true` + `skipNextUrlSyncRef` 避免 URL 循环；`currentConfig.keyword`/`applyPresetConfig` 共用 `effectiveKeyword`；`stock`/`action` 不入 `metric_filters`；`change010Contract.test.ts` + `columns.test.ts` 覆盖源码契约。待部署生产验证：1) `/market` 股票列头筛选与顶部搜索双向同步；2) 列头激活状态正确；3) URL 无循环；4) preset 保存/恢复 keyword 一致。 | 部署后生产 E2E 验证股票名称筛选 alias | P1 |
| ALIGN-061 | 股本数据每日同步待生产验证 | CHANGE-20260713-010 已实现：`InstrumentShareCapitalSyncService.sync_share_capitals` 通过 `pytdx.get_finance_info` 同步 SH/SZ 标的 `total_share`/`float_share`，BJ 跳过，批次 500，写 `share_as_of=trade_date`；`worker.py` 新增 `scheduled_share_capital_sync` 任务（每日 18:00）；migration 063 添加 `instruments.total_share`/`float_share`/`share_as_of` 列；pytdx 同步调用通过 `asyncio.to_thread` 包装避免阻塞事件循环；市值数据缺失时所有 market_cap 字段为 `null`。待部署生产验证：1) 18:00 worker 执行后 `instruments` 表 `share_as_of` 为当日；2) SH/SZ 标的 `total_share`/`float_share` 非空；3) BJ 标的 `share_as_of` 仍为 `null`；4) quote 端点返回真实市值。 | 部署后生产验证股本数据同步 | P1 |
| ALIGN-062 | SMC Pine parity 待生产验证 + Pine golden fixture 待导出 | CHANGE-20260715-001 新增 SMC 指标（基于 ref/smc.py SMA 基线），CHANGE-20260715-002 升级为 Pine 语义核心 `smc_pine_core.py`（唯一核心，生产+测试共用），CHANGE-20260715-003 修复 trailing 执行顺序：Pine 原语 `pine_rma`（Wilder RMA）/`pine_atr`/`pine_cumulative_mean_range`（bar0=NaN）/`pine_highest/lowest`/`pine_crossover/crossunder`；`_SMCPineState` 状态机完全按 Pine lines 766-807 执行顺序（**trailing→swing→internal→equal→BOS/CHoCH→mitigation**，trailing 必须在 getCurrentStructure 之前）；events 使用 `internal: bool` 替代旧 `kind` 字段；warmup ≥500 根（1d 用 `full_daily_bars` 全量日线）；SMC 输出不截断（time 数组保持完整长度对齐 anchor/confirmed）；`ALGORITHM_VERSION` v6→v7（旧 SMA 缓存强制失效）；用户 Pine 代码（`ref/smc_user_source.pine`，SHA256 0bd3d2ad，843 行）为原创作品并授权盘迹使用。**Pine golden fixture 状态 PENDING**（等待 TradingView 导出美诺华 603538 日线 1000 根 + 15m 样本），`TestPineGoldenFixture` skip，**没有 Pine golden fixture 不得宣称"完全对齐"**。待生产验证：1) `include_smc=false` 时无 SMC 计算；2) `include_smc=true` 时返回 smc layer；3) `/market` 右栏不请求 SMC；4) SMC 缓存键 `:smc` 后缀隔离；5) 前端 BOS/CHoCH/OB/EQH/EQL/trailing 渲染对齐 Pine 视觉语义。 | 1) TradingView 导出 Pine golden fixture 后跑逐事件 golden 测试；2) 部署后生产 E2E 验证 SMC 按需计算 + renderer 对齐 | P1 |
| ALIGN-063 | MiniKline viewport 彻底重写待生产验证 | CHANGE-20260715-001 实现纯函数 `computeMiniKlineViewport`，CHANGE-20260715-002 彻底重写：目标根数按周期固定（15m=48、60m=44、日=40、周=36、月=30）；`barSpacing = clamp(contentWidth/visibleBars, 5.5, 8)`；左侧 1-2 根留白 `from=max(-2, n-visible-1)`；右侧 3 根留白 `to=n-1+3`；禁止调用 `fitContent`/`resetTimeScale`/`scrollToRealTime`；`autoscaleInfoProvider` 扩展价格范围（上 12% 下 15%）；`rightPriceScale` `autoScale=true` + `scaleMargins {0.08,0.08}` + `minimumWidth=56`；`setData` 后在 `requestAnimationFrame` 应用 range；图表容器高度固定 190px；15 项纯函数测试 + 组件级 mock 测试。待生产验证：1) 五周期切换 viewport 正确；2) 上下边距合理；3) 切周期不复用旧 range；4) resize 后重算 viewport；5) 无 `fitContent` 调用。 | 部署后生产 E2E 验证 MiniKline 五周期 viewport + 上下边距 | P1 |
| ALIGN-064 | Bug 1 详情左栏 loading 占位 + Pine 参考源文件入 Git 跟踪待生产验证 | CHANGE-20260715-004 已实现：`useStockDetailActions` 新增 `sourceListLoading: boolean` 字段（`hasMarketContext=true` 时为 `publishedRunsQuery.isLoading \|\| !activeRunId \|\| sourceResultsQuery.isLoading`，否则为 `monitorStatusQuery.isLoading`）；`StockDetailPage` 新增 loading 占位渲染分支（`<aside data-testid="detail-source-list-loading">` + "加载中…" 占位文案）；`global.scss` 新增 `.tv-source-list-placeholder` 样式；`ref/smc_user_source.pine`（SHA256 0bd3d2ad，843 行）使用 `git add -f` 纳入 Git 跟踪（`.gitignore` 仍排除 `ref/` 其他文件）；9 项源码契约测试 `detailSourceLoadingContract.test.ts` 全部通过（sourceListLoading 字段、loading 占位渲染、列表渲染条件排除 loading、header 显示、CSS 存在、handleNavigateToStock 显式传 source/strategy、URL 完整性、不使用 useMarketStocks、上一只/下一只保留 returnTo）。待生产验证：1) `/market` 设置筛选+排序+页码后进入详情，左栏先显示 loading 占位再切换到列表；2) DSA 数据加载中不空白；3) 上一只/下一只保留 source/strategy/returnTo/timeframe；4) `git ls-files ref/smc_user_source.pine` 返回该路径；5) SHA256 匹配 0bd3d2ad。 | 部署后生产 E2E 验证 Bug 1 loading 占位 + Pine 参考源文件 Git 跟踪 | P1 |
| ALIGN-065 | 详情左栏来源状态四态拆分 + 表格 sticky 列和工具栏对齐根治待生产验证 | CHANGE-20260715-005 已实现：`useStockDetailActions` 新增 `sourceListError`/`sourceListEmpty`/`sourceContextInvalid` 三字段，与 `sourceListLoading` 构成 loading/error/empty/invalid 四态；显式 `source` 参数优先于 `returnTo` 推断；`source=selection` 即使 returnTo 无效也不回退 watchlist（设置 `sourceContextInvalid=true`）；`normalizeInternalReturnTo` 上限 500→4096；表格结构 `table-wrap`→`table-shell > meta-bar + search-bar + table-scroll > table + pager`（只有 `table-scroll` 设 `overflow-x: auto`）；`isStickyColumn(col)` 统一判断只允许 `col.key==='stock'` 为 sticky 列；删除死 CSS `.sticky-col-change-pct`；`AdminAfterClosePipelinePage` 同步迁移。待生产验证：1) `/market` 筛选后进入详情左栏正确显示四态；2) returnTo 无效时显示 invalid 引导；3) 表格横向滚动时工具栏/分页器不随滚动消失；4) 只有 stock 列 sticky；5) 复杂筛选 URL 不被 4096 截断。 | 部署后生产 E2E 验证详情左栏四态 + 表格 sticky/工具栏对齐 | P1 |
| ALIGN-066 | MiniKline 闭包根治 + SMC Pine 对齐 RMA NA 语义 + 首个 pivot off-by-one + EQH/EQL 三时间点待生产验证 | CHANGE-20260715-006 已实现：（1）MiniKline 闭包根治：`MiniKlineCard.tsx` 新增 `barsLengthRef`/`timeframeRef`/`rafIdRef`；`applyViewportRange` 改为 `useCallback([], )` 稳定函数从 refs 读取；新增 `scheduleApplyRange` 稳定函数取消 pending rAF 后调度新 rAF；`ResizeObserver` 回调调用 `scheduleApplyRange`（不直接闭包捕获 bars/timeframe）；卸载清理取消 pending rAF；5 项闭包契约测试（16-20）。（2）SMC Pine 对齐 `pine_rma` NA 语义：`smc_pine_core.py` 的 `pine_rma` 在 `bar_index < length-1` 返回 `na`（非逐步 SMA），`bar_index == length-1` 写入 SMA 种子，之后 Wilder 递推；严格复现 Pine v5 `ta.rma`；`test_pine_rma_min_periods_before_seed` 更新为断言 NaN。（3）首个 pivot off-by-one 修复：`start_of_new_leg`/`start_of_bearish_leg`/`start_of_bullish_leg` 从 `i > size` 改为 `i >= size`；`get_current_structure` 从 `if i <= size: return` 改为 `if i < size: return`；首个 leg/pivot 在 `i == size` 检测。（4）EQH/EQL DTO 三时间点：EQL 和 EQH 两处新增 `detection_index`/`detection_time`（leg change 确认 bar, `i`），与 anchor/confirmed 分离。待生产验证：1) MiniKline 切周期/resize 后 viewport 正确（不使用 stale 值）；2) SMC `pine_rma` 前 `length-1` 根为 NaN；3) 首个 pivot 在 `i==size` 检测；4) EQH/EQL DTO 含 detection_index/detection_time；5) Pine golden fixture 仍 PENDING。 | 部署后生产 E2E 验证 MiniKline 闭包 + SMC Pine 对齐 | P1 |
| ALIGN-067 | SMC crossover/crossunder level_curr/level_prev 快照修正 + 缓存 v8→v9 + swing_bias 直接返回待生产验证 | CHANGE-20260716-001 已实现：（1）`display_structure` 修正：旧 `pine_crossover(close_curr, close_prev, current_level, current_level)` 错误地将 `current_level` 同时作为 curr 和 prev；改为每 Bar 快照六个 pivot level（swing/internal 独立），`displayStructure` 接收 `level_curr`/`level_prev` 独立快照，crossover=`close_curr > level_curr && close_prev <= level_prev`，NaN→False。（2）EQH/EQL DTO 统一：`anchor_*`=前一 pivot；`second_pivot_*`=新 pivot 所在 `i-size`（视觉线端点）；`confirmed_*`=当前检测 Bar `i`（因果/回放使用）；`ref_i` 不得命名为 `confirmed`。（3）`swing_bias` 直接返回 `state.swing_trend.bias`（{1,-1,0}），前端不猜测。（4）缓存 v8→v9（旧 v8 自动失效）。待生产验证：1) BOS/CHoCH 触发时点与 Pine 一致；2) EQH/EQL 视觉线端点为 `second_pivot_index`；3) `swing_bias` 从 DTO 读取；4) 缓存 v9 生效。 | 部署后生产 E2E 验证 SMC crossover 修正 + swing_bias | P1 |
| ALIGN-068 | SMC 前端 viewport 区间求交 + anchor_index 统一 + slice(0,5) + 标签不加 ·I + 纵轴候选 + Canvas mock 测试待生产验证 | CHANGE-20260716-001 已实现：（1）前端 OB 字段从 `bar_index` 统一为 `anchor_index`（与后端 DTO 一致）。（2）viewport 区间求交：anchor 左侧 clip+clipped_left，confirmed 右侧 clamp 到 plotRight，仅完全不相交跳过；不得要求 anchor 与 confirmed 都在 displayTimes 中。（3）OB slice(0,5)：只显示数组头部最近 5 个 `internal && !mitigated` OB。（4）标签不加 `·I` 后缀。（5）纵轴候选：event.level、OB high/low、EQH/EQL level、trailing top/bottom。（6）纯函数 `smcRendering.ts` + Canvas mock 行为测试。待生产验证：1) viewport 滚动时 OB/BOS/CHoCH/EQH/EQL 正确显示/隐藏；2) anchor 在左侧时 clipped_left 标记；3) 最近 5 个 internal OB 显示；4) 标签无 `·I`；5) 事件不被 Canvas 裁掉。 | 部署后生产 E2E 验证 SMC 前端 viewport 区间求交 | P1 |
| ALIGN-069 | MiniKline CSS 重做 + barSpacing 应用 + 真实左留白 + 五等分 tabs 待生产验证 | CHANGE-20260716-001 已实现（取代 ALIGN-063 旧 barSpacing 方案）：（1）单一方案按周期取目标根数 15m=48/60m=44/日=40/周=36/月=30，`bars.slice(-target)` 传给 series。（2）`setData` 后设置 logical range `{from:-2, to:visibleData.length-1+3}`（真实左 2/右 3 空位）。（3）删除死 barSpacing 计算（旧 `barSpacing = clamp(contentWidth/visibleBars, 5.5, 8)` 只计算未应用）。（4）autoscale 只基于 visibleData 的 high/low，上 12%/下 15%。（5）CSS：tabs 五等分全宽 grid；chart `height:190px`；价格轴 56px。（6）真实 mock 测试。待生产验证：1) 五周期 setData 根数正确；2) logical range 左 2/右 3 空位；3) 无死 barSpacing；4) tabs 五等分；5) chart 高度 190px。 | 部署后生产 E2E 验证 MiniKline 真实方案 | P1 |
| ALIGN-070 | indicator_service required_inputs 优化待生产验证 | CHANGE-20260716-001 已实现：`_REQUIRED_INPUTS` 映射（dsa_selector={daily}、volume_node_monitor={daily,15min,minute}、bb_monitor={daily}、watchlist_monitor={daily}）；`_determine_required_bars()` 合并需求；15min 回看 400 天（limit=4000）、minute 回看 5 天（limit=2）、60min 回看 750 天；`needs_15min = "15min" in required_bars or timeframe == "15m"`；`_query_minute_bars` 新增 `limit` 参数。待生产验证：1) 1d 请求不再查询 750 天 15m/1m；2) SQL 数量/耗时/响应字节不变差；3) VP 计算结果不变；4) timeframe==15m 时 macd_bars=bars_15min 正常加载。 | 部署后生产 E2E 验证 required_inputs 优化 + SQL 性能 | P1 |
| ALIGN-071 | SMC source diagnostics 字段待生产验证 | CHANGE-20260716-001 已实现：API 新增 `smc_source_bar_hash`/`smc_source_first_time`/`smc_source_last_time`/`smc_source_bars`/`smc_adj`（仅 `include_smc=True` 时返回）；`smc_source_bar_hash` 基于 SMC 实际完整输入（1d 用 `full_daily_bars`，其他用 `macd_bars`），不复用截断后的 macd_bars hash。待生产验证：1) `include_smc=true` 时返回 smc_source_* 字段；2) `include_smc=false` 时字段为 None/0；3) 1d 时 `smc_source_bars` 等于 full_daily_bars 长度；4) hash 跨请求稳定。 | 部署后生产 E2E 验证 SMC source diagnostics | P1 |
| ALIGN-072 | TV parity baseline 待 TV CSV 导出 | CHANGE-20260716-001 已实现：（1）`ref/smc_user_source.pine` 末尾新增 18 个 `display=display.none` 隐藏 export 字段。（2）`backend/tests/test_smc_tv_parity.py` 3 项测试（bar parity/event parity/swing_bias parity），无 fixture 时 skip。（3）fixture 路径 `backend/tests/fixtures/smc_pine/smc_tv_<symbol>_<tf>.csv`。**PENDING**：等待用户从 TradingView 导出 CSV 后才能完成输出级完全一致断言；未提供前不得宣称"完全对齐"。待验证：1) TV 添加 indicator 后导出 CSV；2) CSV 放置到 fixture 路径；3) parity 测试不再 skip；4) 事件差异为 0。 | 用户提供 TV CSV 后跑 parity 测试 | P1 |
| ALIGN-073 | AFC V1 新 summary 持久化未在生产 worker 验证 | CHANGE-20260716-003/004/005 已实现：冻结研究合同与产品展示合同分离；后端 `PublicAtomicFactItem`（无 factId/sourcePath）、valueText 短原子值（统一格式器 `_fmt_atomic_value` 读 presentation `valuePrecision`）、visualKind 统一枚举（`metric/value_with_category/relation/position/distance/ratio`）、M5 任一缺失即缺失（`or` 非 `and`，**CHANGE-20260716-005**）+双true质量异常（`正在收紧/正在释放/正常`）、recentChanges 含中文 `label`（非 publicKey）；**持久化/调试分离**：`compute_atomic_facts()` 仅 core/aux/availability（无 debug）、`compute_atomic_fact_debug()` 管理员即时生成、`build_persisted_afc_payload()` 含四版本字段无 debug、`_is_valid_stored_afc` 严格校验四版本+四组+publicKey+无 debug；前端 `FactRow` 按 visualKind 渲染（CSS Grid 透明行非卡片）、S3 完整轨道、Auxiliary 分组、Drawer 焦点 trap+body 滚动锁定。**CHANGE-20260716-005 终审修正**：1) M5 `_squeeze_state` 改 `or`；2) Recent Changes 按 each Fact `valuePrecision` 量化（`_quantize_fact_value`）+ `FACT_DIMENSION_BY_ID`（事实消失不默认 trend）+ `_combine_text`（M3 双文本）；3) `PersistedAtomicFactsPayload` Pydantic Schema 严格校验（四版本完全匹配/core 键恰好四维度/每项通过 `PublicAtomicFactItem`/publicKey 属正确维度且无重复未知/T3/T6/V1 不存在/availability 一致/不含 debug，不兼容 fallback 不得 500）；4) `as_of` 改为截止日期语义（`trade_date <= as_of` + `DESC LIMIT 1`，周末/无批次返回之前最近发布）；5) legacy 快照 `snapshot_run_not_linked`/`legacy_snapshot_ambiguous` 进入 `dataQuality.degradedReasons`；6) API 响应新增 `meta`（`payloadVersion`/`researchFreezeVersion`/`presentationVersion`），前端 Header 从 `data.meta.researchFreezeVersion` 读取禁止硬编码 V4.13；7) presentation `secondaryLabel`/`unclassifiedLabel` 为真源（`_secondary_text_for` helper，移除服务中散落常量）；8) 前端 factRow secondary 右对齐（`.secondary` grid-template-areas `". secondary"`）、PositionRow 独立布局（轨道横跨整组宽度）、RecentChanges 显示 `deltaText`、Drawer 双向焦点限制（`!drawer.contains(active)` Tab/Shift+Tab 均回环）。但**当前生产 worker 仍为旧镜像 `88dd7d1`**，不会在盘后 `build_summary_payload` 中持久化新四版本字段格式，页面主要依赖旧快照 fallback（同纯函数重算，validator 判定旧格式缺版本→fallback）；新 summary 持久化 + persisted-first 直读链路**尚未在 production worker 验证**。本轮仅 `docker compose up -d --no-deps backend frontend`，未重启 worker。 | 升级 worker 镜像后，部署验证：1) 盘后 `stock_feature_snapshots.summary_payload.atomic_fact_contract_v1` 含四版本字段且无 debug；2) Context API 优先读取已持久化 payload（`PersistedAtomicFactsPayload` 校验通过）且 `coreDenominator=14`、结构含 `publicKey`；3) fallback 与 persisted 输出一致；4) worker `build_sha` 与 backend/frontend `/version` SHA 一致 | P1 |
| ALIGN-074 | AFC 详情页终审修正（originScope 唯一来源 + confirmed_swing_position 产品观察 + recentChanges 仅最新交易日 + grid 布局）待生产验证 | CHANGE-20260716-006 已实现：（1）**originScope 唯一来源真源**：新增 `frontend/src/features/stock-research/stockDetailNavigation.ts`，提供 `buildStockDetailUrl`/`resolveStockDetailOrigin`/`sourceForOriginScope`/`strategyForOriginScope`；显式 `originScope=market\|watchlist` 参数为来源第一真源，不被 `returnTo.scope` 覆盖；旧 URL 无 `originScope` 时兼容解析 `returnTo.scope`；无任何来源的直接 URL 默认 watchlist；`originScope` 存在且 `returnTo.scope` 不同 → `contextMismatch=true`，禁止静默回退自选；三入口（`MarketWorkspacePage` 点击股票、`useStockDetailActions` 上一只/下一只、`StockDetailPage` 左栏来源列表点击）全部调用同一 `buildStockDetailUrl`，删除手工字符串拼接；`MarketWorkspacePage` 使用当前 `searchParams` 副本并强制写入 `scope` 和 `selected` 生成 returnTo，禁止直接复制可能滞后的 `location.search`；`detailNavigation.ts` 旧 `buildStockDetailUrl` 已删除（禁止第二套 URL 拼接）；`stockDetailNavigation.test.ts` 10 项 + `detailNavigation.test.ts` 12 项全通过。（2）**confirmed_swing_position 产品观察扩展**：新增独立第三合同 `backend/app/contracts/atomic_fact_product_observations_v1.json`，sourceFields 仅 `raw`/`confirmed_high`/`confirmed_low`，`inside` 由 raw 派生（`inside = raw if 0<=raw<=1 else None`，不读取 worker 持久化的 `_0_1` 字段）；`structural_factor_service.py` 不新增 `price_position_in_confirmed_swing_0_1` 持久化字段，不修改 worker 持久化链；`compute_product_observations` 纯函数派生，区间外数值不静默 clip（<0 显示"低于确认区间"，>1 显示"高于确认区间"）；新增 Pydantic 模型 `ProductObservationItem`/`ProductObservations`；Context 响应新增 `productObservations.structure` 字段，观察项 `publicKey=confirmed_swing_position`/`visualKind=confirmed_position`/`scope=product`；**不计入 Core 14**（产品观察不进入 `core`/`auxiliary`/`availability`，不影响 14/14 统计）；前端新增 `ConfirmedPositionRow` 组件插入结构组"价格与已确认区间关系"之后，0.33/0.67 边界与 S3 一致，保留现有 S3"价格在当前主要波段的位置"；10 项后端产品观察边界测试（raw=-0.2/0/0.33/0.67/1/1.2 边界 + confirmed_high/low None 处理 + Core 14 独立性 + scope="product" 字段 + 不修改 worker）全通过。（3）**recentChanges 仅最新交易日**：`_find_recent_published_snapshots` limit 10→2，只比较最新交易日与前一发布交易日；响应新增 `latestChangesFrom`（前一发布交易日）/`latestChangesAsOf`（最新发布交易日）字段；空状态显示"最近交易日（{asOf}）无状态变化"；删除 `compute_recent_changes` 30 条上限；confirmed_swing_position 变化标 `scope=product` 不伪装 V4.13 Core；2 项后端 2-snapshot 对比测试全通过。（4）**详情右边界 grid 布局**：`.tv-detail-layout` 从 `display: flex` 改为 `display: grid; grid-template-columns: 200px minmax(0,1fr)`；`.tv-workspace`/`.strategy-chart-wrap`/`.tv-chart-column`/`.tv-canvas-wrap` 统一 `width:100%; min-width:0; max-width:100%; box-sizing:border-box`；`ResizeObserver` 改为 `requestAnimationFrame` 立即 draw + 120ms trailing draw（保留 window resize 补偿）。后端 42 passed + API 集成 25 passed + ruff/mypy clean；前端 navigation 10 + detailNavigation 12 + AFC contract 33 passed + tsc/eslint clean + vite build 成功。待部署生产验证。 | 部署后生产 E2E 验证：1) `/market` 设置筛选+排序+页码后进入详情，左栏来源列表正确（market/watchlist），不静默回退自选；2) `originScope` 与 `returnTo.scope` 冲突时 `contextMismatch=true` 不静默回退；3) 上一只/下一只和左栏切换保留来源上下文；4) 旧 `detailNavigation.ts:buildStockDetailUrl` 不存在；5) Context 响应含 `productObservations.structure`，`confirmed_swing_position` `scope=product`，`inside` 仅 0≤raw≤1 时有效；6) `structural_factor_service.py` 未新增 `price_position_in_confirmed_swing_0_1` 字段；7) recentChanges 只比较 2 个快照，响应含 `latestChangesFrom`/`latestChangesAsOf`，无 30 条上限；8) 详情页右边界 1440/1680/1920 视口对齐 ≤1px，`.tv-detail-layout` 为 grid 200px+minmax(0,1fr)；9) ResizeObserver raf+trailing 模式生效 | P1 |
| ALIGN-075 | SMC Pine parity 最终收口（warmup/历史分离 + execution gate + trailing NaN + OB 顺序 + EQH/EQL 几何 + Strong/Weak 起点 + golden 重做 + 确定性测试 + ALGORITHM_VERSION v10）待生产验证 | CHANGE-20260717-001 已实现（11 项修复，逐项引用 Pine 行号 L74/76/84/384/389/396/397/519/706/721-727/784/787）：（1）**warmup/历史分离**：15m 独立查询 `bars+_SMC_WARMUP_BARS`（5000 计算/4000 展示），1mo 扩展回看 7000 天确保 ≥200 根（ATR200 可初始化），1d 用 `full_daily_bars` 全量日线，1h/1w 复用 `macd_bars`；删除"15m≈12000"失真注释。（2）**execution gate**：`smc_pine_core.py` `DEFAULT_PARAMS` 新增 `show_internals`/`show_structure`/`show_trend`；`internal_gate = show_internals OR show_internal_order_blocks OR show_trend`（Pine L784），`swing_gate = show_structure OR show_swing_order_blocks OR show_high_low_swings`（Pine L787），不再只由 OB 开关门控。（3）**trailing NaN**：`update_trailing_extremes` 严格复刻 `math.max(high, na)=na`，trailing.top/bottom 为 NaN 时不更新，只由 swing pivot 初始化（L426/L454）。（4）**OB 顺序**：`store_order_block` 改 `insert(0,...)` newest-first 复刻 Pine `array.unshift`（L519），前端 `slice(0,5)` 取最新 5 个 active internal OB。（5）**EQH/EQL 几何**：`StrategyChart.tsx` 两端点线（起点 `prev_level`、终点 `level`，可能不水平），EQH=bearish 绿色 + label_down，EQL=bullish 红色 + label_up，标签位于两 pivot 中点（Pine L397）。（6）**Strong/Weak 起点**：线起点改 `trailing.last_top_time`/`last_bottom_time`（非最后可见 bar），终点 `plotRight`（Pine L722 向右延伸约 20 bar clamp）。（7）**adapter 文档清理**：删除"约 12000 根 15m time"失真描述。（8）**golden 测试重做**：`test_smc_tv_parity.py` 修复 EQH 类型误映射、15m 时间戳压缩、OHLC no-op、±1 容差；新增 OB/EQ/全链断言 3 测试。（9）**确定性测试**：新增 `test_smc_pine_deterministic.py`（427 行，8 测试类：CHoCH/BOS/warmup 一致性/OB 顺序/OB 全链/trailing NaN/execution gate/EQ 几何）。（10）**导出增强**：`ref/smc_user_export.pine` 追加 8 个隐藏 plot（trailing top/bottom/lastTopTime/lastBottomTime + swing/internal pivot level）；真源 `ref/smc_user_source.pine` 不可变（SHA256 0bd3d2ad，843 行，参考源人工阅读）。（11）**ALGORITHM_VERSION v9→v10**：`indicator_cache.py` bump 使旧 v9 SMC 缓存（事件数量/位置/OB 顺序/窗口左缘 pivot-BOS-CHoCH）强制失效；`test_indicator_cache.py` 断言 v10。**PINE_PARITY_PENDING**：无真实 TradingView CSV fixture，代码级修复通过，输出级 parity pending，不伪造 fixture 不声称"完全对齐"。 | 部署后生产 E2E 验证：1) 1d/15m/1h/1w/1mo 五周期打开 SMC，BOS/CHoCH/OB/EQH/EQL/Strong-Weak 可见；2) 15m 展示 4000 根时窗口左缘 pivot/BOS/CHoCH 不丢失（warmup 5000 计算）；3) 1mo ≥200 根（ATR200 可初始化）；4) OB 显示最新 5 个 active internal OB（newest-first）；5) EQH 绿/EQL 红、两端点线（prev_level→level）、中点标签；6) Strong/Weak 线起点为 trailing pivot 时间而非最后 bar；7) `ALGORITHM_VERSION == "v10"`、旧 v9 缓存失效；8) 无 console/API 错误；9) 用户提供真实 TV CSV 后 parity 测试不再 skip | P1 |
| ALIGN-076 | 市场数据 SSOT + 前复权统一出口（MDAS 唯一出口 + adj_factor 列信任 Bug 修复 + adjustment_as_of point-in-time + factor rebuild 完整重建）待生产验证 | CHANGE-20260717-002 已实现（详见 `docs/changes/records/CHANGE-20260717-002.md`）：（1）**MDAS 唯一行情出口**：`MarketDataAggregationService` 为行情读取+复权+周/月聚合唯一出口；业务层（indicator/strategy_batch/feature_snapshot/chart_bars/structural/capture/monitor/bars API）全部收口走 MDAS，禁止导入 repository 私有 `_query_*`/`_get_adj_factor_df`/`apply_adj_factor_to_bars`/旧 `get_bars`。（2）**adj_factor 列信任 Bug 修复**：`adj_factor.py::_apply_adj_factor_core` 始终用权威因子序列 `merge_asof` 结果 `_adj`，移除 `if "adj_factor" in merged.columns` 分支；pytdx hybrid bar/15m/60m/1m 行内自带 `adj_factor=1.0` 不可信，必须由权威日线因子覆盖。（3）**adjustment_as_of point-in-time**：公式 `qfq=raw×factor(bar_date)/factor(as_of)`；`AdjustmentFactorService.get_factor_series(as_of=)` 返回只含 `trade_date <= as_of` 的截断因子序列，禁止未来除权事件泄漏；盘后/历史回算 `as_of=trade_date`。（4）**日内/周/月口径统一**：日内同一交易日映射同一权威日线因子；周/月"日线完成复权后聚合"。（5）**factor rebuild 完整重建**：从最早受影响日期完整重建（禁止只更新最近 5 根），成功后精确失效该股票 MDAS/indicator 缓存，失败不得用 1.0 伪装成功返回 degraded+原因。（6）**盘后顺序**：原始日线刷新→factor 重建（成功）→覆盖率门禁/DSA→snapshot 发布；因子未完成不得创建 DSA 或发布 snapshot。（7）**MDAS v2 契约**：参数 timeframe/adj/include_realtime/completed_only/start-end/limit/warmup_bars/adjustment_as_of；返回 bars+market_data_contract_version/source_bar_hash/adj_factor_hash/adjustment_as_of/completed_through/degraded 诊断；缓存键含全部参数+版本。（8）**feature_snapshot schema v2**：盘后调用 MDAS 显式 `include_realtime=False, end_date=trade_date, adjustment_as_of=trade_date`，保存 hash/contract_version/completed_through/as_of 到 run metadata；alembic 063↔064 落库。（9）**架构守护 AST 测试**：`test_market_data_ssot_architecture.py` 5 个测试全通过。（10）**603538 真实回归**：`verify_603538_step6.py` 6 步全通过（factor rebuild 856 根/除权日 07-09 factor 0.7115→1.0；1d/15m/1h × none/qfq 价格连续；as_of 三锚点无未来泄漏；对照股 600276 factor 全 1.0；跨调用方 hash 一致 source=48d5bd812528ca42/adj=262a210aea141032；rebuild 幂等）。**PINE_PARITY_PENDING**：SMC 仅验证输入一致和无回归，不宣称 Pine 输出级完全对齐。 | 部署后生产 E2E 验证：1) 603538 1d/15m/1h 主/小 K 线 none/qfq 切换正确，除权日 07-09 前后无异常断层；2) 同一股票/周期/截止日 `/bars`/indicator/SMC/strategy_batch/feature_snapshot 的 source_bar_hash/adj_factor_hash 一致；3) 盘后链路 run metadata 含 contract_version/source_bar_hash/adj_factor_hash/completed_through/adjustment_as_of；4) factor rebuild 先于 DSA/snapshot；5) 因子失败不发布受影响结果（无 1.0 伪装）；6) 所有相关容器同一分支 SHA、RestartCount=0；7) 架构测试 5/5 通过 | P1 |
| ALIGN-077 | 日线 SMC 盘中监控 + 三类独立飞书图片 + Canonical 四链 re-export 接入待生产验证 | CHANGE-20260720-001 已实现（合并 §一-§五，详见 `docs/changes/records/CHANGE-20260720-001.md`）：（1）**§一 详情页 Node 输入修复**：`MarketDataContext` 字段拆分为 `bars_display`/`display_timeframe`/`bars_daily`/`bars_15min`/`bars_minute`；`WATCHLIST_MONITOR` required_inputs 修复为 `{daily, 15min}`；新增 `_compute_independent_node_cluster` 固定输入 completed qfq 1d×250 + 15m×4000；返回 `data.node_cluster` + `availability` + `degraded_reason`；五周期一致性测试 `TestNodeClusterFivePeriodConsistency`（1d/15m/1h/1w/1mo profile_hash 完全一致）。（2）**§二 日线 SMC 盘中监控**：新增 `backend/app/strategy/monitors/smc_monitor.py`；`WatchlistMonitor` 改为 `self._bb + self._vn + self._smc` 三合一；主输入已完成前复权日线，1m 仅用于触发检测；调用现有 SMC Canonical Adapter（禁止复制公式）；FVG 完全排除；第一版事件 `smc_bos_retest`/`smc_choch_retest`/`smc_order_block_first_touch`（含稳定 `smc_entity_id` + `touch_episode` 去重）；`MonitorState` 升级为 `bb/node_cluster/smc/market` 命名空间。（3）**§三 三类监控独立飞书图片**：新增共享枚举 `backend/app/constants/indicator_view.py`（`IndicatorView = Literal["node_cluster", "bollinger", "smc"]` + `EVENT_TYPE_TO_INDICATOR_VIEW` 自动映射 + `resolve_indicator_view`）；`IndicatorView` 贯穿 `StrategyEvent.payload` → `NotificationMessage.resource_refs` → Capture 请求 → `CaptureJob` → 输出文件名 → 缓存键 → 幂等键 → 状态查询 → 前端 URL 参数；三套 Capture Preset `FEISHU_CAPTURE_PRESETS`（layers 互斥，除共享 candlestick）；`build_monitor_event_text(indicator_view, ...)` 按 view 拆分文字卡片字段；`CaptureJob` 新增 `indicator_view` 字段（nullable 兼容历史）；监控自动发送从事件类型映射 `indicator_view`。（4）**§四 详情页发送飞书增加选择**：`SendFeishuRequest` body schema（`indicator_view: Literal["node_cluster", "bollinger", "smc"] \| None`）；前端弹窗三单选项（筹码共识价/布林带/SMC 结构）；`useStockDetailFeishu` 新增 `selectedIndicatorView`/`setSelectedIndicatorView`/`handleSendFeishu(indicatorView)`；POST body 透传到 capture worker；`output_filename` 加 `-{indicator_view}` 后缀。（5）**§五 Canonical 生产接入**：`canonical_adapters.py` 新增 re-exports 区段（node_cluster_engine/smc_view_adapter/structural_factor_service/temporal_feature_service/bollinger_features_plotly/merged_dsa_atr_rope_bb_factors/smc_indicator/sqzmom_lb）；`indicator_service.py` 移除 5 个直接 kernel import；`feature_snapshot_service.py` 移除 4 个直接 kernel import；`monitor_batch_service.py` 修复 L1847 延迟 import；`stock_capture_service.py` 早已无直接 kernel import；`compute_macd` 改为延迟 import 避免循环；`test_four_chain_no_direct_kernel_import` 移除 `xfail=True`，AST 守护升级为硬失败；`tests/allowlist.json` 移除 issue #83 条目。**诚实声明**：§五 四链当前通过 `canonical_adapters` re-export 接入（满足 AST 门禁），未全部改为 `compute_with_mdas()` 调用（后续优化）；SMC 对齐基准为京东方 A 000725 真实 golden，`PINE_PARITY_PENDING` 保留。 | 部署后生产 E2E 验证：1) `/stock/:symbol` 1d/15m/1h/1w/1mo 五周期切换，`data.node_cluster.profile_hash` 完全一致；2) 盘中 SMC 监控触发 `smc_bos_retest`/`smc_choch_retest`/`smc_order_block_first_touch` 事件，飞书收到 SMC 指标图片；3) 详情页 POST `/instruments/{id}/send-feishu` body `indicator_view=bollinger` 收到 BB 指标图片，`indicator_view=node_cluster` 收到 Node 指标图片；4) 三类 indicator_view 图片文件名/缓存键/幂等键互斥；5) `monitor_batch_service` 自动发送从事件类型映射 `indicator_view`；6) `test_four_chain_no_direct_kernel_import` 硬失败通过；7) 所有相关容器同一分支 SHA、RestartCount=0；8) 真实飞书测试渠道 E2E 三张不同图片实际投递 | P1 |

## CLOSED 摘要

| ID | 摘要 |
|---|---|
| ALIGN-004/005/019/020 | DSA 发布门禁、预算、partial_failed 发布、数量语义已收口 |
| ALIGN-006/007/008 | Watchlist、趋势 API、Worker 资格已接入统一权限/资格路径 |
| ALIGN-009 | 行情聚合与尾部补齐相关路径已收口 |
| ALIGN-011/018 | Capture Token 与 Capture Snapshot 链路已实现并测试 |
| ALIGN-016 | Node Cluster 15m 输入已修正为 4000 |
| ALIGN-017 | 飞书 Platform App only，Webhook 永久删除 |
| ALIGN-022 | `target_channel_id` 手动通知跳过资格过滤，自动通知仍过滤，已补隔离测试 |
| ALIGN-023 | worker-watchdog 生产服务已部署(67105c2)，38 条 stale running 已自动清理为 stopped，stale running=0 |
| ALIGN-024 | docs v2 结构已通过 PR #5 合并落库（cafbdc4），旧 00-18 归档，check 已适配 |
| ALIGN-032 | 趋势选股页全量 universe 展示已生产验证通过。PR #15 部署后 run_id=f0c15e1c: source_total=5293, filtered_total=5293, succeeded 行正确显示 35 个 DSA 指标，skipped 行显示股票但指标为空。修复 commit: cc025e0 (JOIN by run_id+instrument_id) |
| ALIGN-038 | monitor 图片链路 capture token claims 修复后生产验证通过。PR #43 (commit 79d96e0) 补全 `scope/user_id/instrument_id`，部署后构造 `[SMOKE_IMAGE]` 单标的事件：capture_jobs.status=succeeded、image_url 非空；Outbox delivery_type=image 已 processed；message_deliveries delivery_type=image status=success、image_upload_status=success、image_key 非空；delivery_worker 日志确认“飞书图片消息投递成功”。 |
| ALIGN-039 | capture worker 偶发 502 因 `page.goto` networkidle 超时修复后生产验证通过。PR #45 (commit 8c1f9c4) 将 `wait_until` 从 `"networkidle"` 改为 `"load"`，保留 `data-render-ready="true"` 等待 bars+indicators。部署后通过 `notification_service.test_channel_latest_event` 触发单条图片链路：capture_jobs.status=succeeded 且 image_url 非空；Outbox delivery_type=image 已 processed；message_deliveries delivery_type=image status=success、image_upload_status=success、image_key 非空；delivery_worker 日志确认“飞书图片消息投递成功”。 |
| ALIGN-056 | 详情页来源上下文丢失修复（CHANGE-20260713-009）：修复前从 `/market?scope=market` 进入 `/stock/:symbol` 时详情页左栏错误显示自选列表而非行情搜索结果（`useStockDetailActions` 使用 `useMarketStocks` 且未解析 returnTo 筛选上下文）。修复后 `MarketWorkspacePage.handleNavigateToStock` 按 scope 传递 `source`/`strategy`（market→`source=selection&strategy=dsa_selector`，watchlist→`source=watchlist&strategy=watchlist_monitor`）+ 完整 `returnTo`；`useStockDetailActions` 改用 `usePublishedRuns` + `useStrategyRunResults` + `adaptStrategyResultToTrendRow` + `getStockDisplay` 链；共享纯函数 `decodeMarketListContext`/`buildStrategyResultQueryParams`（`marketWorkspaceUrlState.ts`）由两端共用避免 filter drift；`sourceListKind` 由 `marketContext.scope` 派生（market→"行情来源"，watchlist→"自选来源"）；`normalizeInternalReturnTo` 长度限制 200→500；7 项契约测试覆盖。 |
| ALIGN-041 | qstock/THS 板块同步烟测未通过 → pywencai 唯一数据源已收口（CHANGE-20260716-007 + PR #77 收口 + PR #77 收口第二轮）：原 C10 实现的 `ths_adapter.py`+`qstock_fetcher.py` 因物理机出口 IP 被 THS 反爬拦截（成分股端点 403）无法烟测。已删除 qstock/THS 适配器，新增 `wencai_board_provider.py`（pywencai 作为唯一板块分类数据源，查询 `同花顺概念，行业分类`，`asyncio.to_thread` 包装，Referer 头，3 次重试）；`board_sync_service` 改用 `BoardSnapshot` 原子切换 + 绝对门禁（raw≥5000/唯一性≥99.9%/行业≥200/概念≥300/关系≥60000/解析率≥95%）+ 相对门禁（降幅>20% 拒绝）+ `source=wencai` 显式返回；`after_close_orchestrator` 新增 `syncing_boards` 步骤（软失败，不阻断 DSA/snapshot/publish）；pywencai 配合 `WENCAI_COOKIE` 在物理机真实烟测返回完整目录与代表性成分；PR #77 收口：industry 改关键词 ilike 匹配（`board_filter_helper.py`，NFKC + trim + 转义 `\`/`%`/`_`）、`BoardFilterCombobox.tsx` 替代原生 datalist、`Dockerfile` 安装 Node.js（pywencai `get_token()` 需要）、`config.py` `_resolve_board_sync_enabled()` 环境变量注入、249 只未解析股票分类（BJ 234 + SH 13 + SZ 2，全 DB_NOT_EXIST）；**PR #77 收口第二轮 P0 前端 + P1 后端**：BoardFilterCombobox `activeIndex=-1`/NFKC/`useId`/`suggestionRank`/清除按钮 tabIndex/无结果反馈/SCSS 宽度/title 属性；`board_filter_helper.py` concept 应用 `_normalize_keyword()` NFKC+trim 后 `==` 精确匹配；`config.py` `_resolve_board_sync_enabled()` 严格解析（truthy/falsy 集合、非法值 fail-fast、修复 `bool("false")` bug）；`after_close_orchestrator.py` 新增 `_record_board_sync_outcome()` 写入 `job_run_events` + `SchedulerJobRun.metadata_json.board_sync_result`；`market_stocks_service.py` 新增 `_get_board_sync_status_from_job()` Redis 缺失时回退 job metadata + `source="unknown"` 语义；删除 `classify_unresolved_stocks.py` 一次性调查脚本；后端 154 用例（wencai_board_provider 53 + board_sync 17 + after_close_board_sync 10 + board_filter_helper 32 + board_sync_enabled_config 28 + board_sync_status_fallback 14）+ 前端 45 用例（wencaiBoardSyncContract 37 + marketToolbarSearch 8）全通过；EXPLAIN ANALYZE 生产数据「电子」0.819ms / 「半导体」0.589ms / 「电子+光刻机」AND 1.044ms。 |

## 关闭要求

每项关闭必须有：代码 commit、测试、CI 或生产验证证据、CHANGE 记录。关闭后只保留摘要，详细历史归入 CHANGE。

## V3.3 差异闭合（CP-V3-A2/B2/C2/D/E）

### CP-V3-A2: MDAS count-aware 回补
- **差异**: MDAS `_DEFAULT_INTRADAY_LOOKBACK_DAYS=180` 导致 15m 不足 4000 根
- **修复**: count-aware 回补 — daily=250/15m=4000 自动向前扩展，返回 history_exhausted
- **状态**: CLOSED (@ ca38a0c)

### CP-V3-B2: 实时 partial bar 收口
- **差异**: /quote 实时与 /bars 1d realtime 语义混淆
- **修复**: /bars?timeframe=1d&include_realtime=true 必须返回今日 partial daily bar；**禁止前端 mergeRealtimeQuoteIntoBars 或任何 quote→bar 兜底**（CP-V3-D2 修正：Exchange→MDAS→ChartSnapshot→Canvas 为唯一 K 线链）
- **状态**: CLOSED (@ 4dab274) — 后端 partial bar 合同已实现；前端兜底禁止在 CP-V3-D2 修正

### CP-V3-C2: SMC 严格 time-key
- **差异**: SMC 渲染使用 index fallback，viewport 250→90 时标签错位
- **修复**: strictTimeKey=true 禁止 index fallback；events/EQH/EQL OR 逻辑
- **状态**: CLOSED (@ 3393d16)

### CP-V3-D: 因子版本追踪 + auto-resume
- **差异**: instruments factor 版本字段从未写入；auto-resume 缺乏受控测试
- **修复**: stamp_factor_reconciliation_version + find_stale_version_instruments；9 个 auto-resume + 6 个因子版本测试
- **状态**: CLOSED (@ 9001ca0) — 注：`find_stale_version_instruments` 定义但未接入 after-close 生产调用链（CP-V3-D2 分析）；`stamp_factor_reconciliation_version` 已接入 `_rebuild_single` 成功路径

### CP-V3-E: docs/AGENTS 收口
- **差异**: AGENTS.md 289 行超过 180-220 目标
- **修复**: 压缩到 181 行；新增 §7.12/§7.14/§7.22 规则；ADR-0003 SMC time-key
- **状态**: CLOSED

### CP-V3-A3: MDAS 长期停牌边界修正
- **差异**: `_fetch_intraday_with_backfill` 的"连续2轮无新增→history_exhausted"对长期停牌不安全
- **修复**: listing_date 作为历史下界；no_progress 有界递增步长（90→720）；max_rounds 未到边界→INPUT_CONTRACT_VIOLATION
- **状态**: CLOSED (@ d5e0848)

### CP-V3-D2: 因子版本首次运行安全 bootstrap
- **差异**: `find_stale_version_instruments` 未接入生产；8272 NULL 版本字段无安全 bootstrap；`mergeRealtimeQuoteIntoBars` 矛盾
- **修复**: `bootstrap_factor_version_baseline` 基于 dry_run 证据写版本基线；find_stale docstring 警告；禁止前端 quote→bar 兜底（04-frontend-ux/code-doc-alignment/test-coverage-map/frontend-route-map/AGENTS 同步）
- **状态**: CLOSED (@ 64514d2) — 注：bootstrap 未在生产执行（本轮不修改生产 DB）；10 needs_rebuild 清单 UNKNOWN
