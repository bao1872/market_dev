# CHANGE-20260716-007: 问财概念/行业分类接入盘后编排并用于行情筛选（pywencai 唯一数据源 + 原子快照 + 软失败编排 + 行业关键词筛选 + 自定义 Combobox）

## 变更编号
CHANGE-20260716-007

## 任务名称
同花顺概念/行业分类接入盘后编排并用于行情筛选：pywencai 唯一数据源、BoardSnapshot 原子切换、after_close 软失败步骤、行业关键词 ilike 匹配、BoardFilterCombobox 自定义下拉、Dockerfile Node.js 依赖、BOARD_SYNC_ENABLED 环境变量注入、stale 数据契约。

## 需求出处
`ref/PROMPT.md`（同花顺概念/行业分类接入盘后编排并用于行情筛选）。

## 修改前行为

### 数据源
- 板块同步使用 qstock（`qstock_fetcher.py`）+ THS 适配器（`ths_adapter.py`），依赖 py-mini-racer。
- 生产 `BOARD_SYNC_ENABLED=false`，THS 成分股端点 403 被反爬拦截，板块数据不可用。
- pywencai 0.13.1 曾探测但未加入生产依赖（CHANGE-20260714-001）。

### 同步服务
- `board_sync_service.py` 逐板块抓目录/成员，非原子快照。
- 完整性门禁为旧阈值（板块数 <100、成分数 <3000）。
- `code_uniqueness_rate` 计算为 `unique_stock_count / total_symbol_refs`（每股票多次引用导致比率极低）。

### 编排
- `worker.py` 注册独立 17:00 `scheduled_board_sync` cron job（qstock 语义）。
- `after_close_orchestrator.py` 无 `syncing_boards` 步骤。
- 板块同步与盘后流程完全独立，无断点恢复。

### 前端
- 行业/概念输入每敲一字即触发 API 请求。
- 无 stale 数据契约，`available=false` 时直接禁用。
- `/market/boards` 响应仅含 `available` + `reason_code`。

## 修改后行为

### 一、问财唯一数据源（pywencai）
1. **新增 `wencai_board_provider.py`**：固定查询 `同花顺概念，行业分类`，pywencai headers 补 `Referer: http://www.iwencai.com/`，`wc.get(query=..., loop=True, sleep=2, cookie=os.getenv("WENCAI_COOKIE") or None)`，最多 3 次有限重试。
2. **同步调用包装**：`asyncio.to_thread` 包装 pywencai 同步调用，不阻塞事件循环；不记录 Cookie 或完整原始响应。
3. **必需字段**：仅 `股票代码、股票简称、所属概念、所属同花顺行业`；忽略价格、市值、DDE、PE、market_code、数值 code。
4. **股票代码规范化**：取 `.SH/.SZ/.BJ` 前六位并保留前导 0。
5. **概念处理**：按 `;` 拆分、NFKC、trim、单股去重；超过 `MAX_CONCEPTS_PER_STOCK=100` 截断。
6. **行业规范化**：必须为「一级-二级-三级」完整路径，每股仅一个行业。
7. **stable external_code**：`wc:c:`/`wc:i:` + 规范化名称 SHA256 前 24 位；哈希冲突立即失败。
8. **兼容嵌套**：DataFrame/dict/list/tuple 嵌套，选择包含必需字段且行数最大的主表。

### 二、BoardSnapshot 原子快照同步
1. **完整快照构造**：`board_sync_service.sync_boards` 接收完整 `BoardSnapshot`（内存中全部 boards + memberships），不再逐板块抓取。
2. **绝对门禁（V1.1）**：原始行 ≥5000、代码唯一率 ≥99.9%（`unique_stock_count / raw_rows`）、行业 ≥200、概念 ≥300、关系 ≥60000、解析率 ≥95%、行业覆盖 ≥99%、每股行业恰好 1、单股概念 ≤100。
3. **相对门禁**：股票/行业/概念/关系任一下降 >20% 拒绝切换（首次同步 prev=0 不检查）。
4. **原子切换**：校验后一次批量解析 instrument，再在单一事务中差异 upsert/delete；任何异常 rollback 保留上次成功数据。
5. **刷新 updated_at**：成功同步时即使内容未变化也统一刷新有效 board 的 `updated_at`。
6. **未解析股票**：只记录总数和前 50 个脱敏样本，不把全量列表写 metadata。

### 三、盘后编排（soft-failure）
1. **新状态**：`AfterCloseRunStatus.SYNCING_BOARDS`，位于 `REFRESHING_DAILY` 与 `WAITING_DSA_WORKER` 之间。
2. **正常流程**：`refreshing_daily → syncing_boards → waiting_dsa_worker → quality_gate → feature_snapshot → publishing → succeeded`。
3. **位置**：板块同步在日线刷新后、任何「DSA 未触发提前结束」之前执行；非交易日跳过；`mode=dsa_only` 默认跳过，避免人工 DSA 重跑访问问财。
4. **软失败**：失败不覆盖旧数据、不阻断 DSA/快照/发布；写 `job_run_event` 和 metadata（status/source/completed_at/raw_rows/resolved/unresolved/industry_count/concept_count/membership_count/duration_ms/error_code/reused_previous_snapshot）。
5. **断点恢复**：无论成功或 degraded 均把该步骤记为已处理，断点恢复不重复请求问财。
6. **Redis 状态**：`record_sync_status()`/`get_sync_status()` 写入 key `board_sync:status`，TTL 7 天。

### 四、worker.py 清理
1. **删除 17:00 独立 qstock 板块任务**：移除 `scheduled_board_sync` 函数、cron 注册、`_resolve_instruments` 嵌套函数。
2. **保留 `BOARD_SYNC_ENABLED` 开关**：语义改为 pywencai；`false` 时步骤 `skipped`。
3. **不新增并行板块 scheduler**。

### 五、API 契约（/market/boards）
1. **扩展响应**：`source`（str|null）、`stale`（bool）、`last_attempt_status`（str|null）。
2. **stale 语义**：旧数据存在而最新同步失败时 `available=true, stale=true`，仍允许筛选；从未成功才 `available=false`。
3. **请求链禁联网**：`/market/boards` 继续读数据库 + Redis 状态，不在用户 API 请求链访问问财。

### 六、前端
1. **行业/概念提交修复**：各用本地输入，Enter/blur/选中时才提交；行业接受任意关键词（不再用 `industryNameSet` 拒绝/重置），概念只接受当前目录精确值；清空立即提交并重置 `page=1`。
2. **stale 显示**：stale 时控件仍可用并显示简短「沿用上次板块数据」；loading 与从未可用分开。
3. **行业显示**：可将 `-` 渲染为 `/`，API 值不变。
4. **不新增前端依赖**。

### 七、依赖清理
1. **pyproject.toml**：移除 `py-mini-racer`；新增 `pywencai==0.13.1`；保留 `qstock==1.3.1`（strategy_assets 仍用日线和交易日历）。
2. **删除文件**：`qstock_fetcher.py`、`ths_adapter.py`、`test_qstock_fetcher.py`。

### 八、行业筛选语义改为关键词匹配（PR #77 收口）
1. **完整路径存储不变**：`MarketBoard.name` 继续保存「一级-二级-三级」完整路径，禁止拆表或复制三级 membership。
2. **筛选改为 ilike 包含匹配**：`board_filter_helper.build_board_filter_conditions` 的 industry 条件从 `MarketBoard.name == industry` 改为 `MarketBoard.name.ilike('%keyword%', escape='\\')`，匹配完整路径中的任意一级。
3. **关键词规范化**：`_normalize_keyword()` 执行 NFKC 规范化 + trim；空值不生成条件。
4. **特殊字符转义**：`_escape_ilike_pattern()` 转义 `\`、`%`、`_`，避免通配符注入。
5. **industry 参数语义**：正式改为「行业关键词」；输入「电子」「半导体」「数字芯片」均应匹配所有完整行业路径中包含该词的股票。
6. **concept 保持精确匹配**：concept 暂时保持 `MarketBoard.name == concept` 精确筛选；industry + concept 继续为 AND。
7. **单一 helper 复用**：market stocks、StrategyRunResults、行情、自选和 Excel 必须继续复用同一个 `board_filter_helper`，禁止出现两套筛选逻辑。
8. **URL/preset/导出字段名兼容**：继续使用 `industry`，兼容现有链接，但文档说明其值现在是关键词。

### 九、BoardFilterCombobox 自定义下拉组件（PR #77 收口）
1. **删除原生 datalist**：`MarketToolbar.tsx` 不再使用浏览器原生 `<datalist>`，新增可复用的 `BoardFilterCombobox.tsx`，行业和概念共同使用。
2. **行业模式**（`mode="industry"`）：允许任意关键词输入，不再用 `industryNameSet` 拒绝或重置；placeholder 为「搜索行业关键词」；本地过滤完整路径，最多展示 12 条建议，展示格式为「一级 / 二级 / 三级」，高亮命中关键词；Enter 提交当前关键词；点击建议项可提交命中的完整路径；清空立即提交空值。
3. **概念模式**（`mode="concept"`）：本地搜索目录，最终只提交选中的精确概念；不得逐字符请求后端。
4. **键盘与无障碍**：支持 ArrowUp/ArrowDown/Enter/Escape、点击外部关闭、清除按钮、`aria-combobox`/`aria-listbox`/`aria-option`；通过 150ms blur 延迟解决 blur 先于点击造成选项无法选择的问题。
5. **样式**（盘迹 SCSS 变量，不新增依赖）：不使用浏览器原生下拉；输入高度与股票搜索框一致；行业宽 200~240px，概念宽 160~200px；弹层深色 panel、1px 边框、8px 圆角、轻阴影；focus 和选中使用盘迹荧感绿；hover 使用低透明青绿色；最大高度 320px、内部滚动、z-index 高于表格和批次信息；1440 和 1920 宽度下不遮挡或溢出。

### 十、同步链路遗留修复（PR #77 收口）
1. **Dockerfile Node.js 依赖**：`backend/Dockerfile` 安装 `nodejs`（pywencai 的 `get_token()` 通过 `subprocess.run(['node', ...])` 执行 `hexin-v.bundle.js` 计算反爬 token）；纳入新 commit，不能让标记为 c41799c 的镜像与 Git 代码不同。
2. **source=wencai 修复**：`board_sync_service.sync_boards()` 成功返回的 dict 显式带上 `source: "wencai"`，防止手工调用 `record_sync_status(result)` 时丢失 source；`/market/boards` 不再返回 `source=None`。
3. **BOARD_SYNC_ENABLED 环境变量注入**：`config.py` 新增 `_resolve_board_sync_enabled()`，优先级为「环境变量 > CONFIG_FILE > 默认 False」；`docker-compose.prod.yml` 的 worker-after-close 服务显式注入 `BOARD_SYNC_ENABLED: ${BOARD_SYNC_ENABLED:-false}`；盘后任务启用时真实执行 `syncing_boards`，禁用时明确 `skipped`，不依赖手工 python 同步。
4. **249 只未解析股票分类**（仅调查，不降低 95% 门禁）：全部为 `DB_NOT_EXIST`，按交易所分类为 BJ 234 + SH 13 + SZ 2；BJ 以 920xxx 新代码为主（北交所新股，数据库未收录）；调查脚本 `backend/scripts/classify_unresolved_stocks.py`，不把全量样本写日志。

## 影响模块
- 后端：`wencai_board_provider.py`（新增）、`board_sync_service.py`（重构 + source=wencai）、`after_close_orchestrator.py`（新增步骤）、`market_stocks_service.py`（响应扩展）、`worker.py`（清理）、`schemas/market_stocks.py`（响应扩展）、`pyproject.toml`、`board_filter_helper.py`（industry 改 ilike 关键词匹配）、`config.py`（`_resolve_board_sync_enabled`）、`Dockerfile`（Node.js 依赖）、`docker-compose.prod.yml`（BOARD_SYNC_ENABLED 注入）。
- 前端：`endpoints.ts`（类型扩展）、`MarketToolbar.tsx`（删除 datalist，改用 BoardFilterCombobox）、`BoardFilterCombobox.tsx`（新增自定义 Combobox）、`MarketWorkspace.module.scss`（Combobox 样式）、`MarketWorkspacePage.tsx`（stale 传递）、`StrategyDataTable.tsx`（preset 校验跳过 industry 关键词检查）。
- 测试：`test_wencai_board_provider.py`（新增）、`test_board_sync.py`（重写 + source=wencai）、`test_after_close_board_sync.py`（新增）、`test_board_filter_helper.py`（新增，ilike 匹配/转义/NFKC/AND）、`test_board_sync_enabled_config.py`（新增，环境变量解析）、`wencaiBoardSyncContract.test.ts`（重写，28 用例）、`marketToolbarSearch.test.ts`（更新 keywordInput）。
- 调查脚本：`backend/scripts/classify_unresolved_stocks.py`（新增，249 只未解析股票分类）。
- 删除：`qstock_fetcher.py`、`ths_adapter.py`、`test_qstock_fetcher.py`。

## 修改文件
- `backend/app/services/wencai_board_provider.py`（新增）
- `backend/app/services/board_sync_service.py`
- `backend/app/services/after_close_orchestrator.py`
- `backend/app/services/market_stocks_service.py`
- `backend/app/schemas/market_stocks.py`
- `backend/app/worker.py`
- `backend/app/repositories/board_filter_helper.py`（PR #77：industry 改 ilike 关键词匹配）
- `backend/app/config.py`（PR #77：`_resolve_board_sync_enabled`）
- `backend/Dockerfile`（PR #77：Node.js 依赖）
- `docker-compose.prod.yml`（PR #77：BOARD_SYNC_ENABLED 注入）
- `backend/pyproject.toml`
- `backend/tests/test_wencai_board_provider.py`（新增）
- `backend/tests/test_board_sync.py`（重写 + source=wencai 用例）
- `backend/tests/test_after_close_board_sync.py`（新增）
- `backend/tests/test_board_filter_helper.py`（新增，PR #77）
- `backend/tests/test_board_sync_enabled_config.py`（新增，PR #77）
- `backend/scripts/classify_unresolved_stocks.py`（新增，PR #77 调查脚本）
- `frontend/src/api/endpoints.ts`
- `frontend/src/features/market-workspace/MarketToolbar.tsx`（PR #77：删除 datalist，改用 BoardFilterCombobox）
- `frontend/src/features/market-workspace/BoardFilterCombobox.tsx`（新增，PR #77）
- `frontend/src/features/market-workspace/MarketWorkspace.module.scss`（PR #77：Combobox 样式）
- `frontend/src/features/market-workspace/MarketWorkspacePage.tsx`
- `frontend/src/components/StrategyDataTable.tsx`（PR #77：preset 跳过 industry 校验）
- `frontend/src/features/market-workspace/__tests__/wencaiBoardSyncContract.test.ts`（重写，28 用例）
- `frontend/src/features/market-workspace/__tests__/marketToolbarSearch.test.ts`（更新 keywordInput）
- `backend/app/services/qstock_fetcher.py`（删除）
- `backend/app/services/ths_adapter.py`（删除）
- `backend/tests/test_qstock_fetcher.py`（删除）

## 测试
- 后端 provider 测试：53 passed（股票代码规范化、名称 NFKC、概念拆分/去重、行业路径、external_code、哈希冲突、列匹配、BoardSnapshot 构建、主 DataFrame 选择、provider info）
- 后端 board_sync 测试：17 passed（绝对门禁 5 + 相对门禁 5 + 原子切换 5 + 详细计数 1 + source=wencai 1）
- 后端编排测试：10 passed（状态枚举、instrument 解析、_completed_steps 集成、dsa_only 跳过、软失败）
- 后端 board_filter_helper 测试：23 passed（PR #77：一级/二级/三级/局部关键词匹配、`%/_/\` 转义、NFKC 规范化、industry+concept AND、concept 精确匹配、空值不生成条件）
- 后端 board_sync_enabled_config 测试：12 passed（PR #77：env var 1/true/yes/on/TRUE/false/0/no/off/empty/precedence/default）
- 前端契约测试：28 passed（PR #77：BoardFilterCombobox mode/keyboard/12-item max/display/clear/blur/aria/click-outside/highlight/no逐字符/clear empty + 类型字段/stale 提示/disabled）
- 前端 marketToolbarSearch 测试：8 passed（keywordInput 受控、Enter/blur 提交、placeholder）
- ruff：All checks passed
- mypy：changed files 0 errors（pre-existing errors in untouched files 不变）
- tsc：0 errors
- eslint：0 errors 0 warnings
- vite build：Success

## Known Gaps
- 真实问财同步测试不进入 CI，只在部署后执行一次（PROMPT §七）。
- `qstock==1.3.1` 保留：`strategy_assets` 仍用 qstock 获取日线和交易日历（非板块链路）。
- 外网真实问财测试需 `WENCAI_COOKIE` 环境变量；未设置时 pywencai 以无 cookie 模式请求，可能被 captcha 拦截。
- 249 只未解析股票全部为 `DB_NOT_EXIST`（BJ 234 + SH 13 + SZ 2），以 920xxx 北交所新股为主；不降低 95% 门禁，待 instrument 基础库补充北交所新代码。

## 最终状态
- **Status**: READY_FOR_DEPLOY
- **Branch**: `feat/wencai-board-sync-v1`（base: `feat/next-phase-20260716`）
- **PR**: #77
- **Known Gap**: 真实问财同步 + 生产部署 + 浏览器验证待执行（PROMPT §六）。
